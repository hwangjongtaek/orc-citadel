# Orc Citadel — 로드맵 & 진행 관리

> 이 문서는 프로젝트 **진행 내역을 추적**한다. 스펙 정의는 [`docs/design/`](./design/README.md)(SSOT)에 있고, 여기서는 "무엇을 언제 어디까지 했는가"만 관리한다.
> 규칙: 설계·구현 변경은 (1) 해당 design 문서 수정 (2) `design/README.md` Spec version 반영 (3) 본 문서 §5 Changelog 기록의 3단계를 거친다.

- **최종 갱신:** 2026-09-10
- **현재 단계:** durable asynchronous read-only investigation (W3) 구현 완료 — graph/curated mutation 없이 PostgreSQL job·worker·Council polling으로 기존 evidence 조사 영속화
- **Spec version:** 1.2.0 · **Ontology version:** 1.0.0

## 1. 상태 요약 (한눈에)

| 트랙 | 상태 | 비고 |
| --- | --- | --- |
| 상세 설계 (Design SSOT) | ✅ Stable | 11개 문서 **Stable 확정**(현재 global Spec 1.2.0). 리뷰 패스(BLOCKER 3 + MAJOR 36 해소)·상호 일관성 재검증 전항목 PASS. **SLO-06 무비용 LLM 실측 확정(잠정)**(2026-08-19 pass_rate 1.0 n=10 → A24 n=30 재확정 · **2026-09-02 5일치 118건 축적 점검**) · SLO-02/03/04 실측 확정 · SLO-01/05/07/08·tool JSON schema 는 명시적 deferred(A21 · 11 §2.3, 07 §7) |
| 도메인·소스 선정 | ✅ 완료 | AI 반도체·데이터센터 공급망 확정, 초기 Scout 5종 선정(04 §1.4) |
| 1만 문서 샘플 | ✅ 완료 | 2026-08-11 arXiv metadata 1만 + RSS/SEC — 총 raw 11,361건 |
| Prototype 구현 | ✅ 완료 (S1–S47 + Q2/Q3/Q5) | 결정적+LLM 하이브리드 파이프라인 · bitemporal · 소비 계층(S28–31) · 평가/승격 트랙(S33–41) · **조사 에이전트 트랙(S43–47)** 구현 · Q2·Q3 해소 · test 스위트 514 Green · MVP 10/10 · Phase 1 DoD ①② (10만 재처리 · source-span 보고서 E2E) · Q4 3게이트 전항 PASS ([§5 Changelog](#5-changelog)) |
| Phase 6(Stable 운용) — 코드 봉인 축 | ✅ 완결 (스위트 1064) | deferred SLO 하니스(SLO-01 923·05/06/07/08 948) · tool JSON schema(967) · Watchtower SLO 경보 라우팅(984) · SLO nightly 게이트(998, DoD ② 운용 루프) · **SLO-06 무비용 LLM 실측 확정(잠정)**(A21 pass_rate 1.0 → A24 n=30 재확정·A28 1064 Green). **2026-09-02 Phase 6 deferred 점검 런 완결**: SLO-06 5일치 축적·#4 308건 교차 모순 0건(미검출 정직 기록)·SLO-07 0건 유지·1M 미달 확인(105,150 docs)으로 honest-gap(§6.2)·precision-first(§3-4) 규약 준수 봉인 |

범례: ✅ 완료 · 🟡 진행 중 · ⬜ 예정 · ⛔ 블록됨

## 2. 설계 문서 진행 (Design SSOT)

[`docs/design/`](./design/README.md) 12개 문서의 작성 상태. 상태는 각 문서 헤더의 레전드(Draft/Review/Stable)와 동기화한다.

| # | 문서 | 작성 | 리뷰 | 확정 (Stable) |
| --- | --- | :---: | :---: | :---: |
| — | README (인덱스·규약) | ✅ | ✅ | ✅ (Spec 1.2.0) |
| 01 | architecture | ✅ | ✅ | ✅ (Spec 1.0.0) |
| 02 | ontology | ✅ | ✅ | ✅ (Spec 1.0.0) |
| 03 | storage-and-data-model | ✅ | ✅ | ✅ (Spec 1.2.0) |
| 04 | ingestion-and-parsing | ✅ | ✅ | ✅ (Spec 1.0.0) |
| 05 | resolution-and-extraction | ✅ | ✅ | ✅ (Spec 1.0.0) |
| 06 | graph-service | ✅ | ✅ | ✅ (Spec 1.0.0) |
| 07 | llm-and-agents | ✅ | ✅ | ✅ (Spec 1.2.0) |
| 08 | search-and-graphrag | ✅ | ✅ | ✅ (Spec 1.0.0) |
| 09 | api | ✅ | ✅ | ✅ (Spec 1.2.0) |
| 10 | evaluation-and-testing | ✅ | ✅ | ✅ (Spec 1.0.0) |
| 11 | observability-and-governance | ✅ | ✅ | ✅ (Spec 1.2.0) |

**설계 단계 종료 조건 (충족, 2026-08-12):** 12개 문서 전부 Review 통과 + 상호 참조 일관성 검증(깨진 링크 0·OptionA PASS) + Phase 0 완료 조건(§3)에 매핑되는 스펙 확정.**11개 문서 Stable 확정**(Spec 1.0.0). 명시적 deferred: 11 §2.3 SLO-01/05/06/07/08·07 §7 tool JSON schema — 실측 후 확정.

## 3. 단계별 로드맵 (Blueprint §16 → 실행 계획)

각 Phase의 완료 조건(DoD)은 blueprint에서 가져와 검증 가능한 체크리스트로 만든다. 담당 스펙 문서를 함께 표기한다.

### Phase 0 — 설계·데이터 검증 (1~2주) · *현재*

| 작업 | 담당 스펙 | 상태 |
| --- | --- | :---: |
| 초기 도메인·source 3~5개 선정 | [04](./design/04-ingestion-and-parsing.md) | ✅(5종 확정) |
| 최소 ontology 정의 | [02](./design/02-ontology.md) | ✅(v1.0.0 초안) |
| 데이터 이용 조건(라이선스) 검토 | [11](./design/11-observability-and-governance.md) | ✅(04 §1.4 반영) |
| 1만 문서 샘플 확보 | [04](./design/04-ingestion-and-parsing.md) | ✅ arXiv metadata 경로(04 §1.4 CC0)로 1만 수집 완료 — saved 9,979 / skipped 21 / errors 0, 총 raw 11,361건 (2026-08-11) |
| provenance·bitemporal 모델 prototype | [03](./design/03-storage-and-data-model.md) | ✅ |
| 결정적+LLM 하이브리드 파이프라인 prototype | [05](./design/05-resolution-and-extraction.md) | ✅ S1–S42 (수집→어세션→캐노니컬→모순→승격) |
| 소비·조사 에이전트·평가 트랙 | [07](./design/07-llm-and-agents.md), [10](./design/10-evaluation-and-testing.md) | ✅ S28–S47 (read-only 소비·조사 루프·평가/승격·성능) |

**DoD:** ① 원문↔graph element 왕복 추적 가능 ② 동일 문서 재처리 시 중복 mutation 없음.

> **Phase 0 → Phase 1 진입 게이트 (2026-08-03):** ① 1만 문서 샘플 확보(사용자 arXiv 실행) ② Q4/Q6 측정 게이트는 Phase 1 부하에서 첫 판정 ③ 골든·승격 기준선(22건, S41/B2) 재검증. DoD ①②는 prototype에서 이미 충족(S27/S37 E2E 검증).
> **게이트 판정 (2026-08-11):** ① ✅ 총 raw 11,361건 확보 ③ ✅ 골든 22건 로드·평가 스위트 무회귀(canonicalization F1=1.0 유지, suite PASS)·promotion dry_run INITIALIZED/passed — 호스트·컨테이너 양쪽 동일 결과. ②는 예정대로 Phase 1 부하에서 첫 판정.

### Phase 1 — 10만 문서 MVP (3~5주) · *완결 (2026-08-12)*

| 작업 | 담당 스펙 |
| --- | --- |
| raw/normalized/curated 저장 계층 | [03](./design/03-storage-and-data-model.md) |
| parsing·dedup | [04](./design/04-ingestion-and-parsing.md) |
| entity·claim extraction | [05](./design/05-resolution-and-extraction.md) |
| Neo4j graph 적재 | [06](./design/06-graph-service.md) |
| 기본 graph explorer | [09](./design/09-api.md), DESIGN.md |
| 5개 조사 질문 end-to-end | [07](./design/07-llm-and-agents.md), [09](./design/09-api.md) |

**DoD:** ① 10만 문서 전체 재처리 가능 ② 최종 보고서 검증 가능 문장에 source span 연결.

> **Phase 1 완결 (2026-08-12) — 교차 검증 후 기록:**
> - **MVP 10/10 충족** ([§4](#4-mvp-최종-성공-기준) 전항 ✅). 10만 docs 처리(#1)·전체 재처리(#2)·평가 수치 공개(#4)·조사 에이전트 탐색(#7) 등.
> - **DoD ① ✅** — 로컬 raw **104,544건** 수집 후 날짜 윈도우(S50)·선형화 병목 해소로 전체 재처리 완주 ([changelog](#5-changelog) 149·146).
> - **DoD ② ✅** — `get_evidence_provenance`가 extraction_record char span을 trail에 연결, 5개 조사 질문 E2E 보고서가 검증 가능 문장(source-span) 산출 (`test_investigation_e2e.py`).
> - **Phase 0→1 게이트 ② (Q4) ✅ 판정** — 실신호 본문 그래프(194노드·49엣지)·합성 10만 조회에서 **3개 게이트 전항 PASS** (p95 ≤ 1.16ms ≪ 500ms · 노드 ≪ 1e6 · 재구축/증분=1.06 ≪ 10×) → Memgraph 교체 트리거 없음 ([06 §9](./design/06-graph-service.md)). 1e6/p95-500ms 는 Phase 4 부하에서 재판정(게이트 계약 유지).
> - **Q6 (Iceberg) 판정: Phase 4(100만) 유보** — 승격 트리거가 "테이블 >수천만 row·스키마 진화"(01 §122)라 현재 10만 스케일은 **관측 미대상** → 승격 결정 유보 ([01 §120-122](./design/01-architecture.md), changelog 146).

### Phase 2 — Entity·Claim 품질 (6~8주) — *완결 (2026-08-12)*

| 작업 | 담당 스펙 | 상태 |
| --- | --- | --- |
| 후보 blocking + Entity Resolution | [05](./design/05-resolution-and-extraction.md) | ✅ 결정적 ER(ADR-507) + entity pair 골든 평가·오병합률(≤0.02) |
| Claim Canonicalization | [05](./design/05-resolution-and-extraction.md) | ✅ F1=1.00 실측 (MVP #4) |
| 출처 계보 모델 | [04](./design/04-ingestion-and-parsing.md), [11](./design/11-observability-and-governance.md) | ✅ dedup_clusters + lineage 골든 평가(dup P/R, gate ≥0.98) |
| contradiction candidate 생성 | [05](./design/05-resolution-and-extraction.md) | ✅ ConflictCandidate + 골든 측정 전환 회귀 |
| quarantine·review workflow | [05](./design/05-resolution-and-extraction.md), [06](./design/06-graph-service.md) | ✅ review→골든 파생 (derive_golden, ADR-506) |
| 골든 데이터셋 구축 | [10](./design/10-evaluation-and-testing.md) | ✅ claim·entity pair·lineage 골든 확장 (measured 전환) |

**DoD:** ① 핵심 품질 지표 자동 계산 — ✅ `metrics_report` 전 축 자동(골든 존재 시 measured, 부재 시 vacuous pass 없이 honest-gap §6.2) · ② entity merge 감사·rollback 가능 — ✅ `merge_audit`·`audit_rollback`.

> **Phase 2 완결 블록업 (2026-08-12):** 6 작업 + DoD ①② 전항 구현·회귀 봉인 완료 — 스위트 535 Green. 실데이터(curated.duckdb) 측정: claim F1=1.00·ER P=1.00 오병합률=0.0000 measured PASS, **contradiction·lineage는 실데이터에 해당 골든이 자연 발생하지 않아 정직하게 미측정(honest gap §6.2)** — 실데이터 재수집 시 measured 전환 가능(단위 테스트로 경로 봉인). 다음: Phase 3 (Research Agent — Planner·Counter-Evidence·Synthesis, 조사 budget).

### Phase 3 — Research Agent (완결, 2026-08-12)

| 작업 | 담당 스펙 | 상태 |
| --- | --- | --- |
| 조사 품질 평가 하네스 (§12.3) | [10](./design/10-evaluation-and-testing.md) | ✅ golden question 전 지표 채점 (coverage·연결률·지지율·독립증거·반증·modality·부호) |
| Investigation Planner | [07](./design/07-llm-and-agents.md) | ✅ 질문→subclaim 트리 + known/gap 라벨 (§3.2) |
| 조사 budget·종료 + LLM routing | [07](./design/07-llm-and-agents.md) | ✅ STOP=(A∧B∧C)∨D + 승급 게이트 (§2.3·§4.3, ADR-706) |
| Synthesis·Audit 역추적 chain | [07](./design/07-llm-and-agents.md) | ✅ 문장→claim→source span 역추적, 연결률=1.0 (§3.8/§3.9) |
| 비용·evidence coverage 대시보드 | [11](./design/11-observability-and-governance.md) | ✅ D8 coverage·독립증거·비용·latency (§2.2) |

**DoD:** ① Agent가 그래프 공백 탐색·신규 evidence 추가 — ✅ `test_investigation_dod` (조사가 그래프 evidence를 탐색, coverage ≥ 0.80 + Audit 역추적) · ② 반증 탐색 제거 버전 대비 평가 점수 향상 — ✅ AB 검증: baseline(반증 제거, recall 0.0) 대비 counter-evidence 버전 **반증 발견률 1.0 (≥0.70 PASS)로 향상**, 연결률=1.0 유지, 골든 부재 축 honest-gap.

> **Phase 3 완결 블록업 (2026-08-12):** 5 작업 + DoD ①② 구현·회귀 봉인 완료 — 스위트 **598 Green** (Phase 2 종료 535 → +63 · 회귀 0). Phase 3(Research Agent)이 규칙 스텁을 **평가 가능한 조사 체계**로 실체화: 조사 품질 하네스(§12.3·DoD ② 증명 도구) · Planner(subclaim 트리·known/gap) · budget/종료·routing(§4.3·ADR-706) · Audit 역추적 chain(연결률=1.0, evidence-first) · D8 대시보드. DoD ① "그래프 공백 탐색·evidence 추가" + DoD ② "반증 제거 대비 점수 향상(AB)" 통합 검증으로 봉인. read-only·결정적 원칙 유지, Spec 0.1.9. 다음: Phase 4 (100만 문서 확장 — 분산 batch·증분 graph·embedding/LLM batch·ClickHouse).

### Phase 4 — 100만 문서 확장 (11~12주)

### Phase 4 — 100만 문서 확장 (11~12주)

| 작업 | 담당 스펙 |
| --- | --- |
| 분산 batch 처리 | [01](./design/01-architecture.md) |
| 증분 graph update | [06](./design/06-graph-service.md) |
| 대량 embedding·LLM batch inference | [07](./design/07-llm-and-agents.md), [08](./design/08-search-and-graphrag.md) |
| ClickHouse 분석 | [11](./design/11-observability-and-governance.md) |
| full rebuild vs partial recomputation benchmark | [10](./design/10-evaluation-and-testing.md) |

**DoD:** ① 100만 문서 처리 시간·비용 공개 ② 신규 문서 SLO 내 graph 반영.

> **Phase 4 완결 블록업 (2026-08-12):** 5 작업 + DoD ①② 통합 검증 완료 — 스위트 **719 Green** (Phase 3 종료 598 → +121 · 회귀 0, Spec **0.1.9 유지** — Phase 4 전 작업 read-only·결정적·mock/실측 격리). `distributed_batch`(01 §6 분산 batch — shard·벽시계·SLO) · `graph_incremental`(06 §7.2 증분 rebuild — 정합성 = full replay, ratio > 10×) · `batch_inference`(07·08 embedding 핀 bge-m3/LLM batch 50% 절감) · `analytics_promotion`(11 ClickHouse 승격 트리거) · `phase4_report`(DoD ①② 통합 공개). **DoD ① "100만 처리 시간·비용 공개"** — `phase4_report`가 1M 투영(병렬·batch 절감)을 공개하되 **결정적 모델 투영이 실측이 아니므로 정직히 measured=False** 명시 (10 §6.2 honest-gap — 실제 부하·실측 백엔드는 분산/embedding/ClickHouse 승격 단계에서 재측정) · **투영 분모 실측 고정(2026-08-18):** SLO-08 전체 104,677건 병렬 실측(3.191ms/doc, k=10) 기준 1M 병렬 ≈53.2min·순차 ≈271min (물리 1M 미실행 → measured=False 투영 유지) · **DoD ② "신규 문서 SLO 내 graph 반영"** — `test_phase4_dod`가 실제 `GraphService`로 incremental 재적용 == full replay 정합성 + ratio > 10× + `compute_graph_slo`(p95 ≤ 60s) `ok` 판정으로 통합 검증 (SLO 위반·미측정은 정직 slo-gate/not-measured).

### Phase 5 — 1,000만 문서 Challenge

| 작업 | 담당 스펙 | 상태 |
| --- | --- | --- |
| 지속적 Signal Spire 알림 | [11](./design/11-observability-and-governance.md) §4 | ✅ 봉인 (스위트 747) |
| signal source adaptive scheduling | [04](./design/04-ingestion-and-parsing.md), [01](./design/01-architecture.md) | ✅ 봉인 (스위트 785) |
| hot/cold graph 분리 | [06](./design/06-graph-service.md) | ✅ 봉인 (스위트 813) |
| impact graph 부분 재계산 | [06](./design/06-graph-service.md), [10](./design/10-evaluation-and-testing.md) | ✅ 봉인 (스위트 842) |
| 다국어 ER | [05](./design/05-resolution-and-extraction.md) | ✅ 봉인 (스위트 872) |
| ontology migration 자동화 | [02](./design/02-ontology.md) | ✅ 봉인 (스위트 902) |

(상세 계획은 Phase 4 종료 시 확정 — Signal Spire 첫 증분으로 진입.)

> **Phase 5 완결 블록업 (2026-08-12):** 6 작업 + 설계 Review→Stable 확정 준비 완료 — 스위트 **902 Green** (Phase 4 종료 719 → +183 · 회귀 0, Spec **0.1.9 유지** — Phase 5 전 작업 read-only·결정적·mock/실측 격리). `signal_spire`(11 §4 지속 신호 알림 — 5 트리거·fire-once·alert 스키마) · `signal_scheduler`(04·01 신호 수율 기반 수집 예산 배분 — floor 보장+수율 비례, freshness) · `graph_temperature`(06 hot/cold 계층 분리 — 접근 온도·Q4 노드 게이트 아카이브·조회 SLO 라우팅) · `impact_graph`(06·10 영향 하류 부분 재계산 — 전파 BFS 범위·절감 게이트) · `multilingual_er`(05·ADR-507 교차-스크립트 해소 전단부 — 스크립트 탐지·NFKC 정규화·bridge·항상 POSSIBLY) · `ontology_migration`(02 §6 버저닝·거버넌스 계획·판정 — 버전 분류·호환·proposal·backfill 변환). Phase 5 는 1,000만 문서 Challenge 의 **확장 엔지니어링 계약 봉인**(10M 스케일 운용 준비) — 실제 1,000만 문서 처리·실측 부하는 수행되지 않았으며 이 작업들은 **정직히 `measured=False` 계약**의 산출물이다 (10 §6.2 honest-gap — 분산·재구축·아카이브·마이그레이션 등 10M 부하 판정 게이트는 실측 백엔드에서 재측정 대상). §1 현재 단계·§3 Phase 5 완결 마킹. **다음: 설계 문서 Stable 확정 — 완료 (2026-08-12, 11개 문서 Spec 1.0.0).**

### Phase 6 — Stable 운용 (측정 기반 운영 · 미정의, 신규 정의)

> blueprint §16 은 Phase 5 까지만 정의한다. **Phase 6 은 이 프로젝트가 새로 정의하는 진행 단계** — Phase 1–5 가 prototype 계약 봉인(단위 테스트)이었다면, Phase 6 은 **Stable(Spec 1.0.0) 스펙 위에서 deferred 항목 실측 · measured 전환 · 지속 수집 운용** 을 수행한다. Phase 1–5 의 모든 완결 블록업이 "실측 백엔드에서 재측정 대상"으로 남긴 의무(honest-gap §6.2)를 이행하는 단계. **즉 Phase 6 는 단위 테스트 추가가 아니라 실측·운영 작업**이며, 게이트·SLO 판정은 CI 비차단 nightly 로 수행한다(10 §1.4).

| 작업 | 담당 스펙 | 성격 | 상태 |
| --- | --- | --- | :---: |
| 수집 지속 운용 — 1만 샘플 → 10만 → (더) 수집 | [04](./design/04-ingestion-and-parsing.md), [11](./design/11-observability-and-governance.md) | 실측 | 🟡 가동 중 — **2026-09-02 총 raw 105,150건**(arXiv 104,471·semiengineering 240·tomshardware 194·nvidia 114·amd 41·nist 40·bis 37·sec 13) · **A25: Tom's Hardware 독립 언론 RSS 소스 신설, raw 104,915→104,965(+50, press-tomshardware — 모순 자연발생 재료)** · **A26: nightly_collect 배치, raw 104,965→104,970(+5, 일 단위 피드 캡처 실증)** · **A27: nightly dispatch 를 cron → APScheduler 상주 전환(단일 호스트·영속 job store, 셸 cron hang 우회 — 07:07 수집·07:37 SLO-06)** · A5 **104,642→104,677(+35)** · A6 **104,677→104,713(+36)** · A7 **104,713→104,725(+12)** · A8 **104,725→104,735(+10)** · A9·A11 **104,735→104,806(+71, gov 중복 정리 후)** · A14 **104,854→104,868(+14)** · **A15 (b)·URL-skip 적용: +0(중복 재수집 차단)** · **A16: +0(URL-skip 지속, 무중복)** · **A23: 수집 지속 raw 104,915 유지(+0, 전 skip — BIS 37 포화 고착·A22 신규 known 승계)** · **A22 (후보 1): RSS+BIS sitemap 수집 지속, raw 104,906→104,915(+9, nvidia +1·semi +8·BIS 37 포화 전 skip)** · **A20 (후보 a·b): 수집 지속(+0 무중복) + gov/규제 신호 타깃 재평가(재기각·DPL CSV 발견), raw 104,906 유지** · **A19 (후보 1·2·3): RSS/sitemap 수집 지속 + gov 확장 실측(미채택) + deferred SLO 재검토, raw 104,905→**104,906**(+1)** — **A18 (Spec 1.1.0): BIS sitemap gov 커넥터 신설, raw 104,868→**104,905**(+37)** — arXiv **windows=1(최신 연대) 신규 +8(A12)**(과거 연대 소진 ≠ 최신 소진 — 새 논문 지속 유입, 단 A14 는 arXiv errors=1 로 회복·skip). **A13 중복 판정:** arXiv format 혼재(1,247건)는 설계 04 **§2.2 의도적 버전/형식 보존**(ADR-301, Atom 메타+전체 page 두 형식, http 200) · 동일-content 재수집은 **§2.1 S2 content-hash dedup 이 이미 방지**(실증 A12 skip 92/100) → **코드 변경 없음, 기존 데이터 파괴 없음, URL 강제 skip 은 §2.2 와 충돌로 미채택**. 근본 리스크(동적 소스)는 gov 제거로 현 소스셋에 없음. **A15 URL 기반 idempotency(S1) 전면 적용** — `collect_rss(known_urls=...)`+`_stored_urls`, RSS 3 source 에 배선해 **이미 저장 URL 재수집 차단**(A15 실측: 전 skip, raw +0 — 이전 "saved:20" 이 중복 재-harvest 였음을 확정). **RSS 3 source + press-tomshardware(독립 언론, A25) 가 지속 신규 주축**(URL-skip + S2 dedup 으로 무중복). **SLO-05 런별 독립**: n 은 런 자체 시도 수(in-memory) — A12 n=140·A14 n=40·A15 n=0(전 skip, fetch 아님) · 성공률 1.0. 단 수집 오류·중복 계수는 honest 반영 |
| SLO-01 실측 — 신규 문서 → graph 반영 지연 p95 | [11](./design/11-observability-and-governance.md) §2.3 | 실측 후 확정 | 🟢 **measured=True (2026-08-12)** — 신규 RSS 수집 77건 → 실제 `bulk_pipeline` 반영 `{p95_ms:495,638 (~8.3min), n:77, classified:'ok'}` ≤ 30min 목표 (하니스 봉인 923) |
| SLO-05 실측 — source 수집 성공률 | [11](./design/11-observability-and-governance.md) §2.3 | 실측 후 확정 | 🟢 **measured=True (2026-08-12)** — SEC(2 CIK)+RSS(3 source) 실제 수집 **표본 n=50** `{success_rate:1.0, n_success:50, n_attempt:50, n_unknown:0}` · **목표(≥99%) `within_slo=True`** (하니스 봉인 948·관측 로그·배선 완결) · **후속 수집 런 표본 n (런별 독립, 2026-08-18):** A5(total=400) n=440 · A6(windows=10,total=1500) n=1,540 · A7(windows=16,total=2000) n=2,040 · A8(windows=20,total=2000) n=2,040 — 전부 `{success_rate:1.0, n_unknown:0}`. ⚠️ **정정:** `slo_log` 가 in-memory(`SloObservationLog`, 파일/DB 영속 없음)라 **런 간 누적 없음** — 표본 n 은 arXiv `total` 파라미터 연동(이전 "n=50 → n=440 → n=1,540 → n=2,040 누적 확대" 서술 부정확). 원점 n=50(2026-08-12)에서 A8(current)까지 성공률 1.0 유지 |
| SLO-06 실측 — schema validation 통과율 | [11](./design/11-observability-and-governance.md) §2.3 | 실측 후 확정 | 🟢 **measured=True (2026-08-12)** — 실제 LiteLLM judge(`bunker-flash`) **40건 판정** `{pass_rate:1.0, n_pass:40, n_total:40, n_unknown:0}` · **2026-09-02 점검**: 7d 누적 로그(`data/slo06_accum.json`) 5일치(118건) 축적 확인, 7일치 미달 및 8/27 이후 모델 추론 토큰에 따른 스키마 실패 확인 → **확정(잠정) 유지** (honest-gap §6.2) · **2026-09-09 잘림 실패 해소 실측**: LLM env 복구(litellm `bunker-flash`) + `LLM_MAX_TOKENS=8192` 후 일 표본 **30/30 valid (1.00)** — 8/22 이후 연속 0.00 이 env 결함·completion 예산 잘림이었음을 확인. 7d 윈도는 0.33(구 fail 포함) — 연속 pass 로 회복 대기 |
| SLO-07 실측 — quarantine 체류 시간(중앙값) | [11](./design/11-observability-and-governance.md) §2.3 | 실측 후 확정 | 🟡 하니스 봉인 (스위트 948) + **관측 로그·배선 완결(2026-08-12)** · **조사 완결(2026-08-18): `not-measured` 재정의 — 근본 원인이 "재평가 인프라 부족"이 아니라 "현 실데이터·정밀 우선 파이프라인에서 게이트 quarantine=0"**. 실측 증거: 5,000·8,000건 슬라이스 `open_quarantine=0·dwell=0` — 비해소 subject claim 은 `extract_claims` 가 추출 단계에서 폐기(`if not subject_id: continue`, 02 §4-3·05 §3 precision-first) 해 게이트까지 안 옴, 나머지 mention(2,347건) 전부 해소·promote. 이전 71건 `unknown_predicate` quarantine 은 **predicate 정합화로 제거**(quarantine 모집단 소실). → **SLO-07 임계(≤3d) 유지 확정(0 건 quarantine = 위반 아님)이나, dwell-time 분포가 "측정 불가(모집단 0)"** — 재평가 인프라를 구축해도 실데이터에서 측정할 것이 없음(§6.2 과대 주장 금지). quarantine 을 유발하도록 추출/게이트 임계를 낮추는 것은 **precision-first 불변식 §3-4·05 §3와 충돌하므로 채택 안 함**. **not-measured(모집단 0 — 데이터 셰이프 특성) 유지** · 대안: quarantine 을 실제로 발생시키는 재평가/저신뢰 경로(LLM judge 주입, 주변 exploration) 갖춰질 때 재실측 |
| SLO-08 실측 — 100만 문서 전체 재처리 시간 벤치마크 | [11](./design/11-observability-and-governance.md) §2.3 | 실측 후 확정 | 🟢 **measured=True (2026-08-18)** — **104,677건 전체 재처리 실제 완주 벽시계** — 로컬 multiprocessing k=10 (12코어, `distributed_batch.shard` 계약) **병렬 334,033ms (~5.6min) measured**, 순차(깨끗한 단일 프로세스 캘리브레이션 16.28ms/doc) ≈28.4min 외삽 · **speedup 5.10×** · 산출 nodes 14,957·promoted 495. 이전 외삽(≈1.76h projected, measured=False) → **실측 measured=True 전환** — 완주 벽시계가 실제 실행으로 확보됨. 게이트 없음(unclassified, 벤치 공개 계약) |
| tool JSON schema 확정 — 실제 추출·판정 골든으로 | [07](./design/07-llm-and-agents.md) §7 | 실측(골든) 후 확정 | ✅ 4 tool 스키마 확정 (스위트 967) — extractor/canonical/contradiction/report |
| #4·#9 measured 전환 — contradiction·lineage 골든 실데이터 재수집 / 1M 실측 러닝 | [10](./design/10-evaluation-and-testing.md), [11](./design/11-observability-and-governance.md) | 실측 전환 | 🟡 부분 #9 분모 실측 (2026-08-18) — **1M 투영 분모를 실측으로 고정**: SLO-08 전체 104,677건 병렬 실측(3.191ms/doc 병렬, k=10) 기준 **1M 병렬 ≈53.2min·순차 ≈271min (4.5h) 투영** (물리 1M 미실행 → **measured=False** 투영 유지, §6.2). **2026-09-02 #4 실측 런**: Tom's Hardware(194) ↔ NVIDIA(114) 총 308건 파이프라인 투입, 332 claims·983 conflict candidates 실측 완료, **교차 소스 모순 0건(자연발생 모순 미검출 정직 기록, 가짜 골든 제작 금지)**. #4 honest-gap 유지 |
| Watchtower 운영 경보 — SLO 위반 라우팅 | [11](./design/11-observability-and-governance.md) §5.3 | 운용 | ✅ 경보 라우팅 봉인 (스위트 984) — `watchtower_slo_alert` · Signal Spire 와 채널 분리 (ADR-1104) |
| SLO nightly 게이트 — 운용 루프 통합 (DoD ②) | [11](./design/11-observability-and-governance.md) §2.3, [10](./design/10-evaluation-and-testing.md) §1.4 | 운용 | ✅ 봉인(998) + **실측 주입 가동(2026-08-12)** — SLO-01/05/06 실측값 주입 `ok=3`·SLO-07 `not-measured`(구조적)·`error_budget{0,3,0.0}` — DoD ② 루프(측정→판정→경보→budget) 실증 |

**DoD:** ① (이전 Phase 가 남긴) 모든 `measured=False`·`(deferred)` 항목 중 실측 가능한 것의 **measured 전환 및 SLO 확정** ② 지속 수집이 자동 신호·SLO 에 반영되는 **운용 루프** 가동. **블록업 재확정 (2026-08-12):** ② 운용 루프 체계는 봉인 완결(11 §2.3 `slo_nightly_gate`) · ① 의 코드 봉인 축(하니스·판정·경보·게이트) 도 완결. **남은 DoD ① measured 전환(contradiction·lineage)·#9·SLO 목표치 확정·수집 확대 는 실데이터(contradiction/lineage 골든 0건)·1M 부하·실측 인프라 종속** — 실측 여건 갖춰질 때 재개 (당장 실측 불가를 OK 로 착각 금지, §6.2). *작업 우선순위는 실측 여건 따라 조정.*

## 4. MVP 최종 성공 기준 (Blueprint §21 추적)

| # | 기준 | 검증 스펙 | 상태 |
| --- | --- | --- | :---: |
| 1 | 10만+ 실제 공개 문서 처리 | [04](./design/04-ingestion-and-parsing.md) | ✅ 실측 — raw **104,544건** |
| 2 | 전체 데이터셋 처음부터 재처리 | [01](./design/01-architecture.md), [06](./design/06-graph-service.md) | ✅ 실측 — 104,544건 재처리 완주 (S50·선형화) |
| 3 | 모든 authoritative claim에 source span·버전 | [02](./design/02-ontology.md), [03](./design/03-storage-and-data-model.md) | ✅ 실측 — source-span E2E + version 봉인(MVP #3/#10) |
| 4 | ER·Claim Extraction 평가 수치 공개 | [10](./design/10-evaluation-and-testing.md) | ✅ 부분실측 — claim F1=1.00·ER P=1.00 오병합률 0.0000 measured · **contradiction·lineage 실데이터 골든 미자연발생 → honest-gap 미측정(§6.2)** |
| 5 | 동일 근원 파생 출처 독립 중복 계산 방지 | [04](./design/04-ingestion-and-parsing.md), [11](./design/11-observability-and-governance.md) | ✅ 계약 봉인 — dedup+lineage 골든 P/R · **실데이터 dup 0건 → lineage honest-gap** |
| 6 | valid/transaction time으로 변화 이력 재현 | [03](./design/03-storage-and-data-model.md) | ✅ 실측 — `assertions_as_of`·`replay_graph_at_tx` (ADR-604) |
| 7 | Research Agent 공백·반대 증거 탐색 | [07](./design/07-llm-and-agents.md) | ✅ 실측 — AB 검증 반증 발견률 1.0(≥0.70 PASS)·연결률 1.0 |
| 8 | 보고서 검증 가능 문장 그래프·원문 감사 | [07](./design/07-llm-and-agents.md), [03](./design/03-storage-and-data-model.md) | ✅ 실측 — `Audit.trace` 연결률 1.0, source span 역추적 |
| 9 | 문서당 비용·전체 처리 시간 측정 | [10](./design/10-evaluation-and-testing.md), [11](./design/11-observability-and-governance.md) | ✅ 부분 — 단일 프로세스 실측(13.9 docs/s·p50 22.6ms·LLM $0/문서) · **1M 스케일은 투영 measured=False(§6.2)** — **투영 분모 실측 고정(2026-08-18)**: 104,677건 전체 병렬 실측(3.191ms/doc, k=10) 기준 1M 병렬 ≈53.2min·순차 ≈271min (물리 1M 미실행) |
| 10 | 모델·프롬프트·ontology 버전 회귀 테스트 | [10](./design/10-evaluation-and-testing.md) | ✅ 실측 — `test_versioning` 5축 봉인·가드·회귀 |

> **재검증 (2026-08-12, Stable Spec 1.0.0 기준):** 10개 기준 전항이 Stable 스펙(설계 절) ↔ 구현 모듈(`collect_large`/`graph_replay`/`assertion_evidence`/`metrics_report`/`dedup`/`curated_zone·catalog`/`investigation_*`/`synthesis`/`pipeline_bench`/`versioning`) ↔ suite(902 Green, 회귀 0) 3중으로 근거 매핑 확인. **측정 품질을 정직히 구분해 재확정:** 실측 7항(#1·2·3·6·7·8·10) + 부분실측 2항(#4·9 — 실측 축 measured, 미자연발생/스케일 투영 축은 honest-gap measured=False) + 계약 봉인 1항(#5 — 경로 검증, 실데이터 축 미측정). **전항 충족이지만 "전부 실측"은 아님** — #4·5·9는 일부 축이 확정 스펙 대비 계약/투영 경로로 정직히 표기됨 (과대 주장 금지 §6.2). 실데이터 재수집·1M 실측 시 measured 전환 가능(경로 단위 테스트 봉인).

## 5. Changelog

가장 최신이 위로. 스펙·설계 변경을 기록한다 (구현 세부 커밋은 git 이력).

### 2026-09-10
- **War Table · Temporal Evidence Graph 확대 장치.** `warGraph`에 결정적 `viewBox` 산술(`clampView`·`zoomAt`·`fitView`)과 선택 `view`를 추가하고, 화면 전폭·전고 Focus Mode 오버레이를 배선했다. 인라인·오버레이 모두 휠 확대, 포인터 팬, 더블클릭/`⟲` fit, `+`/`−` 접근성 폴백을 제공하며, 그룹 펼침·claim 선택·페이지 상태는 `App`에서 단일 재사용한다. `Esc`는 capture 단계에서 오버레이만 닫고 ⌘K Palette와 공존하며, 닫은 뒤 확대 버튼으로 포커스를 복귀한다. 인라인 캔버스도 가용 높이를 사용하도록 확장했고 `frontend/dist`를 재빌드했다. 표시 계층만 변경 — read-only·API/데이터 계약 및 **Spec 1.2.0 유지**.

- **W3 — 영속 조사 Job 수직 증분.** 기존 `/api/investigate` 동기·비영속 trace를 제거하고, PostgreSQL `investigations`·`jobs`·append-only `steps`·JSONB report/audit_trace와 단일-host `investigation-worker`로 clean cutover. `POST /api/investigations`는 `Idempotency-Key`로 `202 + Location + Retry-After`를 반환하며 job/status/report/cancel polling을 제공한다. `FOR UPDATE SKIP LOCKED` claim·lease token·stage-boundary cancel로 경쟁 worker·고아 재claim·late completion을 차단한다. worker는 독립 read-only DuckDB path에서 기존 Planner → Runner → Synthesizer/Audit만 실행하고 graph/curated는 불변. Council은 POST→polling→완료 report로 전환했고 `frontend/dist`를 재빌드했다. compose base/prod에 worker를 추가. Spec **1.2.0**. **prod 배포 실측 (같은 날):** `deploy.sh` 재배포로 `investigation-worker` 포함 9컨테이너 기동, 8공간 전 라우트 200, 스모크 `POST /api/investigations`("NVIDIA 동향 조사", deterministic) → 202 + `Location`/`Retry-After` → worker claim ~0.2s 후 `succeeded`, report 200 — resolved org known·audit passed·coverage 1.0·linkage 1.0(verified 3/3). 스위트 1367.
- **prod 배포 재개 완료 (orchwang-macbookpro) + 배포에서만 드러난 결함 3건 해소.** 원격 복구 확인 후 보류분(main 7커밋: Watchtower Components·W1·W2) push → `deploy.sh` 재배포 → 후속 배선(ensure_tables → `init-readonly.sql` → parquet 스냅샷 → duckdb-ui) 완료. 배포 과정에서 실측된 결함: **(1) prod 앱 컨테이너 자격증명 미주입** — compose 오버레이가 viewer·scheduler 에 `POSTGRES_HOST` 등 DNS 만 주고 자격증명을 안 줘 클라이언트가 로컬 기본값으로 폴백, `run_metrics` flush 가 조용히 상시 실패(prod postgres relation 0건 실측) → 자격증명 6종 `${VAR:?}` 주입 + viewer 에 LLM env 주입, 계약 테스트 신설. **(2) viewer 컨테이너 frontend/dist 미마운트** — 이미지에는 prototype/ 만 들어가 8공간 전 라우트 정직 503 → base·prod 양쪽 ro 마운트 + 계약 테스트. **(3) `rebuild_zones._swap` 이 DuckDB WAL 사이드카 방치** — 구 live 의 `.wal` 이 새 DB 옆에 남아 남의 WAL 재생 시도(CatalogException) → viewer 크래시 루프 실측 → WAL 짝 이동으로 수정 + 회귀 테스트 3건. prod 첫 L3 재적재 실행(raw 220 docs → assertions 131·entities 21) 후 E2E 실측: 8공간 전부 200, 질문 해소(W1) `NVIDIA 동향 조사` → org 해소·audit pass, LLM 종합(W2) `mode=llm` → bunker-flash 문장 6·폐기 0·linkage 1.0·1,215/2,362 tokens. 미해결 정직 표기: grafana 대시보드는 첫 nightly flush(내일 07:07) 전까지 No data — 메트릭 테이블은 생성됨. 스위트 1354.

### 2026-09-09
- **LLM 조사 종합 (W2 슬라이스) — `mode=llm` 옵트인, evidence-first 유지.** LLM env 복구(litellm proxy `bunker-flash`)를 계기로 조사 보고서 **문장 표현만** LLM 생성으로 대체하는 `llm_investigation.py` 신설 — 결론 봉투·근거·랭킹은 결정적 산출 그대로. 안전 계약: LLM 에는 subject 의 assertions 승격 claim rows(≤20, 결정적 순서)만 제공, asserted 문장의 claim_ref 는 그 화이트리스트 강제(목록 밖 ref·무출처 asserted 폐기 카운트), 최종 문장에 Audit §3.9 역추적 재적용 — 미통과 차단(ADR-703). 실패·미설정은 결정적 문장 유지 + `llm.error` 정직 표기. Council: 'LLM 종합' 체크박스 + Cost 패널 토큰 **실측** 표기(usd 는 단가 미확정 '—', §6.2). 실측 E2E: TSMC 질문 → 문장 4·linkage 1.0·1,072/2,316 tokens. 함정 재확인: reasoning 모델 completion 예산 — 기본 8192 (A30 승계, 2048 은 20건 근거에서 잘림 경계 실측). 스위트 1344.
- **조사 지시 입구 개통 (W1) — 자유 질문 → entity name 해소 → 조사 trace.** 사용자가 직접 조사를 지시할 수 없던 원인 해소: 실데이터 subject 는 `org-<해시>` 라 Planner 의 대문자 약어 추출이 지식에 닿을 수 없었다(질문 해소 결함). `planner._decompose` 에 zone entities(canonical_name·surface_forms — 한글 표면형 포함) 결정적 매칭을 전단 추가(경계 규칙으로 'BN'∈'RBNZ' 오탐 차단, 해소된 표면형은 약어 추출과 중복 배제), `/api/investigate?question=` 신설(기존 `subject=` 불변) + 응답에 `question`·`resolved`(known/gap) 노출. Council Chamber 에 조사 지시 패널(질문 입력 → 해소 배지 → trace, known subject 자동 전환) — 미해소 질문은 gap 정직 표기·산출 없음(§6.2). read-only·비영속·결정적 불변(§3-3) — LLM 조사(W2)·지시 영속화(W3)는 별도 결정. 스위트 1334.
- **Watchtower Components 패널 — 동반 구성요소 도달성 실측·접속 일원화.** `/api/watchtower` 에 `components` 신설(`component_status.py`): compose 동반 6종(postgres·minio·neo4j·opensearch·grafana·duckdb-ui)의 도달성을 뷰어 프로세스 **TCP 연결 수락 실측**으로 판정(가짜 up 없음 — 미가동은 unreachable 그대로, 앱 헬스 아님 명시). 호스트 해석은 컨테이너(compose 서비스 DNS) ↔ 로컬(127.0.0.1) 자동 폴백 — prod/로컬 동일 카탈로그. Watchtower 에 상태 Badge + 접속 링크 패널(런타임 host 조립 — 오리진 리터럴 금지 가드 유지, DuckDB UI 는 `localhost` 강제 함정 표기). 스위트 1318.
- **UI 전면 개편 완결 (specs/ui-overhaul-astryx — 16 steps 전체 Done).** 8공간 전부 `frontend/` React 산출물이 canonical 표시 계층으로 전환 완료(브리핑 가치 순 Gate → Witnesses → War Table → Archive → Spire → Council → Watchtower → Chronicle, 공간당 TDD 선작성·browser E2E 실측). **인라인 표시 계층 제거:** `viewer_pages.py`·`*_ext.py`(4,652줄)와 `/legacy/*` 라우트 일괄 삭제 — 뷰어는 API 서버 + 정적 서빙으로 축소, dist 부재는 폴백 없이 정직 503. **FR-6 Grafana:** run 메트릭 postgres flush(`run_metrics.py`, append-only·비차단) + compose 사이드카(oss 11.5.1 핀·loopback·익명 Viewer·read-only DB 계정) + pipeline 대시보드 7패널(미계측 stage·quarantine 은 No data 정직 표기) + Watchtower 딥링크. **FR-7 데이터 브라우징:** parquet 스냅샷 export(원자 교체·잠금 폴백·105k 문서 0.6s 실측) + DuckDB UI 사이드카(parquet 만 ro 마운트 — `.duckdb` attach 금지 명문화) + `docs/operating/data-browsing.md` 가이드. **운영 함정 실측 봉인:** `/app/*` no-cache(안정 청크명 + 캐시가 재배포마다 구/신 청크 혼합 빈 화면 유발), DuckDB UI 는 `localhost` Origin 강제(127.0.0.1 은 401). wire 계약 무변경 → **Spec 1.1.0 유지**.

### 2026-09-08
- **UI 전면 개편 착수 (specs/ui-overhaul-astryx) — SDD 확정 + 목업 트랙 완료 + 불변식 개정.** 목업↔실UI 갭의 근본 원인(별개 코드베이스)을 없애기 위해 표시 계층을 Astryx React 프런트로 단일화하는 SDD(requirements·specs·plans, FR-1~7/TS-1~7/16 steps) 확정. 주요 결정: `frontend/` Vite MPA + `design-system/ui/` 컴포넌트 1벌(목업=앱) · dist 커밋(런타임 node 배제) · 브리핑-우선 IA(1차 4공간 + 운영·감사 2차, Gate=브리핑 대시보드, ⌘K 통합 검색) · Grafana 사이드카(11 §2.2 정합, run 메트릭 postgres flush 선행) · 데이터 존 브라우징(parquet 스냅샷 + DuckDB UI — 뷰어 `.duckdb` 쓰기 잠금 회피). **목업 선행 완료:** 셸 2계층 내비·8공간 셸 통일(문서형 특례 폐지)·Gate 브리핑 대시보드·신규 로고 반입(lockup 투명화·mark/title 분리·favicon)·Watchtower Grafana 딥링크 카드. **불변식 개정:** 뷰어 "stdlib only" → "런타임 stdlib · 표시 계층 저작 도구 node 허용"(09 §1.3 정본, design README §3 노트). wire 계약 무변경 → **Spec 1.1.0 유지** (`/api/search` scope 확장 시 1.2.0 예정).

### 2026-09-07
- **A30 원격 배포·실전 검증 + 후속 (a) — 재시작 보충 실증, max_tokens 8192 상향 배선.** `deploy.sh` 재배포로 A30 코드 원격 반영. **재시작 보충 실전 실증:** 재기동 배너 `next_run_at=09-07 07:07`(과거 시각 보존 — 구코드면 익일 리셋) → 기동 직후 당일 collect·SLO-06 **즉시 보충 발화**. 직전(9/6) 구코드 런은 grace 보충(호스트 기상 20:52 발화)은 작동했으나 SLO-06 29건 전부 fail(output_tokens 평균 512 — 잘림 실측 재현). **A30 배포 후 런: 30건 중 pass 2** — usage 평균 2,031/2,048 로 **새 예산도 소진**(glm-4.7-flash reasoning 이 예상 초과). 로컬 동일 엔드포인트 실측: 예산 8192 에서 contradiction 판정 1,457 토큰으로 **유효 JSON 생성 확인** → **`LLM_MAX_TOKENS=8192` 상향** (예산은 상한일 뿐 과금·비용은 실사용 토큰 기준). 결함 추가 발견: prod 오버레이가 `LLM_MAX_TOKENS` 를 컨테이너에 미전달 → `docker-compose.prod.yml` scheduler 환경에 배선 + `.env.production.example` 갱신 (원격 .env 에 8192 주입 후 재배포). **Spec 1.1.0 유지**. 다음: 익일 07:37 런에서 pass_rate 회복 확인 → 7d 유효 표본 축적.

### 2026-09-05
- **원격 운영 배포 전환 (2026-09-03~05, 소급 기록) — prod = 원격 단일 호스트 Docker Compose (`orchwang-macbookpro`).** 타 세션이 SSH 원격 배포 체계 구축(`a1a93c1`): `scripts/deploy.sh`(rsync → 원격 compose build/up, dry-run·status·logs), `docker-compose.prod.yml` 오버레이(인프라 포트 비공개·viewer loopback), `.env.production.example`, 운영 runbook `docs/operating/deployment.md` 정본화. viewer 8791 이중 바인딩(OrbStack 포트 포워더 교착) 결함 수정·httpx stub 폴백 가드 동반. **운영 실측·정비 (09-05):** 배포 다음날 OrbStack(GUI 앱) 종료로 데몬·스택 동반 다운 발견 → 재기동(컨테이너 restart 정책 자동 복귀, viewer 200·스케줄러 상주 재확인) + **LaunchAgent `com.orbstack.autostart`**(RunAtLoad) 등록으로 재발 방지. **데이터 정책 확정: 원격 prod 는 신규 수집분만 누적**(로컬 corpus 이관 없음 — 원격 SLO-06 7d·curated 는 0에서 시작, 로컬 실측 이력과 별도 모집단). nightly 발화 주체는 원격 scheduler 컨테이너로 이전(로컬 launchd 정지 — 이중 발화 금지). 문서: design 01 §6.2 를 "원격 단일 호스트 compose 상시 운용(현행)·로컬 네이티브는 개발/폴백" 으로 교정(§7 prod 행 포함) + runbook. **Spec 1.1.0 유지** (계약·스키마 변경 아님 — 배포 토폴로지·운영 절차).
- **A30 — SLO-06 판정 경로 결함 2건 수정 + 스케줄러 재시작 보충 (TDD, 스위트 1187).** A29 가 규명한 SLO-06 validation fail 근본 원인 해소: **(a) 판정 max_tokens 512→기본 2048** (`claude_judge._judge_max_tokens`, `LLM_MAX_TOKENS` 환경 조정 가능 — glm-4.7-flash 등 reasoning 모델의 reasoning 토큰 1200+ 소비로 512 는 본문 잘림). **(b) 응답 관용 파싱 `llm_providers.parse_json_content`** — markdown fence(```json)·전후 설명 문장 제거 후 파싱, 실패 시 첫 `{`~마지막 `}` 재시도. **내용 변형·지어내기 없음**(잘린/비-dict JSON 은 그대로 `{}` → 판정 None, ADR-704·honest-gap §6.2) — 스키마 검증은 무변경(측정 대상 보존), Anthropic·OpenAI-compatible 양 어댑터 적용. **(c) 스케줄러 재시작 보충:** `scheduler_runner` 의 `add_job(replace_existing=True)` 가 재시작마다 영속 store 의 next_run(놓친 발화 시각)을 리셋해 **재시작을 가로지르는 grace 보충이 불가**했던 결함(2026-09-05 원격 실측 — 데몬 다운 중 놓친 당일 발화가 재기동 후 next_run=익일로 스킵) 수정 — `build_scheduler`(store 주입 가능)/`register_jobs`(store 대사: 기존 job 보존, 정의 변경만 반영) 분리, `main()` 은 paused 기동→대사→resume (resume 시 grace 내부 misfire 당일 보충). 부수 발견: APScheduler 자체의 shutdown 레이스(STOPPED 상태에서 마지막 `_process_jobs` 가 due job 을 처리해 next_run 전진 — PAUSED 체크만 존재)를 테스트에서 실측, 테스트는 오프라인 store 조작으로 결정화. **테스트 11개 신규**(관용 파싱 6·max_tokens 3·재시작 보존/멱등 2) — **전체 스위트 1187 passed, 22 skipped, 회귀 0**. **Spec 1.1.0 유지** (wire 계약·스키마 무변경 — 전달 계층·dispatch 정비). 문서: design 11 §2.3 결함 수정 메모 + design 01 §6.2 재시작 보충 + ROADMAP §5(본 항목). 다음: 원격 재배포 → 원격 nightly 7d 누적 → SLO-06 확정(잠정)→확정 재확정.

### 2026-09-02
- **Viewer remaining-gaps 전수 소진 — Wave 1 백엔드 + Wave 2 프런트 (workflowz 오케스트레이션).** `docs/handoff-viewer-fidelity.md` 잔여 갭을 scout 4종 병렬 갭분석(58 feasible/39 blocked 분류)으로 리스트업 후 수행: **Wave 1** `/api/search`(text contains — BM25 아님 명시)·`/api/claim`(per-claim confidence+surface 병합) 신설, `/api/table`(entities·quarantined)·`/api/graph`(nodes[].label)·`/api/archive`(cluster_role·segment_kinds·url_groups)·`/api/chronicle`(bounds·events)·`/api/watchtower`(intake 시계열 — fetch.json 클래스 캐시 1회·governance)·`/api/investigate`(counter_evidence 배열·audit_trace·iterations·terminated_by·computed 문구) 확장. **Wave 2** 페이지 프런트 4 워커 병렬(서로 파일 배타): War Table(subclaim known/gap 카드·entity chip 토글·SVG zoom/pan/fit·edge 타입 색·incident 강조·expand more 커서·claim 상세+excerpt·bitemporal dual-slider 레일+이벤트→loadClaim)·Witnesses(claim 카드 실문장+per-claim conf·bitemporal join·독립성/clusters 정직·인용 char-span 발췌·trail 전개·toolbar 딥링크·legend·`?claim`/`?doc` 부트스트랩)·Council(8역할 executed/not-run·loop 결과 trace·subclaims 트리·retrieved≤10·반증 카드(결정적 후보 라벨)·Stopping 실제 입력·audit 분모0 과장금지·딥링크 — 버튼 클릭 시에만 on-request fetch)·Aux(Gate mini-watchtower+전역 text-contains 검색 드롭다운·Watchtower 24h intake SVG+governance 표·Archive facet/stacks/contains+`?doc` 부트스트랩·Chronicle bitemporal plane/preset/events rail). 오케스트레이터 병합: viewer_pages.py 하단 `_inject`(앵커 1회 봉인) + base edge-label `NaN` 우선순위 버그 수정 + `test_viewer_ext_merge.py` 13(병합 앵커·헬퍼 재정의 금지·node --check 8페이지). live 8791 실데이터 스모크 전 항목 확인(`?doc` 부트스트랩 hl 1건·preset as-of 29건·intake 24버킷·trace executed/not-run). **표시 계층만 → Spec 1.1.0 유지.** 전체 스위트 **1174 passed**, 22 skipped (pre-existing `test_claude_*` 2 실패 무관).
- **Viewer 목업 충실 후속 — Hall of Witnesses·Council Chamber 신설 + page-4 보조 블록.** `docs/handoff-viewer-fidelity.md` 우선 2~4. `/witnesses` 신설: 목업 3-column(claim 목록·Evidence·Provenance Trail·원문 하이라이트)으로 **결과→원문 3-hop 왕복**(§2.3 계약 대응) — `get_claim_evidence`→`get_evidence_provenance` trail→`/api/document`(normalized 존 `documents`/`segments` read-only 조회)로 원문을 `ord` 대응(char 오프셋은 세그먼트 clean-text 축 기준)해 supports 하이라이트로 표시한다. `/council` 신설: `get_investigation_report` 의 결론·by_predicate·open_questions·signal + Stopping(A/B/C bars)·Cost·Audit. **조사 '실행' 은 쓰기라 범위 밖** — `execution.available=False` 정직. page-4 보조: `/`(Campaign 카드 = `/api/table`·공관 quick-enter)·`/watchtower`(normalized `publication_time` **실측** freshness 타일)·`/archive`(source_type facet·Codex 상세)·`/chronicle`(as-of 상태 분해·War Table bitemporal 딥링크 → `/table` URL `subject`·`as_of_*` 부트스트랩)·`/spire`(5 트리거 필터행). contradicts/Seer/alert 피드는 계속 정직 빈. 라이브 스모크에서 발견한 두 표시 버그(witnesses `||` 우선순위로 trail 컨테이너 소실·chronicle `qs` 재선언 SyntaxError) 수정 후 봉인: **인라인 JS 문법 회귀 테스트**(node --check, 8페이지) 신설. **표시 계층만 → Spec 1.1.0 유지.** TDD `test_viewer_witnesses.py` 4 + `test_viewer_aux.py` 8(+문법가드 8 parametrized) — 전체 스위트 **1086 passed**, 22 skipped (pre-existing `test_claude_*` 2 실패 무관).
- **Viewer 목업 충실 — War Table (`/table`) 3+1 패널.** `docs/handoff-viewer-fidelity.md` 우선 1. 개발 화면(랭킹·조사 실행)을 목업 정보 구조로 교체: Campaign Map(subject 랭킹·predicate/open_questions)·SVG 그래프 캔버스(`get_investigation_graph` + `get_graph_node`/`get_graph_expand` cursor 클릭 확장)·Evidence Inspector(`get_claim_evidence` + `get_evidence_provenance` trail)·하단 Chronicle 레일(`/api/chronicle` valid-time). contradicts·Seer 는 파사드/영속 부재로 정직 빈. 조사 실행 UI 는 범위 밖(쓰기) — `_api_investigate` API 는 회귀용 존치. **표시 계층만 → Spec 1.1.0 유지.** TDD `test_viewer_table.py` 6개 + 전체 스위트 회귀 0 (pre-existing `test_claude_*` 2 실패 무관).
- **A29 — Phase 6 deferred 실측 항목 점검 및 실측 런 (honest-gap §6.2·precision-first §3-4 준수).** `docs/handoff-phase6-deferred.md` 지침에 따라 외부 조건 종속으로 deferred 된 항목의 착수 조건을 실측 점검하고 파이프라인 런 실행:
  - **(1) SLO-06 7d rolling 재확정:** 착수 조건(7일치 누적) 미충족 확인. `data/slo06_accum.json` 에 5일치(118건: 8/22 10건, 8/26 30건, 8/27 30건, 8/28 30건, 8/31 18건) 누적 확인. 8/27 이후 환경 LLM(`glm-4.7-flash`, reasoning)의 reasoning_tokens(1200+) 소비로 인한 max_tokens=512 초과 및 markdown fence 출력으로 validation fail 확인. 결측일 발생 및 착수 조건 미달로 정직하게 **확정(잠정) 유지** (honest-gap §6.2).
  - **(2) #4 contradiction·lineage 실데이터 골든:** A25 독립 언론 `press-tomshardware`(194건) ↔ `official-nvidia-news`(114건) 총 308건 문서 대상 결정적 파이프라인 전수 실행. 동일 사건 기사쌍(SpaceXAI Vera CPU `doc-9a2a8e33...` ↔ `doc-7a5d5472...`, Nvidia Financial Q2 `doc-5d5c99fc...` ↔ `doc-314c66f3...`, Groq 3 LPX `doc-df1dcfce...` ↔ `doc-eb6c94f8...`) 및 전체 308건 교차 실측. 산출: claims 332건(NV 329, TH 3), conflict candidates 983건. **교차 소스(TH ↔ NV) 모순 0건(자연발생 모순 미검출)** — 983건 충돌 후보는 100% NVIDIA 공식 문서 내부의 동일 subject/predicate 간 서로 다른 object 후보임. 원인: L1 gazetteer(`extract.py`) 대문자 `NVIDIA` 전용으로 TH 본문 `Nvidia` 추출 제한, 동일 사건 보도 팩트 일치, 추출 규칙 긍정문 편향. **미검출을 있는 그대로 기록** (가짜 골든 제작 금지, honest-gap §6.2).
  - **(3) SLO-07 (quarantine dwell-time):** 착수 조건(real quarantine 모집단 > 0) 미충족 확인. DuckDB 전 존 및 파이프라인 실행에서 `quarantine=0` 유지 (비해소 subject 는 추출 단계 폐기). 임계 하향을 통한 인위 유발 금지(precision-first §3-4), **`not-measured(모집단 0)` 유지**.
  - **(4) 1M 물리 부하 실측 (#9):** 착수 조건(1M corpus 또는 부하 인프라) 미충족 확인. 총 raw 105,150건(포화). 기존 104,677건 병렬 실측 분모(3.191ms/doc, k=10) 기반 1M 투영(병렬 ≈53.2min·순차 ≈4.5h) **`measured=False` 투영 유지**.
  - **검증:** 전체 테스트 스위트 1064 passed, 22 skipped 무회귀 확인 (`LLM_PROVIDER=offline`). 코드 변경 없음 → **Spec 1.1.0 유지**.
- **A28 (A27 후속) — nightly 스케줄러 슬립 보충(misfire grace 하루) + 상시 로컬 네이티브 운용 배포 절 신설 (design 01 §6.2).** 상시 로컬(맥 단일 호스트)에서 슬립으로 아침 07:07/07:37 발화를 놓치면 APScheduler 기본 `misfire_grace_time=1초`가 놓친 job 을 **다음날로 미루는 문제**를 해소. **(a) 정책을 job 정의에 봉인 (TDD):** `orc_citadel/scheduler.py` — `MISFIRE_GRACE_SECONDS=86400`(하루)·`GRACE_KEY` 상수, `build_scheduler_jobs()` 가 job dict 에 `misfire_grace_seconds` 포함 (정책·정의 분리 유지). **(b) 드라이버 배선:** `scripts/scheduler_runner.py` — `_misfire_grace(job)`(명시값 우선·기본 하루) 를 `add_job(misfire_grace_time=...)` 으로 전달 — 스케줄러가 살아있는 동안 grace 내부면 **당일 job 을 보충 실행**. **정직 한계 (§6.2):** 슬립 중엔 어떤 스케줄러도 실행 불가 — 보충은 '깨어난 뒤 당일치'까지만 보장, 물리 Mac 켜짐이 전제. **테스트 2개 신규**(각 job 의 grace 정의 ≥하루·드라이버 전달 배선) — `test_scheduler.py` 8 passed, 전체 스위트 회귀 0. **(c) 배포 문서 정본화:** design 01 **§6.2 "상시 로컬 네이티브 운용 (Phase 6·현행)" 신설** — venv+launchd+파일/DuckDB 토폴로지·단일 발화 주체·슬립 보충 정책·환경/시크릿(.env gitignore) 명문화, 기존 §6.2 확장(Kubernetes)→§6.3·분산 batch 메모→§6.4 로 재번호(외부 상호참조 없음 확인), §7 환경 표 `prod` 행을 "상시 로컬 네이티브 운용(§6.2)" 으로 갱신. **Spec 1.1.0 유지** (계약·스키마 추가 아님 — dispatch 정책·배포 토폴로지 문서화). 문서: design 01 §6.2·§7 + ROADMAP §5 changelog(본 항목). 다음(운영): launchd 상주 지속 → 7d SLO-06 누적 재확정, 모순 재료(Tom's Hardware ↔ 벤더 공식)로 #4 contradiction 실측 전환.

### 2026-08-24
- **A27 (제안 조합 1번 후속) — cron → APScheduler 상주 스케줄러 전환: 셸 cron hang 우회 + 영속·단일 발화 주체 (celery beat 대체, A26 계승).** A26 이 cron 직접 등록을 사용자에게 위임했으나 **crontab 쓰기 명령(`crontab -`, `crontab <file>`)이 이 실행 환경에서 hang(샌드박스/권한)** 하여, **셸 cron 대신 파이썬 상주 스케줄러**로 전환 — 사용자 질문("celery beat 와 유사한 대체제")에 대한 선택지로 **APScheduler 채택**(BackgroundScheduler + CronTrigger + **SQLAlchemyJobStore(sqlite)** — celery beat 과 유사한 파이썬 프로세스 내 스케줄, 외부 broker 불요). 복수 pod/프로세스 중복 dispatch 가능성 질문에 **단일 호스트(현재 저장소 운용 범위) 선택** — 활성 스케줄러는 정확히 1개. **(a) job 정의를 순수 함수로 분리 (TDD):** `orc_citadel/scheduler.py` — `build_scheduler_jobs()` 가 `{"id","func","schedule"}` 순수 dict 반환 (테스트 가능·결정적). **CronTrigger fields 타입 함정 수정:** APScheduler `trigger.fields` 는 dict 가 아닌 Field 리스트 → job 정의에서 **`schedule`(hour/minute) 만 순수 dict 로 분리**해 표시·단일 발화 검증에 사용, `runner` 가 `CronTrigger(**schedule)` 로 변환. **6개 테스트** (두 job·07:07/07:37 슬롯·job-id 정합·단일 발화(슬롯 유일성)·결정적 정렬) — **스위트 1046→1052 passed, 회귀 0**. **(b) 상주 드라이버:** `scripts/scheduler_runner.py` — `build_scheduler()` 가 BackgroundScheduler + SQLAlchemyJobStore(`data/scheduler-jobs.sqlite`)**등록 (dispatch 만, 실제 작업은 `scripts/nightly_*.main()` 이 소유 — 태스크·정의 분리, 불변식 §3-3 read-only 준수)**, `main()` 은 상주 loop(sleep 3600). **(c) 배선 실측 검증:** sqlalchemy 미설치 `ImportError(SQLAlchemyJobStore requires SQLAlchemy)` → **`sqlalchemy>=2.0` 의존 추가** (pyproject·uv lock, sqlalchemy 2.0.52 설치) → 재검증 **성공**: `nightly_collect` next_run 07:07·`nightly_slo06` 07:37, **슬롯 유일성 OK(단일 발화 주체)**, job store sqlite 영속. **(d) launchd 상주:** `scripts/com.orc-citadel.nightly.plist`(KeepAlive+RunAtLoad, 표시 plist) — `plutil -lint OK`, 활성 스케줄러 1개가 재시작·재부팅 후에도 발화 기회 유지. ℹ️ 실제 등록은 사용자 확인('표시 plist 로드') 후 — `launchctl load -w scripts/com.orc-citadel.nightly.plist`. **Spec 1.1.0 유지** (계약·스키마 추가 아님, 스케줄러는 기존 nightly 태스크의 dispatch 계층 — 태스크·job 정의 그대로). 문서: design 04 §1.4 + design 11 §2.3(SLO-06 축적) + ROADMAP §5 changelog(본 항목). 다음(운영): 사용자가 launchd 활성화 → 매일 07:07 수집·07:37 SLO-06 축적 → **7일 운용 후 SLO-06 확정(잠정)→확정 재확정**, 또는 모순 재료(Tom's Hardware)로 **#4 contradiction 실측 전환** 시도.
### 2026-08-23
- **Viewer 미구현 페이지 5종 신설 — `docs/handoff-viewer-pages.md` 위임 실행 (목업 충실도까지는 아니어도 실데이터 화면).** 개발용 read-only 뷰어(`prototype/orc_citadel/viewer.py`, stdlib `http.server`)에 목업 8페이지 중 미구현 5개를 실데이터(또는 정직 빈/`not-measured`)로 구현. **(구조 선행 분리):** 기존 개발 화면 HTML 을 `viewer_pages.py` 로 이동(공유 CSS·경량 내비 포함) — `/` 는 **Citadel Gate** 진입 대시보드, 기존 화면은 **`/table`**(War Table·Council) 로 보존(nav 에서 진입, 기능 무손실). **(신설 5페이지, 전부 TDD 스모크 테스트 선작성):** ①`/watchtower`(수집 관제 — raw source·source_type·doc 수 실측 + nightly SLO 판정표는 in-memory 관측 미누적이라 전항 `not-measured`·error_budget ratio None 정직) ②`/archive`(Grand Archive — normalized documents(+segment 수, `oc.duckdb` read-only)·raw source 목록·dedup cluster 수, raw/normalized 형식 구분 표기 04 §2.2) ③`/chronicle`(Chronicle Vault — curated `assertions_as_of` bitemporal·supersedes 체인, graph_replay 는 postgres SoT 미가동 정직 `available=False`) ④`/spire`(Signal Spire — 5 종 트리거 카탈로그(signal_spire.TRIGGER_TYPES docstring)·fire-once 규칙·영속 저장소 없어 alert feed 정직 빈) ⑤`/`(Citadel Gate — 존 카운트 raw/normalized/curated·랭킹 top5·신호 분포). 전 페이지 `/api/gate|watchtower|archive|chronicle|spire` JSON fetch 클라이언트 렌더, read-only·결정적, DuckDB read_only 연결로 잠금 회피. **테스트:** viewer 스모크 신규 10개(`test_viewer_gate/watchtower/spire/archive/chronicle`) + 기존 `test_viewer_graph` — **전체 스위트 1054 passed, 회귀 0** (기저 baseline 2개 실패 `test_claude_cost::test_usage_zero_when_stub`·`test_claude_judge::test_stub_fallback_not_logged_as_schema` 는 LLM/judge 모듈 pre-existing·환경성 — 본 작업 무관, base 커밋에서도 동일 실패 확인). **라이브 스모크:** 6 라우트(`/`·`/table`·`/watchtower`·`/archive`·`/chronicle`·`/spire`) HTTP 200 실데이터 렌더 실측(raw 8 source/104,970 docs·normalized 6/90·curated 29 어세션). **Spec 1.1.0 유지** — 계약·스키마 추가 없음(뷰어 라우트는 개발 표시 계층, 09 wire 계약과 별개). 문서: handoff 3단계 규칙 — design 09 §1.3 프로토타입 뷰어 주석 추가 + ROADMAP §5 changelog(본 항목) + design README Spec 유지 확인.
- **Viewer 목업 공유 셸 적용 (보충, `03e725e`).** 기능 구현 후 "임시 페이지" 느낌 해소를 위해 목업 공유 시각 셸(`docs/mockups/*.html` 인라인 셸)과 DESIGN.md 토큰을 6 페이지에 일괄 적용: `:root` 토큰을 DESIGN 값(정확 hex)으로, 마스트헤드(워드마크 crest·공간 브레이드크럼·검색·Signal Spire 칩)·masthead 밴드·공간 전환 탭(.spaces)을 `shell()` 로 신설. 외부 폰트(Cinzel/Space Grotesk/Inter/JetBrains)와 히어로 PNG 는 오프라인 stdlib 뷰어라 로컬 fallback 스택/flat night gradient 로 대체(목업 자체의 fallback 조합). presentation-only — 데이터 렌더 로직 무변경, viewer 15 테스트 회귀 0, 6 라우트 스모크 확인. **Spec 1.1.0 유지.**

### 2026-08-22
- **A26 (제안 조합 1번) — 시간 축 전환: nightly 수집·SLO-06 7d 축적 cron 배치.** 세션 빈도 순시 폴링(A24 가 "반복 폴링은 저가치"로 기록)을 **일 단위 스케줄**로 전환해 "피드 갱신 대기(수집)"·"7d rolling(SLO-06 재확정)" 블로커를 동시 해소 (방향성 제안 1번). **(a) 누적 로그 모듈 (TDD):** `slo06_accum.py` — `accum_append`·`accum_recent`·`accum_slo06` 순수 함수. nightly 마다 개별 schema 검증(`record_schema`) 을 `data/slo06_accum.json`(gitignore) 에 append, 최근 7일 누적으로 `schema_pass_rate`→`evaluate_slo06` 재확정 판정. **스위트 1041→1046 passed (신규 5개 테스트)**, 회귀 0. **(b) nightly 스크립트:** `scripts/nightly_collect.py` — RSS/sitemap 신규만(arXiv 10만 bulk 제외 — 과거 연대 소진, 매일 재시도 과부하, §6.2), `collect_rss`/`collect_sitemap`(known_urls=S1) 직접 호출. `scripts/nightly_slo06.py` — `.env`(repo root) 로드 → 무비용 `bunker-flash` LLM 로 canonical/contradiction 판정 → 개별 결과 append → 7d roll 판정. **(c) 실측 검증:** nightly_collect 1회 → **press-tomshardware 신규 +5** (세션 순시 폴링은 +0 이지만 일 단위는 피드 갱신 캡처 — **시간 축 전환 실효 실증**) · nightly_slo06(cap=10) → 10건 append, `pass_rate 1.0 within_slo:True measured:True`. **(d) cron 등록:** crontab 쓰기 명령이 이 실행 환경에서 hang(샌드박스/권한)되어 **사용자 직접 등록 위임** — `/tmp/orc_crontab.txt`(07:07 수집·07:37 SLO-06, `cd <proto> && ...`)를 사용자가 `crontab /tmp/orc_crontab.txt` 로 등재. **데이터 정합 정정:** press-tomshardware 55건 전부 2026-08-22 저장, raw 104,915→**104,970**(+55: A25 50 + nightly_collect 5). **Spec 1.1.0 유지** (신규 계약·스키마 추가 아님, 독립 스크립트·순수 함수). 문서: ROADMAP §1·§3 수집 행·§5 changelog(본 항목) + design 04 §1.4 + design 11 §2.3(SLO-06 축적 방식). 다음: 7일 nightly 축적 후 SLO-06 확정(잠정)→확정 재확정, 또는 확보된 모순 재료로 #4 contradiction 실측 전환 시도(A24 다음 후보).
- **A25 (제안 조합 1+2) — Tom's Hardware 독립 언론 소스 도입: "상호 모순될 수 있는 관점이 소스셋에 없다" 근본 원인 해소 착수 (04 §1.4).** 방향성 제안(세션 검토)에서 1번(시간 축 cron 전환)·2번(복수 관점 소스) 조합 채택, 2번 탐색으로 **The Register 기각**(default-deny·AI scraping BLOCK·licence 요구 — EE Times와 동일 robots-개방 원칙 위반) · **Tom's Hardware 채택**(robots `User-agent:*` 아티클 본문 크롤링 허용, RSS 개방, 반도체 수출통제 커버리지 풍부). **(a) TDD 등록 (Red→Green):** `press-tomshardware`(`rss`, `https://www.tomshardware.com/feeds/all`) 소스 등록 테스트 신규 → Red 확인 → `SOURCES` 추가 → Green — 전체 스위트 **1040→1041 passed, 22 skipped, 회귀 0**. **(b) collect_rss 실측 런:** 첫 실행이 RSS 항목 45건 저장 후 타임아웃, 재실행이 **신규 5건 추가** → **A25 런 총 +50 (press-tomshardware 50건 저장, raw 104,915→104,965)**, errors 0. ⚠️ 최초 기록("104,915→104,920 +5")은 첫 실행 45건을 누락해 부정확했음 — 이 항목으로 정정. **(c) 신호 밀도(A10)·독립성 실측:** press-tomshardware 총 50건 중 ①수출통제/제재 5건(smic 가격·H200 중국수출·Nvidia LPU 부인·Supermicro 밀수·Pixel 제조 이전) + ②반도체 산업 13건 + ③AI인프라/DC 13건 — **절반 이상이 시스템 관심 영역**, `robots_allowed` 전부 True. **공식 nvidia(38건)와 URL 겹침 0 — 완전 독립**: 벤더 자사(자사 PR 프레이밍)와 Tom's Hardware(독립·때로 비판적 관점)가 같은 사건을 달리 보도 → **#4 contradiction·SLO-07 의 "모순 자연발생 부재" 근본 원인(A24 이전 진단)이 실질 해소되기 시작** — 모순 재료 확보. **코드 변경 동반 → Spec 1.1.0 유지** (계약·스키마 추가 아님, 소스 등록은 기존 RSS kind 계약 이행). 문서: ROADMAP §1·§3 수집 행·§5 changelog(본 항목) + design 04 §1.4 소스 등록 반영. 다음: 1번 시간 축 전환(일 단위 수집·SLO-06 축적 cron) 설계·배치, 또는 확보된 모순 재료로 #4 contradiction 실측 전환 시도.
### 2026-08-18
- **B블록 — SLO-08 전체 재처리 병렬 실측, measured=True 전환 (design 11 §2.3, 01 §6).** deferred SLO-08 "100만 문서 전체 재처리 시간 벤치마크 공개" 를 **실측 완료**. 전체 104,677건 raw 를 `distributed_batch.shard`(01 §6 k-파라미터 파티셔닝 계약) 로 k=10 분할 → 로컬 `multiprocessing.Pool` 로 각 파티션 `bulk_pipeline`(결정적 체인, `:memory:` zone) 실제 실행 → **병렬 완주 벽시계 `334,033ms (~5.6min)` `measured=True`**. 정직 교정: speedup 은 깨끗한 단일 프로세스 캘리브레이션(2,000건 32.56s = **16.28ms/doc**) 대비 **5.10×** — 순차 외삽 ≈28.4min (Σ파티션 내부 elapsed 로 구한 9.86× 는 분할 zone 초기화 오버헤드가 순차에 포함돼 과장된 값으로 기각, 정직 표기 §6.2). 산출: nodes 14,957·promoted_claims 495·edges 0. `reprocess_benchmark({full_ms:334,033})` → `{measured:True, source:recompute_bench}` — 이전(2026-08-12) "배치 1,000건 16.83s + 전체 외삽 1.76h projected(measured=False)" 를 **실측 완주로 대체**. nightly 게이트 재주입: SLO-08 `unclassified`(벤치 공개 계약, 게이트 없음)·`measured:True`, ok=[01/05/06], SLO-07 `not-measured`, `error_budget{0,3,0.0}`. read-only·결정적(mock 없음 — 실제 실행). **코드 변경 없음 → Spec 1.0.0 유지** (시스템에 새 인프라 미설치 — stdlib multiprocessing 재사용). §3 Phase 6 SLO-08 행·design 11 §2.3 반영. 다음: SLO-08 목표치 확정·SLO-07(재평가 인프라)·#4/#9(contradiction·lineage 골든)·1M 부하.
- **#9 1M 부하 투영 — 실측 분모로 고정, measured=False 유지 (10 §6.2, 11 §2.3).** MVP #9 "문서당 비용·전체 처리 시간 측정" 의 1M 스케일 투영 분모를 **실측으로 교정**. 이전(2026-08-12)은 mock/배치 16.83ms 외삽이었으나, 이제 SLO-08 전체 104,677건 병렬 실측(334,033ms, k=10)이 있어 **per-doc 병렬 = 3.191ms 실측 분모**로 1M 을 투영: **병렬(k=10) ≈53.2min · 순차 ≈271min (4.5h)**. 단, **물리 1M 재처리를 실행하지 않았으므로 measured=False 투영 유지**(§6.2 — 실측 분모 기반 투영이 1M 완주를 대체하지 않음, 과대 주장 금지). `phase4_report.project_time_to_scale` 은 per_doc 실측 시 measured=True 를 주지만 1M 은 물리 미실행이므로 **문서에서 정직하게 projected 로 구분 표기**. cost 축은 per-doc 비용 실측 부재로 여전히 None. 코드 변경 없음 → **Spec 1.0.0 유지**. §3 Phase 6 #9 행·§4 MVP #9 행 반영. 다음: 1M 물리 실측은 부하 인프라에서 재개·SLO-07(재평가)·#4(contradiction·lineage 골든).
- **SLO-07 근본 원인 조사 완결 — not-measured 를 "모집단 0"으로 정직 재정의 (design 11 §2.3, 05 §3·§6).** 실측 진행 중 "재평가 인프라 구축"이 SLO-07 실측을 가능케 하는지 조사 → **반전 발견**. 현재 실데이터에서 게이트 **quarantine 가 0건** 임을 실측 확정: 5,000·8,000건 슬라이스 모두 `open_quarantine=0·dwell=0` (모든 mention 2,347건 해소·promote, claims 430 전체 promote, edges 0). 근본 원인은 **"재평가 인프라 부족"이 아니라 정밀 우선(§3-4) 파이프라인·현 데이터 셰이프**: ① 비해소 subject claim 은 `extract_claims` 가 추출 단계에서 **폐기**(`if not subject_id: continue`, 02 §4-3) → 게이트 `missing_subject` quarantine 가 도달 불가 ② 이전 유일 quarantine 모집단(71건 `unknown_predicate:partners`) 은 predicate 정합화로 제거됨. → 재평가 인프라를 구축해도 **실데이터에서 earthquake 가 아님** (§6.2). SLO-07 임계(≤3d) 관점 `0건 quarantine = 위반 아님` 로 **확정 유지**되나, dwell-time **분포는 모집단 0 이라 measured 전환 불가** — `not-measured(구조적)` 라벨을 `not-measured(모집단 0 — 데이터 특성)` 으로 교정. quarantine 을 인공 유발하도록 추출/게이트 임계를 낮추는 것은 **precision-first 불변식 §3-4·05 §3 위배**라 채택 안 함. read-only·결정적·mock/실측 격리 — 신규 코드 없음(조사만) → **Spec 1.0.0 유지**. §3 Phase 6 SLO-07 행·design 11 §2.3 반영. 다음: real quarantine 모집단을 만드는 재평가/저신뢰(LLM judge)·주변 exploration 경로 갖춰질 때 재실측, 또는 다른 인프라 방향(수집·관측·분산).
- **A6 수집 지속·확대 배치 — raw 104,713 + SLO-05 표본 n=1,540 (04 §1.4, 11 §2.3).** A5(2026-08-18, `windows=4`) 후속으로 **arXiv 날짜윈도우를 `windows=10`(2025-11 까지)으로 확장**해 더 과거 미수집 연대를 재흝는 실제 수집 런을 폴라이트(1 req/3s)·resumable·`slo_log` 주입으로 재가동 → **raw 104,677 → 104,713 (+36)**. 세부: arXiv windows=10 **+25 신규**(10 페이지 호출 중 skip 1,475/1,500 — 과거 연대 대부분 이미 수집돼 있음, 정직히 skip 다수 기록) · RSS 3 source **+11 신규**(nvidia 미수집 아티클 +20 시도 중 신규 확보; amd·semiengineering content-hash 중복 — `saved` 카운터가 fetch 성공을 세는 것임을 구분, 실제 디스크 신규는 +36). **SLO-05 표본 n=1,540** `{success_rate:1.0, n_success:1,540, n_attempt:1,540, n_unknown:0}` measured=True, `within_slo=True`(≥99%) — ⚠️ **정정 (2026-08-18, A8 관측):** n=1,540 은 **이번 런 자체 시도 수**(arXiv `total=1500`+RSS 40) — `slo_log` in-memory 라 이전 "A5 표본 n=440 → n=1,540 (3.5×)·시도/성공 1,100건 추가 축적" 서술은 부정확(런 간 누적 없음). raw 데이터는 gitignore(수집 원문 커밋 제외) — 커밋은 문서만. 코드 변경 없음 → **Spec 1.0.0 유지**. §3 Phase 6 수집 행·SLO-05 행·design 11 §2.3 반영. 다음: 수집 계속 확대(10만+ → 더)는 사용자 인계로 폴라이트 러너 실행 가능.
- **A7 수집 지속·확대 배치 — raw 104,725 & SLO-05 표본 n=2,040 (04 §1.4, 11 §2.3).** A6(2026-08-18, `windows=10`) 후속으로 **arXiv 날짜윈도우를 `windows=16`(2025-07 까지)으로 추가 확장**해 더 깊은 과거 연대를 재흝는 실제 수집 런을 폴라이트(1 req/3s)·resumable·`slo_log` 주입으로 재가동 → **raw 104,713 → 104,725 (+12)**. 세부: arXiv windows=16 **+2 신규**(skip 1,998/2,000 — **A6에 이어 arXiv 과거 연대가 거의 소진됐음을 정직히 기록**, 더 깊은 연대도 대부분 이미 수집됨) · RSS 3 source **+10 신규**(nvidia 미수집 아티클 계속 확보; amd·semiengineering content-hash 중복 — `saved` 카운터가 fetch 성공을 세는 것임을 구분). **수집 포화 신호:** A5(+35) → A6(+36) → A7(+12) 로 신규 감소, 실질 신규가 RSS(유한)·미수집 연대 여백에 의존 — **raw 가 ~104.7k 자연 포화에 접근**. **SLO-05 표본 n=2,040** `{success_rate:1.0, n_success:2,040, n_attempt:2,040, n_unknown:0}` measured=True, `within_slo=True`(≥99%) — ⚠️ **정정 (2026-08-18, A8 관측):** 표본 n=2,040 은 **이번 런 자체 시도 수**(arXiv `total=2000`+RSS 40) — `slo_log`(`SloObservationLog`)가 **in-memory(파일/DB 영속 없음)라 런 간 누적 없음**. 이전 "n=1,540 → n=2,040 (1.3×)·시도 500건 추가 축적·다음 런 RSS refresh 로 계속 축적" 서술은 부정확 — in-memory 라 RSS refresh 로는 축적 불가, n 은 arXiv `total` 파라미터 연동. raw 데이터는 gitignore(수집 원문 커밋 제외) — 커밋은 문서만. 코드 변경 없음 → **Spec 1.0.0 유지**. §3 Phase 6 수집 행·SLO-05 행·design 11 §2.3 반영. 다음: 수집 확대의 유의미한 성장을 위해서는 새 소스(추가 저널·프레스 릴리스) 또는 10만+ 초과 목표 재결정 필요.
- **A8 수집 지속·확대 배치 — raw 104,735 & SLO-05 표본 n=2,040 + in-memory 정정 (04 §1.4, 11 §2.3).** A7(windows=16) 후속으로 **arXiv `windows=20`(2025-03 까지)** 재가동 → **raw 104,725 → 104,735 (+10)** — arXiv **+0 (skip 2,000)**: 2025-03 까지 **전 연대 소진 재확인**(A7 skip 1,998 에 이어 A8 은 +0), raw 성장은 RSS 신규 3 source **+10**(semiengineering 미수집 아티클 확보)이 유일. **SLO-05 표본 n=2,040**(=arXiv total 2000+RSS 40, `success_rate:1.0`) — A7 과 **동일 n** 임을 관측 → **`slo_log` 가 in-memory(`SloObservationLog`, import `time` 외 영속 없음)라 런 간 누적 배열 불가**를 코드 검증으로 확정, 이전 A5·A6·A7 "표본 누적 확대" 서술 전면 정정(§3 수집 행·SLO-05 행·design 11 §2.3 표·실측 메모). 성공률 1.0·`within_slo=True` 유지. **함의:** "수집 확대 그 자체로 SLO-05 표본이 커진다" 는 아니며, n 을 키우려면 런 별 arXiv `total` 파라미터를 조절(런 간 축적과 무관). raw 데이터는 gitignore — 커밋은 문서만. 코드 변경 없음 → **Spec 1.0.0 유지**. 다음: SLO-05 표본 확대는 `total` 조절, raw 성장은 새 소스 필요(2026-08-18 현재 상호 독립).
- **A9 새 소스 배선 시도 — CHIPS/NIST gov 추가 후 A11 실측으로 보류(제거), dedup 결함 발견 (04 §1.4, 11 §2.3).** raw 성장의 포화(A7/A8 arXiv +0)에 대응해 설계 04 §1.4 초기 #3 **CHIPS/NIST**(`gov`, `gov-public`)를 `collect_large.SOURCES` 에 시도 배선. **TDD** `test_sources_registers_chips_nist_gov` 로 검증 후 **A9 런**에서 gov 40건 수집 (raw 104,735→104,796 관측). ⚠️ **A11 추적 검증으로 치명적 결함 발견 (2026-08-18):** gov zone 이 **80건이지만 고유 URL 40건** — 같은 URL에 doc_id 2개씩. `_save_zone` 의 doc_id 가 **content sha256** 인데, **NIST 동적 페이지 본문이 매 요청 달라져** hash 가 매 런 새로 발급 → **중복 저장**. A9(A11 모두 "gov 신규 +40" 은 **전부 중복 허수** — 실은 40 고유 문서가 2번씩(80건) 저장). **처리:** 40 간 중복 doc dir 정리(80→**40 고유**) · `SOURCES` 에서 gov **제거·미배선 복귀** (`test_sources_excludes_chips_nist_gov` Red→Green, 스위트 1032 Green·회귀 0) · **raw 정확 수치: 104,806**(104,846−중복 40). **함의:** content-hash idempotency(03 §7·불변식 §3-6)가 **동적 페이지에서 실패** — 근본 해결은 **URL 기반 idempotency 키**(04 §2.1 `hash(source_id,url,fetch_window)`) 전환(계약·Spec 번프 수반, 별도 작업). gov 는 그 전까지 **보류**. SLO-05: gov 중복도 fetch-success 로 계수됨(성공률 1.0 유지했으나 real-doc 아님 — honest 반영). raw gitignore·커밋 문서만. design 04 §1.4·ROADMAP §3 수집 행 반영. 다음: URL 기반 idempotency(계약 변경) 별도 판단, 또는 기존 RSS 3 source 수집 지속.
- **A10 신호 수율 실측 — CHIPS/NIST gov 를 signal 스코프에 미포함 결정 (read-only·결정적, 04 §1.4/11 §1.4).** A9 로 배선된 gov 가 실신호 재현(`signal_source_runner.SIGNAL_SOURCE_IDS`) 스코프에 **포함할지 여부를 데이터로** 판정. 결정적 파이프라인으로 소스별 `signal_density(docs, claims, edges)` 실측: **gov-chips-nist 40건 → claims 1·density 0.025** (= 비신호 arXiv 0.000 바로 위) vs **기존 signal 3종: nvidia 2.922(77건·claim 225)·amd 5.900(30·177)·semiengineering 0.630(173·109)**. **정직 신호 (자기검증):** `result.edges` 는 `mutation_log=None` 시 빈 replay(ADR-304)로 **항상 0** — 본 경로에서 엣지는 미계수. 그러나 **claims 수는 정본**(`result.claims`)이고 density 순위는 claims 로 결정 — gov(0.025) 가 signal 소스(0.63~5.9) 와 현격한 격차, **결론은 엣지 계수와 무관하게 견고**. **판정:** gov 는 raw 성장엔 유효(A9 +40)하나 **신호 밀도가 비신호(arXiv) 수준이라 `SIGNAL_SOURCE_IDS` 미포함 유지** — 신호 수율 기반 스케줄(§1.1)의 "저신호/미측정 소스를 신호로 오판 금지"(11 §1.4·§6.2) 원칙과 정합. **두 스코프(raw 수집 vs 실신호 재현)는 별개임을 실측으로 확정.** 코드 변경 없음(`SIGNAL_SOURCE_IDS` 미변경 — 신규 소스, 정책 데이터만) → **Spec 1.0.0 유지**. read-only(`:memory:` zone·결정적 체인·저장소 쓰기 없음). **동반 재검증 — EE Times robots.txt (2026-08-18):** 후보 재평가 시 "EE Times 추가" 의제가 검토됐으나 실측로 **설계 04 §1.4 라인 131 제약 재확인** — `eetimes.com/robots.txt` 가 `anthropic-ai`(및 CCBot·GPTBot·ChatGPT-User·cohere-ai·omgili 등)에 **명시 `Disallow: /`** → 신규 소스로 **부적격 확정**(기존 결론 유지, robots 정책 회피 법적 금지). 설계가 이미 옳았음을 재검증으로 확정, 신규 채택 없음. 다음: (개별 소스 신호 수율은 raw 성장과 무관하게 별도 판정 — 향후 gov 실데이터 축적·재측정은 가능).
- **A13 arXiv 중복 판정 — "신규 중복 방지만" 결정, 코드 변경 없음 (04 §2.1/2.2, 03 §7).** A12 가 발견한 arXiv format 혼재(중복 1,247건 = 같은 논문을 Atom XML 메타 2-3KB·전체 page 40KB 두 형식으로, http 200)에 대해 사용자 결정 "기존 유지·신규 중복 방지만". **정직 재판정 — 이미 현행 계약이 커버:** 설계 04 **§2.1 S2 idempotency(`doc_id`=내용 기반)** 가 "같은 bytes 재저장" 을 방지(동일 content 반복 수집 → 동일 doc_id skip). **§2.2 는 "동일 url 의 다른 버전/형식은 새 doc_id 로 모두 보존" 을 명시 규정**(ADR-301, 불변식 03 §2.2) — arXiv 의 Atom/HTML 두 형식은 **설계 의도의 버전·형식 보존** 이지 결함 아님. **실증:** A12 arXiv windows=1 이 skip 92/100 — 이미 존재하는 동일 content 를 S2 가 정확히 skip(신규 +8 만 저장). **결론: 코드 변경 필요 없음** — 신규 동일-content 중복은 이미 방지, format 혼재는 §2.2 의도적 보존. URL 강제 skip 을 추가하면 **§2.2(변경·형식 보존)와 충돌**해 오히려 계약 위반 → 채택 안 함. **남은 실질 리스크 = 동적 소스(NIST) 재추가 시 §2.2 변경분 새 버전 누적** — gov 는 이미 `SOURCES` 제거(보류)로 현 소스셋엔 없음, 재추가 전 URL 기반 정책 재검증 요구. 기존 1,247 건은 **§2.2 정상 보존으로 유지** (파괴 안 함). docs-only → **Spec 1.0.0 유지**. 다음: RSS 3 source 수집 지속(A14).
- **A14 수집 지속 — RSS 3 source refresh, raw 104,868 (+14) + arXiv 오류 회복 (04 §1.4, 11 §2.3).** gov 보류·A13 중복 판정 후 기존 RSS 3 source 로 수집 지속 → **raw 104,854 → 104,868 (+14)** — nvidia +20·amd +10·semi +10 (content-hash dedup 정상 — 신규만 저장, RSS 가 유일 지속 신규 주축 재확인). **arXiv `errors=1`** — 이번 런 최신 연대 창 쿼리 1회 오류(`_EmptyPage`/HTTPError, S49+ transient 재시도 소진 후 errors 집계·resumable — 전체 실행 중단 없이 다음 배치 계속, 데이터 손실 없음). A12 의 최신 +8 은 유효(별도 창), A14 는 회복 경로로 skip — **정직 기록** (부분 실패를 성공으로 오기록하지 않음). SLO-05 이 런 `n=40`(=RSS 40 시도, arXiv errors 는 성공/시도 아님 — `record_collect` 는 성공만 기록)·성공률 1.0·`within_slo=True`. raw gitignore·커밋 문서만. code 변경 없음 → **Spec 1.0.0 유지**. SLO-05·수집 행·design 11 §2.3 반영. 다음: RSS 3 source 수집 지속(A15) 또는 deferred SLO 재개(실측 여건).
- **A15 URL 기반 idempotency(S1) 전면 적용 — b 작업 완결 (04 §2.1, 03 §7, 불변식 §3-6).** "b(URL 기반 idempotency)" 구현·실측 검증. **변경:** `collect_rss` 에 `known_urls=None`(선택) 추가 — 제공 시 이미 저장된 URL 은 **재수집(fetch) 하지 않고 skip**(이전엔 매 런 같은 URL 재-fetch → `saved:20` 계수) · `_stored_urls(source_id)` 헬퍼 신규(기존 저장 URL 집합, read-only) · `main()` RSS 루프가 `known_urls=_stored_urls(...)` 로 배선. **TDD:** `test_collect_rss_skips_known_urls_from_prior_run`·`test_stored_urls_reads_existing_fetch_meta` 신규 2개 (Red→Green) — 스위트 **1032→1034 Green**·회귀 0. **실측 (A15, 2026-08-19):** RSS 3 source `known_urls` 적용 → nvidia skip 20·amd skip 10·semi skip 10, **raw +0** — 이전 A-runs 가 매 런 같은 URL 을 재-fetch 하며 "saved:20" 으로 계수하던 것이 **중복 재-harvest** 였음을 확정, URL 기준으로 완전 차단. SLO-05 이 런 `measured:False`(전부 skip — skip 은 fetch 아님, 성공률 분모 부적절 · honest). **Spec 판정: 1.0.0 유지** — 변경이 설계 04 §2.1 이 이미 규정한 S1 idempotency 의 **미이행 이행**(새 계약 추가 아님), `known_urls` 는 선택 인자(역호환)·`_save_zone`(S2) 무변경. 문서: design 04 §2.1 구현 메모 + ROADMAP §3 수집 행·§5 changelog(본 항목). 다음: RSS 3 source 수집 지속(이제 URL-skip 으로 무중복) 또는 deferred SLO.
- **A16/A17 후보 종합 — RSS 수집 지속 + BIS 실현 불가 + deferred SLO 정직 유지 (04 §1.4, 11 §2.3).** "다음 후보 모두" 처리 (docs-only, 코드 변경 없음). **(1) RSS 수집 지속 (A16):** URL-skip 적용 런 — RSS 3 source 전 skip(nvidia 20·amd 10·semi 10), **raw +0** — 이 시점 신규 아티클 부재, URL-skip 이 중복 재수집 을 정확히 차단(무중복 확인). SLO-05 n=0(전 skip, fetch 아님)·honest. **(3) BIS 수출통제 후보 검증 (A17):** robots.txt **`User-agent:*` — AI 크롤러 명시 차단 없음(개방)** 이나 **RSS 전 피드 404(`/rss.xml`·`/news/rss.xml`·`/feed/`)** — 기존 `collect_rss` 아키텍처로 **수집 불가** 확정 (설계 04 §1.4 "BIS 는 RSS 없어 scraping Medium" 예상 정합). 도메인 가치 최고(수출통제)지만 채택하려면 **scraping 커넥터(새 진입점·정의 scope 확장) 개발** — 별도 의사결정 항목으로 분리, 현재 미채택. **(2) deferred SLO 재개:** #4(contradiction·lineage 골든 0건 미자연발생)·SLO-07(quarantine 모집단 0)·#9(1M 물리 미실행) — 오늘 신규(수십 건)로는 contradiction 자연발생 기대 불가, **정밀 우선 파이프라인 데이터 셰이프 특성으로 실측 여건 미충족 유지**(§6.2). SLO-06 확대는 이미 measured=True(n=40)라 추가가 판정을 바꾸지 않음(비차단·낮은 정보가치) → **정직히 추가 실측 여건 대기**. **종합:** 즉시 실제 수집은 RSS(무중복), 신규 소스·deferred SLO 는 데이터/커넥터/라벨링 종속으로 별도 결정. raw gitignore·커밋 문서만 → **Spec 1.0.0 유지**.
- **A18 BIS sitemap 커넥터 개발·배선·실측 + Spec 1.1.0 (04 §1.3/1.4, 크롤러 개발·비용 지출 없음 결정).** A17 이 "BIS RSS 없어 실현 불가" 로 유보했으나, **sitemap.xml(정적 37 URL)·robots `Allow: /`** 실측로 **sitemap 1급 kind 로 실현 가능** 재평가 → **정식 채택** (A17 결론 정정 — RSS 는 없어도 sitemap 으로 채집 가능, "scraping" 아님). **구현 (TDD, 코드):** `orc_citadel/connectors/sitemap.py`(`SitemapConnector`, 04 §1.2 discover/fetch 계약, `source_type=gov`) · `collect_sitemap()`(`known_urls` S1 skip+`_save_zone` S2 재사용) · `SOURCES` 에 `gov-bis-exportcontrol`(`sitemap`, `bis.gov/sitemap.xml`) · `main()` kind dispatch. 스위트 **1034→1038 Green**·회귀 0. **실측 (A18):** sitemap 37 `<loc>` 전부 수집 → **raw 104,868→104,905(+37)**(수출통제 정책·가이드 — country guidance·compliance·deemed exports), SLO-05 `success_rate:1.0 n=37`. **신호 수율:** `signal_density` **0.000**(claims 0, 비신호 arXiv 수준) → **`SIGNAL_SOURCE_IDS` 미포함**(A10 원칙 정합) — raw 수집 소스로 유효(쿼리·조사 재료). **Spec 번프: 1.0.0 → 1.1.0** — 새 커넥터·`sitemap` 1급 kind·새 소스 = 계약·정의 확장(1.0.0 호환 증분, additive). **비용 지출 없음**: robots 개방 + 정적 sitemap(XML) → headless 불필요, 무료/무비용. 문서: design 04 §1.3/1.4·README Spec 번프 + ROADMAP §3 수집 행·§5 changelog(본 항목). 다음: sitemap 기반 gov 소스 확장(다른 수출통제·규제 기관) 또는 수집 지속.
- **A19 후보 1·2·3 진행 — RSS/sitemap 수집 지속 + gov 확장 실측(미채택) + deferred SLO 재검토 (04 §1.4, 11 §2.3).** "다음 후보 1, 2, 3" 모두 진행. **(후보 2) 수집 지속 (A19):** RSS 3 source + BIS sitemap URL-skip 배치 → **raw 104,905→104,906(+1)** — AMD IR 신규 1건만 저장(nvidia 0·amd 1·semi 0), 나머지 nvidia 20·amd 9·semi 10·BIS 37 **전부 S1 URL-skip**(무중복 재확인, raw +1 임 = 신규만). SLO-05 `success_rate:1.0 n=77`(RSS 40 시도 + sitemap 37, in-memory 런별 독립 — skip 은 시도 아님). **(후보 1) sitemap 기반 gov 소스 확장 실측 — 추가 미채택 (정직-gap §6.2):** 후보 기관 sitemap.xml 실측 — **state.gov** HTTP 200 이나 실체는 "Technical Difficulties" HTML 오류 페이지(유효 sitemap 아님) · **home.treasury.gov** sitemap index(페이지당 2,000 loc ×10 ≈2만, 재귀 필요) · **ofac.treasury.gov** 단일 urlset(커넥터 재사용 가능!) 4,269 loc 이나 **4,124개(97%)가 recent-actions(3,137)+faqs(987) 비신호**, 러시아 제재 페이지(`russia-related-sanctions`) 본문에 반도체 신호 **0**(semi·conduct·chip·integrated circuit·export 전부 0, 379단어 짧은 안내) → **전체 수집은 신호 밀도 원칙(A10) 위배(노이즈 폭증), supply chain 타겟 소수도 비신호** → **BIS 가 유일 gov sitemap 소스로 안착, 추가 gov 확장은 데이터로 정당화되지 않음** (신호 수율 정합, 빈 소스를 채우기 위해 노이즈 수집하지 않음). SitemapConnector 인프라 자체는 **검증·재사용 가능**으로 유지(미래 신호성 소스 대비). **(후보 3) deferred SLO 재검토 (비용 유보 내):** **SLO-05** 재실측(A19 n=77 `success_rate:1.0`, 비용 0) ✓ — 유일하게 비용 유보 내 실측 가능 · **SLO-06** 측정 대상이 **LLM judge output schema**(`claude_judge.record_schema`) → **비용(LLM verifier) 의존으로 유보 유지**(비용 없이 실측 불가, §6.2) · **SLO-07** quarantine 모집단-0 재확정·유지 · **SLO-08** 실측 완주 완료(104,677건 5.6min), 1M 물리(#9)만 infra 비용 유보. **종합:** 즉시 유효 수집은 RSS+BIS(무중복), gov 추가·deferred SLO 실측은 신호/비용 정당화 대기. raw gitignore·커밋 문서만 → **Spec 1.1.0 유지**(sitemap kind·SOURCES 무변경 — 실측/문서만). §3 수집 행·SLO-05 행·design 11 §2.3 반영. 다음: RSS + BIS sitemap 수집 지속, 또는 신호 정당화되는 gov/규제 소스(타깃 카테고리) 재평가, 또는 비용 유보 해제 시 SLO-06/관련 실측.
- **A20 후보(a)·(b) 진행 — 수집 지속(+0 무중복) + gov/규제 신호 타깃 재평가(재기각, 추가 발견 기록) (04 §1.4, 11 §2.3).** **(a) 수집 지속 (A20):** RSS 3 + BIS sitemap URL-skip 배치 → **raw 104,906 유지 (+0)** — 전 source 전부 skip(nvidia 20·amd 10·semi 10·BIS 37 · A19 신규 AMD 1건이 known_urls 11 로 반영), **무중복 운용 안정 재확인**. SLO-05 이 런 n=0(전 skip — skip 은 fetch 아님, 성공률 분모 부적절 · honest). **(b) 신호 정당화 gov/규제 소스 타깃 재평가 — A19 기각을 더 깊은 실측으로 재확인, DPL CSV 발견:** A19 가 전체 수집 기준으로 기각했으나 "타깃 카테고리" 재평가 — **OFAC 최근 recent-actions**(20260818/12/07, ICC·베네수엘라 지정 등) 본문 반도체 신호 전부 0 (semiconductor·chip·GPU·integrated circuit 검출 불가) · **이미 수집된 BIS DPL(Denied Persons List, 754KB HTML)** 검사 — 정적 fetch 본문은 **제목+정부 사이트 헤더뿐, 실제 명단은 JS 동적 로딩**으로 content 미포함(NIST 동적 문제와 동류) · 단 **동일 DPL 이 정적 CSV 제공** 확인 — `media.bis.gov/sites/default/files/dpl_04142026.csv`, robots `Allow: /`(media.bis.gov 전면 개방) · CSV **110KB·564개 명단**(Name_and_Address·Effective_Date·Expiration_Date·FR Citations·Type of Denial), 반도체 직접 언급은 **semiconductor 0·IC 0·China 0**, 홍콩 4·electronics 4 소수 → **직접 supply chain 신호 약함** · **BIS Entity List** 는 `bis.doc.gov` robots Cloudflare 301 차단 + PDF/편집 리스트로 CSV 미노출 → **인입 경로 부적합**. **종합:** "신호 정당화되는 gov/규제 소스 추가 인입" 경로가 **재실측으로도 없음** (A19 결론 유지) — 정적 크롤러(04 §1.2) + 비용 유보 조건에서 gov 명단 신호 인입은 정당화되지 않음, 억지 생성 안 함(§6.2). **기록 가치:** BIS DPL CSV(정적·robots 개방·구조화 564행) 는 **미래 supply chain 명단 신호 소스 로 재사용 가능한 발견** — 현재는 반도체 신호 약해 미채택, 인입 시 `datafile`/목록 kind 계약 확장 필요(04 §1.4 우선순위·Spec 판단 별도). raw gitignore·커밋 문서만, 코드 변경 없음 → **Spec 1.1.0 유지**. §3 수집 행·SLO-05 행 반영. 다음: RSS + BIS sitemap 수집 지속, 또는 비용 유보 해제 시 SLO-06/관련 실측, 또는 DPL CSV 등 구조화 명단 kind 재검토.
- **A22 수집 지속 (후보 1) — RSS + BIS sitemap 4 source URL-skip 배치 (04 §1.4).** 사용자 지시 "1 진행"(후보 1). `collect_large.SOURCES` 의 4개 비-arXiv 소스(nvidia/amd/semiengineering RSS + BIS sitemap) 만 `collect_rss`/`collect_sitemap`(`known_urls=S1`) 로 순회 — arXiv 10만 bulk 제외(`/tmp/rss_sitemap_continue.py`). **raw 104,906→104,915(+9)** — nvidia +1(`blogs.nvidia.com/geforce-now-thursday-firefox`)·semiengineering +8(week-in-review·custom HBM·interposer 열팽창·CFET·feedforward 제어 등)·amd **+0**·BIS **+0**, errors **0**. **S1 URL-skip 실측:** nvidia 19·amd 10·semi 2·BIS **37 전부 known_urls skip** → **BIS sitemap 37 URL 은 완전 포화(전 skip, 유한 소스 소진 확정 실증)** · RSS 3 source 는 신규 유입 위주 무중복 운용 안정 재확인. SLO-05 이 런 n=9(fetch 시도=신규 저장만, skip 은 시도 아님)·`success_rate:1.0`. raw gitignore·커밋 문서만, 코드 변경 없음 → **Spec 1.1.0 유지**. 문서: ROADMAP §3 수집 행·§5 changelog(본 항목). 다음: RSS + BIS 수집 지속, 또는 SLO-06 운영 7d 축적(재확정), 또는 DPL CSV 등 구조화 명단 kind 재검토.
- **A23 "다음 후보 지속" — 수집 지속(+0 무중복) + BIS DPL CSV 구조화 명단 kind 필드-레벨 재검토 (04 §1.4, A20 재확인 심화).** **(a) 수집 지속 (다음 후보 1):** RSS 3 + BIS sitemap URL-skip 배치 → **raw 104,915 유지 (+0)** — nvidia 20·amd 10·semi 10·BIS **37 전부 S1 known_urls skip**, errors 0. **A22 신규 문서(9건)가 known_urls 로 정상 승계**(nvidia 38·semi 49 로 반영) → **연속 런 간 URL-skip 멱등 재확인, 0 재수집·0 중복**. BIS sitemap **37 URL 완전 포화 재확정**(유한 소스 소진 고착). RSS 3 source 는 피드 갱신 주기 대기 상태(연속 즉시 런에선 신규 없음) — **무중복 운용 안정 고착 실증**. **(b) DPL CSV 구조화 명단 kind 재검토 (다음 후보 3, A20 심화 — 필드-레벨 정량):** A20 가 "신호 약함" 으로 text 검색만 한 것을, 이번엔 **564행 CSV 전 필드(Name_and_Address·Type of Denial·FR Citations) 결합 정량 스캔+실제 매칭 행 열람**으로 재검토. 결과 — **반도체/수출통제 신호 실질 0**: semiconductor·integrated circuit·chip·GPU·wafer·microelectronics·entity·Russia·China·DPRK·Iran·MSS **전부 0회** · `electronics` 5회는 전부 **회사명 자가포함**(Manten Electronics Inc·Drayworth Electronics·Elektronische Bauelemente — 1981~2006 FR, 반도체 아님) · `PLA` 7회는 **전부 거리명 false positive**(Candlewood **PLA**ce 등 — 인민해방군 아님, 0 military end-user) · `Hong Kong` 4·`export` 1 = 1984~2007 년대 역사 회사명/주소지(Shuttle Long·WYSH·NORSAL EXPORT Limited) · `Type of Denial` 559 Standard+3 Non-Standard(빈 2), **신규/현대/첨단기술 귀속 명단 0**. **판정: `datafile`/목록 kind 신설·DPL 인입 미채택 유지 (A20 결론 유지·근거 심화)** — 정적·구조화·robots 개방 CSV 임에도 신호 밀도 원칙(A10) 실측상 **supply chain 신호가 없어 인입 정당화 안 됨**(빈 소스를 채우기 위해 노이즈 수집 억제, §6.2). `datafile` kind 계약 확장은 **신호 정당화되는 구조화 명단(entity list·특정기술 제한) 실측 확보 시** 별도 판단 — 미래 재사용 발견으로 유지. raw gitignore·커밋 문서만, 코드 변경 없음 → **Spec 1.1.0 유지**. 문서: ROADMAP §3 수집 행·§5 changelog(본 항목). 다음: RSS + BIS 수집 지속(피드 갱신 주기 타이밍 재러), 또는 SLO-06 운영 7d 축적(재확정, 7일 운용 필요), 또는 신호 정당화되는 구조화 명단 소스 재평가.
- **A24 "다음 후보 진행" — 수집 지속(+0, 피드 소진 고착) + SLO-06 표본 확대 실측(10→30건 재확정) (04 §1.4, 11 §2.3).** **(a) 수집 지속:** RSS 3 + BIS sitemap URL-skip 배치 → **raw 104,915 유지(+0)** — nvidia 20·amd 10·semi 10·BIS **37 전부 known_urls skip**, errors 0. A22/A23 이어 3연속 런 무중복 → **RSS 3 source 는 세션 시계열에서 피드 갱신 주기보다 짧은 빈도의 순시 런에는 신규 없음(소진·갱신 대기 상태 고착)**, BIS sitemap **37 URL 완전 포화 확정 실증**. 반복 폴링은 저가치 — 무중복 운용 안정은 이미 확보. **(b) SLO-06 표본 확대 (후보 2 진전 — 7d 재확정 방향):** 무비용 `bunker-flash` LLM(client: OpenAICompatibleClient·litellm)로 supply chain claim-id pair **30건(canonical 15·contradiction 15)** 실측 → **`pass_rate 1.0`(30/30) `within_slo:True` `measured:True` n=30 n_unknown=0** — **A21 n=10 → A24 n=30 표본 3배**, canonical·contradiction 양 경로 **100% 통과 재확정**(None verdict 0, A21 결함 수정 후 경로 건전성 확인). 실제 비용 **$0**(무비용 datamaker proxy; 표시 $0.057 은 S42 placeholder 환산) · read-only·유한 상한. **Spec 판정: 1.1.0 유지** — 실측·문서만, 코드 변경 없음(결함 수정은 A21 이미 반영). **판정 유지: 확정(잠정)** — 30건 유한 샘플, 운영 7d rolling 축적 후 재확정(여전히 7일 운용 태스크). 문서: design 11 §2.3 SLO-06 행 + ROADMAP §1·§5 changelog(본 항목). 다음: 수집은 피드 갱신 주기 후 재러, SLO-06 은 운영 7d 축적(7일 운용 필요), 또는 신호 정당화되는 구조화 명단 소스 재평가.
- **A21 SLO-06 무비용 LLM 실측 — "현재 설정 LLM(무비용) 대체"로 deferred 해소 + 설계-프롬프트 불일치 발견·수정 (07 §7, 05 §5.2, 11 §2.3).** 사용자 결정: "비용은 현재 설정한 LLM(무비용 bunker-flash)이 무비용 — 이로 대체 가능한가" → **SLO-06 실측을 무비용 LLM으로 수행**. `.env`(`LLM_PROVIDER=litellm`·`LLM_BASE_URL=llm-proxy.datamaker.io`·`LLM_MODEL=bunker-flash`) 로드 → `build_llm_client()` → `OpenAICompatibleClient`(bunker-flash, vllm) 실제 연결 검증·**무비용** (chat/completions HTTP 200, `{"ok":true}` 응답). **실측 (10건 supply chain pair, contradiction 5·canonical 5):** 초기 결과 **통과율 0.5·`slo-gate`** — canonical 5/5 통과·contradiction **0/5 전수 실패**. 근본 원인 **2건 결함 발견·수정 (TDD):** (1) **버그 — `validate_contradiction_verdict` 크래시**: `evidence_spans` 항목이 dict 가 아닐 때(str) `s.items()` `AttributeError` → ADR-704·07 §7("구조화 실패 → None·후보 유지") 위반, 예외 전파 → **방어적 수정**(비-dict → None). (2) **설계-프롬프트 불일치**: contradiction 프롬프트가 `evidence_spans`를 모호한 `<지지 구간>` 으로 정의(05 §5.2 는 dict `doc_id/char_start/char_end`) → bunker-flash 가 **str 리스트 체계적 반환** → **프롬프트를 설계 05 §5.2 dict 구조로 정합**. **재실측 (동일 10건):** canonical 5/5 + contradiction 5/5 → **`pass_rate 1.0` ≥95% `within_slo:True`** measured=True n=10 · 무비용(usage 2190/987 tokens·10 calls, 비용 지출 0). **Spec 판정: 1.1.0 유지** — 검증 수정은 07 §7/ADR-704 기존 계약 이행(버그), 프롬프트는 설계 05 §5.2 미이행 이행(additive) — 계약/스키마 추가 아님. **TDD:** `test_contradiction_verdict_malformed_evidence_span_returns_none`·`test_contradiction_prompt_specifies_dict_evidence_spans` 신규 — 스위트 **1038→1040 Green**(회귀 0). **SLO-06 확정(잠정)** 전환 — 10건 샘플 실측, 운영 7d rolling 축적 후 재확정. **함의:** 비용 유보가 SLO-06 실측을 막던 것을 **무비용 LLM 대체로 해소** — SLO-06 은 유일 비용-유보 대상이었으나 실측 완료(나머지 #4 라벨링·SLO-07 은 모집단/라벨링 종속으로 유지 유지). 문서: design 11 §2.3 표·실측 메모 + ROADMAP §1·§5 changelog(본 항목). 다음: RSS + BIS sitemap 수집 지속, 또는 SLO-06 운영 7d 축적(재확정), 또는 DPL CSV 등 구조화 명단 kind 재검토.
- **A12 수집 지속 — RSS 3 source refresh + arXiv 최신 윈도우, raw 104,854 (+48) + arXiv 중복 발견 (04 §1.4, 11 §2.3).** gov 보류 후 기존 RSS 3 source 로 수집 지속 → **raw 104,806 → 104,854 (+48)** — nvidia +20·amd +10·semiengineering +10(content-hash dedup 정상) · **arXiv `windows=1`(최신 연대) 신규 +8** — A7/A8 과거 연대 소진과 달리 **최신 윈도우는 새 arXiv 논문 지속 유입**(과거 소진 ≠ 최신 소진, `abs/2608.*v1` 08:37 새 URL 확보 — A12 +8 유효). SLO-05 n=140(RSS 40+arXiv 100, 런별 독립)·성공률 1.0. ⚠️ **arXiv 중복 1,247건 발견 (2026-08-19, 추적 검증):** `research-arxiv-cs-cr` 104,471 doc = **고유 URL 103,224 + 중복 1,247**(URL 1,199개가 doc_id 2+). 같은 abs 가 doc_id 2 개로 저장 — **content 완전 상이(3KB vs 42KB, 전체/부분 응답 혼재, memory "arxiv raw 형식 혼재")**, 2026-08-11 집중(1,199). gov(동적 페이지)와 달리 arXiv 는 **수집 품질 차이(부분 응답)** 로 중복 누적. **공통 근본 원인:** content-hash idempotency(03 §7)가 **URL 기준이 아니라 변동 본문 기준** → 동일 URL 재경로 수집 시 중복. **전체 raw(104,854)에 잠재 중복 존재** — gov(40) 는 정리했으나 arXiv(1,247) 는 성격(부분 응답 삭제 vs 보존) 판단 필요. **근본 해결: URL 기반 idempotency 키(04 §2.1) 전면 적용 — 계약·Spec 번프 수반, 별도 의사결정으로 명확히 분리.** RSS 3 source 는 dedup 정상·유일 지속 신규 주축. raw gitignore·커밋 문서만. code 변경 없음(수집 config 데이터) → **Spec 1.0.0 유지**. 다음: arXiv 중복 1,247 처리 방향(부분 응답 clean) 또는 URL idempotency(Spec 번프) 별도 결정.
- **A5 수집 지속 운용 배치 — raw 성장 + SLO-05 표본 (04 §1.4, 11 §2.3).** `collect_large`(RSS 3 source + arXiv `windows` 날짜윈도우, S50 10k 한계 우회) 의 실제 수집 런을 폴라이트(1 req/3s)·resumable(content-hash 멱등)·`slo_log` 주입으로 재가동 → **raw 104,642 → 104,677 (+35)**. 세부: arXiv 날짜윈도우(과거 연대 미수집) **+25 신규 metadata**(CC0 경로 — abs 스크레이핑 없음, anti-bot 회피) · RSS 신규 **+10**(semiengineering 신규 아티클; nvidia·amd 는 모두 content-hash **중복** — `collect_rss` 의 `saved` 카운터가 fetch 성공을 세는 것임을 정직히 구분, 실제 디스크 신규는 +10). **SLO-05 표본 n=440** `{success_rate:1.0, n_success:440, n_attempt:440, n_unknown:0}` measured=True (= arXiv `total=400`+RSS 40, `within_slo=True`(≥99%)) — ⚠️ **정정 (2026-08-18, A8 관측):** `slo_log` in-memory 라 "n=50 → n=440 누적 확대·시도 400건 추가 축적" 서술은 부정확 — n=440 은 이번 런 독립 시도 수, 원점 n=50(2026-08-12) 과 런 간 누적 관계 없음. 성공률 1.0 유지. raw 데이터는 gitignore(수집 원문 커밋 제외) — 커밋은 문서만. 코드 변경 없음 → **Spec 1.0.0 유지**. §3 Phase 6 A5 행·SLO-05 행·design 11 §2.3 반영. 다음: 수집 계속 확대(10만+ → 더)는 사용자 인계로 폴라이트 러너 실행 가능.

### 2026-08-12
- **Phase 6 — predicate 정합화 재검증: quarantine 71→0건.** 정합화 커밋(a3eadd8) 후 실제 재처리(read-only `:memory:` 골든 500건, `run_pipeline(slo_log=)`) 로 영향 실증 — **`open_quarantine` 71→0건** (이전 전부 `unknown_predicate:partners`), promoted_claims 425 정상 승격·nodes 2186. `partners_with` predicate 는 §4-2 폐쇄성 게이트 통과 → 해당 claim quarantine 제거 확인. SLO-07 의 **고정 원인(unknown_predicate) 성분 제거 완료** — real quarantine 체류(진입→해소) 는 여전히 재평가 인프라 필요로 not-measured(구조적) 대기 유지. §3 Phase 6 SLO-07 행·design 11 §2.3 반영. 코드 변경 없음 → **Spec 1.0.0 유지**.
- **Phase 6 — predicate 정합화: extractor `partners` → `partners_with` (SLO-07 quarantine 근본 해소).** SLO-07 원인 분해(2026-08-12, 71건 `unknown_predicate:partners`) 의 **근본 원인이 "온톨로지 어휘 확장 여부"가 아니라 **추출·온톨로지 정합화 버그**임을 확정. 온톨로지 02 §5.1·05·08 은 전부 공식 `partners_with`(gate.py `CONTROLLED_PREDICATES` 포함) 를 표준으로 봉인 — 반면 extractor(`extract_claims.py`) 의 파트너십 규칙만 과거 축약 `partners` 를 방출해 §4-2 폐쇄성 게이트를 떨어뜨려 71건 permanent quarantine 유발. 컨트롤드 세트를 오염(`partners` 추가)하는 대신 **온톨로지 표준 `partners_with` 로 정합화** (frozen Spec 과 일치 — 어휘 변경 아님). `extract_claims.py` 파트너십 규칙 predicate `"partners"` → `"partners_with"`. **재처리 시 `unknown_predicate:partners` quarantine 이 사라져 해당 claim 은 정상 승격** — SLO-07 의 "고정 원인" 성분이 제거됨 (남은 quarantine 근거는 subject 미해소·저신뢰·provenance 등 정상 게이트). TDD — `test_claims.test_partnership_claim` 을 `partners_with` 표준 단언으로 갱신(Red→Green) + `test_gate` 신규 1개(어휘에 `partners_with` 포함·`partners` 미포함·partners_with predicate quarantine 없음) — 스위트 **1030→1031 통과**(회귀 0). read-only 제외·결정적·mock/실측 격리. **Spec 1.0.0 유지** — design 05·02 이미 `partners_with`, 코드만 정합 (계약 변경 없음). §3 Phase 6 SLO-07 행·design 11 §2.3 반영.
- **Phase 6 — SLO nightly 게이트 실측 주입, DoD ② 운용 루프 가동 (design 11 §2.3).** 봉인된 nightly 게이트(`slo_nightly_gate`, 998) 에 **이번 세션의 실제 실측값을 주입**해 운용 루프 가동을 실증. `run_nightly_gate` — SLO-01 `p95_ms=495,638`·SLO-05 `success_rate=1.0`·SLO-06 `pass_rate=1.0`·SLO-07 `None`·SLO-08 `{full_ms:16830}` 주입 → **`ok=[SLO-01, SLO-05, SLO-06]`·`not_measured=[SLO-07]`(구조적)·`violations=[]`·`error_budget={violations:0, measured_count:3, ratio:0.0}`**. SLO-08 과 이 `evaluate_slo08` 은 `unclassified` 로 분류(분류 계약 정직 동작). Watchtower 운영 경보 라우팅(ADR-1104) 함께 구동 — 위반 0·not-measured 노출. **DoD ② 운용 루프(측정→판정→경보→error budget) 실측 주입 가동 확인**. read-only·결정적. 코드 변경 없음 → **Spec 1.0.0 유지**. §3 Phase 6 nightly 게이트 행·design 11 §2.3 반영.
- **Phase 6 — SLO-06 표본 확대 (schema 통과율 실측 n=20 → n=40).** 실제 LiteLLM judge(`bunker-flash`) 로 실제 원문 결정적 체인 판정 표본 확대. `_BoundedJudge` cap 20→40 — canonical·contradiction **40건 실제 판정** → 관측 로그 → sealed 하니스 `schema_pass_rate` **`{pass_rate:1.0, n_pass:40, n_total:40, n_unknown:0, measured:True}`** — **SLO-06 measured=True 유지·표본 2배**. read-only(`:memory:` zone)·유한 상한(40)·`n_unknown=0`. usage 8858/3455 tokens·40 calls. 더 넓은 샘플·실데이터 골든(contradiction 미자연발생)은 차기 여건. §3 Phase 6 SLO-06 행·design 11 §2.3 반영. 코드 변경 없음 → **Spec 1.0.0 유지**.
- **Phase 6 — SLO-05 목표치(≥99%) 판정 + 표본 n=50 (design 11 §2.3).** `slo_log` 주입으로 SEC(2 CIK)+RSS(3 source) 실제 수집 재실행 → 성공/실패 시도 **50건 축적** → sealed 하니스 `collect_success_rate` **`{success_rate:1.0, n_success:50, n_attempt:50, n_unknown:0, measured:True}`** — **목표 `≥99%` 대비 `within_slo=True`** (기계적 판정, 표본 50건). SEC 쪽은 content-hash 중복(skipped 10)으도 fetch 성공 시도로 계수·RSS 양쪽 기록 경로로 시도 확대. **SLO-05 목표 판정 기록** — 단, 50건 채널 성공률 1.0 이 **일반화**되지 않도록(§6.2) 표본 한계는 후속 수집 런에서 더 확대하며 명시. §3 Phase 6 SLO-05 행·design 11 §2.3 반영. 코드 변경 없음 → **Spec 1.0.0 유지**.
- **Phase 6 — SLO-01 실측 measured=True (신규 문서 → graph 반영 지연 p95).** deferred SLO-01 을 **진짜 실측**. **정직 경계:** 기존 원문의 `fetched_at` 은 과거(8/18 이전)라 '수집→반영 지연' 이 아니라 '문서 나이' 가 됨(오인) → **신규 RSS 수집**(`fetched_at≈now`, SOURCES 3개) 으로 신규 문서 77건 확보 → 실제 반영 경로 `bulk_pipeline`(read-only `:memory:` zone) 구동. 측정 공식(설계 11 §2.3) `graph_commit_ts − fetched_ts`(p95) — 배치 단일 주기 반영 경계(스케줄러 incremental commit, 06 §7.2) 로 각 문서 지연 = 반영 완료 시각 − 본인 `fetched_at`. **`{p95_ms:495,638 (~8.3min), n:77, classified:'ok'}`** — `≤ 30 min` 목표 내(하니스 봉인 923 `evaluate_slo01`). wall 3.895s·nodes 549. **SLO-01 `measured=True`** + &nbsp;**A5 수집 지속**(RSS +67 신규 문서) 동시 진행. read-only·결정적. 표본 77건은 이 수집 런 기준 — 더 넓은 샘플은 후속 런. 코드 변경 없음 → **Spec 1.0.0 유지**. §3 Phase 6 SLO-01 행·design 11 §2.3 반영.
- **Phase 6 — SLO-05 실측 표본 확대 (성공률 실측 n=1 → n=10, design 11 §2.3).** SLO-05 가 measured=True 지만 `n_attempt=1` 표본 부족이었던 것을 **다출처 확대 수집 런으로 보강**. `collect_sec(limit=5, ciks=["1045810","1046179"])` — NVDA·TSM 두 CIK 에서 실제 fetch(성공/실패 양쪽 `record_collect`) → 관측 로그 누적 → sealed 하니스 `collect_success_rate` 판정 **`{success_rate:1.0, n_success:10, n_attempt:10, n_unknown:0, measured:True}`** (counts `{saved:7, skipped:3, errors:0}` — 중복 저장 3건은 성공 시도로 계수). **성공률 1.0 은 10건 시도 기준 실측으로 유지** — 목표치(≥99%) 판정은 표본을 충분히 축적한 다음 런에서 확정(여전히 저표본 정직 표기§6.2). §3 Phase 6 SLO-05 행·design 11 §2.3 반영. 코드 변경 없음 → **Spec 1.0.0 유지**.
- **Phase 6 — SLO-07 quarantine 원인 분해, 구조적 not-measured 확정 (design 11 §2.3).** 실제 파이프라인(`run_pipeline(slo_log=)`) 500건 구동: **`open_quarantine=71`, `dwell_resolved=0`**. quarantine 원인 분해 → **71건 전부 `unknown_predicate:partners`**(02 §4-2 폐쇄성 미포함, 고정 원인). SLO-07 해소는 현 체인에서 **구조적 발생 불가** 확인 — Gate 결정성+단일 패스+고정 원인(후속 라운드가 해소시킬 의존 상태 차이 없음). 71건은 permanent quarantine(정당 — 미지 predicate authoritative 금지). **not-measured 는 측정 부재가 아니라 구조적 성질** — real 체류는 quarantine 이 promoted 로 해소되는 element(의존 상태 변화 + 재평가) 가 필요 → 다중 라운드·분산 후속 재평가 인프라와 함께. §3 Phase 6 SLO-07 행·design 11 §2.3 반영. 코드 변경 없음 → **Spec 1.0.0 유지**.
- **Phase 6 — SLO-06 measured=True 전환 실측 (design 11 §2.3).** 실제 LiteLLM judge 로 SLO-06 진짜 측정. `.env` `LLM_PROVIDER=litellm`/`LLM_BASE_URL`/`LLM_MODEL=bunker-flash` config(`build_llm_client`) → 실제 proxy 라우팅. 실제 원문 결정적 chain 으로 candidate 쌍 생성, `ClaudeJudge(slo_log=)` 로 canonical·contradiction **20건 판정** → 관측 로그 → sealed 하니스 `schema_pass_rate` **`{pass_rate:1.0, n_pass:20, n_total:20, n_unknown:0, measured:True}`** — **SLO-06 measured=True** (프로바이더 config e3fc602 실증). read-only(`:memory:` zone)·유한 상한(20, `_BoundedJudge` cap)·`n_unknown=0`. usage 4420/1678 tokens. 표본 20건·실데이터 골든은 차기. §3 Phase 6 SLO-06 행(🟢)·design 11 §2.3 반영. 코드 변경 없음 → **Spec 1.0.0 유지**.
- **Phase 6 — LLM 프로바이더 config: Anthropic/OpenRouter/LiteLLM (design 07 §6.2).** ClaudeJudge 하드코딩 anthropic 의존 → 명시적 환경 config 로 확장. `llm_providers.py` — `OpenAICompatibleClient`(OpenRouter·LiteLLM 공용, OpenAI-compatible `/chat/completions` httpx 어댑터, 신규 의존성 0) + `build_llm_client()`(다른 백엔드와 동일 `LLM_PROVIDER/LLM_MODEL/LLM_API_KEY/LLM_BASE_URL` 환경변수 패턴). SDK 부재·불가 provider → `None` → ClaudeJudge stub 폴백(ADR-507). `ClaudeJudge._version()` `model_provider`/`model_id` 를 하드코딩 → 실제 client provider·model 로 정직 산출(07 §6.1 재현성). 주입 계약·출력 재검증·stub 폴백 무변경 → judge 소비 계약 intact. wire 계약·스키마 변경 없음 → **Spec 1.0.0 유지**. TDD — `test_llm_providers` 신규 10개(openrouter/litellm POST·usage·비JSON {}·provider 선택·기본 스텁·불가 provider None·judge version tuple openrouter) — 스위트 **1020→1030개 통과**(회귀 0). `.env.example` LLM_* 4개 추가·design 07 §6.2 반영. 다음: SLO-06 실측 — 실제 프로바이더(OpenRouter/LiteLLM) 키 허가 후 구동.
- **Phase 6 — SLO-05 measured=True 전환 실측 (design 11 §2.3).** 신규 수집 런으로 SLO-05 진짜 측정. `collect_sec(limit=1)` — 성공/실패 **양쪽 기록하는 유일한 경로**. SEC EDGAR 1건 실제 fetch 성공 → 관측 계층 누적 → sealed 하니스 `collect_success_rate` 판정 **`{success_rate:1.0, n_success:1, n_attempt:1, n_unknown:0, measured:True}`** — **SLO-05 measured=True** (배선 3ecb063 실데이터 실증). `n_attempt=1` 표본 부족 정직 표기 — 목표치(≥99%) 확정·샘플 확대는 다음 수집 런. §3 Phase 6 SLO-05 행·design 11 §2.3 반영. 코드 변경 없음 → **Spec 1.0.0 유지**.
- **Phase 6 — SLO-05/07 부분 배치 실측, DoD ① 경로 실증 (design 11 §2.3).** 배선된 관측 계층을 유한 배치(1,000건, 기존 104,554 read-only 재처리, `bulk_pipeline(slo_log=...)`)로 **실제 구동**해 배선의 실동작을 실증: **SLO-07 `open_quarantine=71`** — quarantine 진입 로그가 실제 `Gate`→`slo_log` 경로로 71건 누적(**배선 실증**, 미배선 시 존재 불가). 그러나 **어느 SLO도 measured=True 가 안 됨**(honest-gap §6.2): SLO-07 `dwell_resolved=0`(in-memory 단일 렌더 quarantine 해소 미발생→체류 미확정), SLO-05 `collect_attempts=0`(기존 문서 재처리 fetch 없음→분모 부재), SLO-06 `schema_checks=0`(결정적 체인 `judge=None` verdict 미검증→분모 부재). **배선 실증 + 실측 여건 의존 명시**: SLO-05 신규 수집 런·SLO-06 실제 LLM judge·SLO-07 quarantine 해소 유발 재평가 인프라 갖춰질 때 진짜 measured 전환. 특정 SLO 변경 없음 → **Spec 1.0.0 유지**. §3 Phase 6 SLO-05/06/07 행·design 11 §2.3 반영. 다음: 신규 수집·LLM·분산 재평가 여건에서 실측 전환.
- **Phase 6 — 실행 경로 slo_log 배선, DoD ① 경로 완결 (design 11 §2.3).** 위 배선은 **선택 주입 계약**이었고 실제 진입점 `run_pipeline`(pipeline_runner) 이 내부 `Gate()` 를 직접 생성해 `slo_log` 전달 경로가 **없던 미배선**을 해소 — `run_pipeline(..., slo_log=None)` → `Gate(slo_log=)` 로 quarantine 진입/해소 로그가 실제 파이프라인 실행 시 **자동 축적** (SLO-07) · `bulk_pipeline` 도 스레딩. 기본 None → 기존 동작 무변경·**Spec 1.0.0 유지** (선택 인자만). TDD — 배선 테스트 신규 2개 — 스위트 **1018→1020개 통과**(회귀 0). §3 Phase 6·design 11 §2.3 반영. 다음: 실제 런에서 SLO-05/06/07 실측·목표 확정.
- **Phase 6 — SLO 관측 로그 배선, DoD ① 선행 완결 (design 11 §2.3).** 관측 계층(`slo_observation_log`)을 **실제 측정 경계에 배선** — 다음 수집·처리 런부터 로그 자동 축적 → SLO-05/06/07 실측 경로 **코드 완결**. 전부 선택 주입(`slo_log=None` 기본) → 기존 동작 무변경·**Spec 1.0.0 유지**. **SLO-05** `collect_large`(`collect_arxiv`/`collect_rss`/`collect_sec`) — 문서 저장·fetch 실패 지점 `record_collect` · **SLO-06** `claude_judge.ClaudeJudge(slo_log=)` — verdict 검증 `record_schema`(스텁 폴백=미검증 계수 제외, §6.2) · **SLO-07** `Gate(slo_log=)` — quarantined→진입·promoted 재평가(해소)→종료 (진입-무해소 제외·진입 없는 해소 무시, §6.2). read-only·결정적·mock/실측 격리. TDD — 배선 테스트 신규 11개 — 스위트 **1007→1018개 통과**(회귀 0). §3 Phase 6 SLO-05/06/07 행·design 11 §2.3 반영. 다음: 다음 런에서 로그 자동 축적 → SLO-05/06/07 실측·목표 확정.
- **Phase 6 — SLO-05/06/07 실측 관측 로그 신설, DoD ① 선행 (design 11 §2.3).** deferred SLO-05/06/07 가 "실데이터에 측정 로그가 없어 측정 불가"였던 근본 원인(attempt/failure/quarantine 로그 부재 → 분모 0 → vacuous) 해결 — **다음 수집·처리 런부터 실제로 측정 가능**하게 하는 관측 계층 신설. `slo_observation_log.py` — sealed 하니스(`slo_metrics_harness` **수정 없음**)의 **입력 셰이프를 생산**하는 비침투적 어댑터 + clock 주입(결정적·mock/실측 격리). **SLO-05** `record_collect`→`success_results`→`collect_success_rate`(시도/성공) · **SLO-06** `record_schema`→`schema_results`→`schema_pass_rate`(verify_* 통과) · **SLO-07** `record_quarantine_enter`+**`record_quarantine_exit`(종료 이벤트 신설 — 기존 코드엔 종료 개념 없음)** → `dwell_entries`(=(enter_ms, exit_ms)) → `quarantine_dwell_median`(진행 중·진입-무종료·재진입·비정방향 honest-gap §6.2 제외). 로그 부재 → 하니스 `measured=False`/`not-measured` 자동(부재가 OK 아님). read-only(불변식 §3-3 — 비파괴 조회)·결정적. **스키마·계약 변경 없음 → Spec 1.0.0 유지** (신규 모듈만, 기존 하니스·파이프라인 무변경). TDD — `test_slo_observation_log` 신규 9개 — 스위트 **998→1007개 통과**(회귀 0). §3 Phase 6 SLO-05/06/07 행·design 11 §2.3 반영. 다음: 다음 수집 런에서 로그 주입해 SLO-05/06/07 실측·목표 확정(contradiction·lineage 재수집과 동일 실측 여건 대기).
- **Phase 6 — SLO-08 실측 (실측 진행, design 11 §2.3).** deferred SLO-08 "100만 문서 전체 재처리 시간 벤치마크 공개" 의 **실측 실행** — 기존 "104,544건 재처리 완주" 가 Q4 signal-slice 재처리이며 full rebuild 총 벽시계가 **영속 기록 없음**을 정직히 재확인, 전체 104,554건 단일 프로세스 재처리는 이 환경에서 ~2h 걸려 **3차례 완주 실패**(58min·3h 시도 모두 kill) → **유한 배치 실측 + 정직 외삽** 전환. `reprocess_benchmark`(SLO-08 하니스, read-only)에 실제 `full_ms` 주입 실측: **배치 1,000건 in-memory full rebuild 벽시계 = 16.83s → measured=True·per-doc 16.83ms**`bulk_pipeline(zone, mutation_log=None)`·판독 전용). 실측 per-doc 계수로 전체 외삽: 104,554 × 16.83ms ≈ **1.76h — projected(measured=False)** 명시(전체 완주가 아니므로 §6.2 honest-gap). **직접 측정(배치)과 외삽(전체) 구분 표기**로 과대 주장 없음 — 전체 완주 벽시계는 분산 batch(01 §6)·부하 인프라 갖춰질 때 재측정 대기. read-only·결정적·mock/실측 격리 유지. **스키마·계약 변경 없음 → Spec 1.0.0 유지** (코드 변경 없음 — 실측·문서화). §3 Phase 6 SLO-08 행·design 11 §2.3 반영. 다음: 실측 여건 갖춰질 때 전체 재처리 완주 재측정 또는 나머지 deferred.
- **Phase 6 — 코드 봉인 축 완결·블록업 (스위트 998 Green).** Phase 6(Stable 운용, blueprint 미정의 — ROADMAP 신규 정의) 에서 **코드로 봉인 가능한 축을 전부 완결** (DoD ② 운용 루프 체계): deferred SLO 하니스(SLO-01 923·SLO-05/06/07/08 948) · tool JSON schema 확정(967, 07 §7) · Watchtower SLO 경보 라우팅(984, 11 §2.3·ADR-1104) · **SLO nightly 게이트(998, DoD ② — 측정→판정→경보→error budget, CI 비차단)**. 전 작업 read-only(불변식 §3-3)·결정적·mock/실측 격리 · **Spec 1.0.0 유지** (스키마·계약 실질 변경 없음). **실데이터 재확인(read-only, DuckDB 직접 조회):** ER·claim 측은 실데이터 골든으로 이미 measured(`golden_entity_pairs` 5·`golden_pairs` equivalent 22) — **contradiction/conflict·lineage(`golden_lineage_pairs`) 는 여전히 0건 미자연발생**, 1M 부하는 미실행 → **남은 DoD ① measured 전환(contradiction·lineage)·#9·SLO 목표치 확정은 실데이터/인프라/부하 종속** — 실측 여건 갖춰질 때 재개 대상으로 대기(과대 주장 금지, §6.2). §1 현재 단계→블록업·상태 요약·§3 Phase 6(#4·9 대기 표기) 반영.
- **Phase 6 — SLO nightly 게이트 봉인, DoD ② 운용 루프 가동 (design 11 §2.3, 10 §1.4).** Phase 6 DoD ② "지속 수집이 자동 신호·SLO 에 반영되는 운용 루프" 의 **통합 오케스트레이션** 봉인 — `slo_nightly_gate.py`. 각 SLO 하니스 **측정 → 판정(evaluate_slo0*) → Watchtower 운영 경보 라우팅 → error budget** 을 하나의 기계적 nightly 게이트로 연결(10 §1.4 CI 비차단). `NIGHTLY_SLOS=(SLO-01/05/06/07/08)` · `classify_slo(미지정 id ValueError)` · `run_nightly_gate(measurements주입, router)` — `per_slo`·`violations`·`not_measured`(honest-gap §6.2 — 위반/분모 제외)·`error_budget{violations, measured_count, ratio}`(SLO-08 게이트 제외)·`router`(fire-once 상태). read-only·결정적 — 게이트는 분류·알림 산출물만, 저장·발송·스케줄은 운영 드라이버. **스키마·계약 변경 없음 → Spec 1.0.0 유지.** TDD — `test_slo_nightly_gate` 신규 14개 — 스위트 **984→998개 통과**(회귀 0). §3 Phase 6 nightly 게이트 행 ✅·design 11 §2.3 반영. 다음: 실데이터 축적 시 nightly 게이트 실측 주입·SLO 목표 확정.
- **Phase 6 — Watchtower SLO 운영 경보 라우팅 봉인 (design 11 §2.3, ADR-1104).** §2.3 계약("SLO 위반은 Signal Spire 결론 알림이 아니라 **Watchtower 운영 경보**로 라우팅")을 코드로 봉인 — `watchtower_slo_alert.py`, Signal Spire `signal_spire.py` 결론 알림과 **별개 채널 `watchtower-operational`**. **위반 = `classified=="slo-gate"` 만** (각 SLO 하니스 `evaluate_slo01/05/06/07` 산출 shape 주입, 10 §1.4 CI 비차단 nightly) · **`not-measured` 는 위반 아님** — honest-gap(§6.2 부재가 OK 아님) → 버리지 않고 `not_measured` 버킷으로 노출 · **fire-once** `slo:{slo_id}` dedup_key 재알림 금지(ADR-1104) · **§2.3 rolling 창** SLO 별 메타(7d/1d/30d/release) · classified 미지정은 `unclassified` 버킷으로 퉁치지 않음. 알림에 측정 스냅샷(값·목표·within_slo) 담아 온콜 판단 근거 제공. read-only·결정적 — alert 는 산출물, 저장·발송은 호출자. **스키마·계약 변경 없음 → Spec 1.0.0 유지.** TDD — `test_watchtower_slo_alert` 신규 17개 — 스위트 **967→984개 통과**(회귀 0). §3 Phase 6 Watchtower 행 ✅·design 11 §2.3 반영. 다음: 실데이터 축적 시 위반 라우팅 실측 주입 또는 나머지 deferred.
- **Phase 6 — tool JSON schema 확정 (design 07 §7 deferred 해소).** 07 §7 "각 tool별 구체 JSON schema 본문은 미확정"이 실제로는 **4 tool 계열이 이미 추출·판정 골든으로 검증된 코드**에 존재함을 정직히 정리 (측정 없는 확정 금지 blueprint §20 — 골든 검증된 contract). `tool_schemas.py` — 명시적 JSON Schema + 검증기로 공개 계약 봉인: **Extractor claim**(`CLAIM_CANDIDATE_SCHEMA`, 05 §6·provenance_ref ADR-305) · **canonical verdict**(07 §4.2 7라벨 enum) · **contradiction verdict**(07 §5.2 verdict/conflict_type/rationale 필수) · **synthesis report**(07 §3.8/§9). 각 `output_schema_version="1.0.0"` (07 §7 → [03] 저장 스키마 정합, contract test 대상 blueprint §15). 검증기 read-only·결정적, 실패 시 `{valid, reason}` — quarantine 경로. 스키마 실질 값 변경 없음(기존 validate_* 골든 그대로) → **Spec 1.0.0 유지**. TDD — `test_tool_schemas` 신규 19개 — 스위트 **948→967개 통과**(회귀 0). §3 Phase 6 tool schema 행 ✅·design 07 §7 반영. 다음: 실데이터/API·저장 contract 나머지 deferred.
- **Phase 6 — SLO-05/06/07/08 실측 하니스 봉인 (design 11 §2.3).** `slo_metrics_harness.py` — deferred SLO-05/06/07/08 의 **측정 계약·하니스** 봉인 (SLO-01 과 동일 패턴, 목표치 실데이터 축적 후 확정). **SLO-05** `collect_success_rate` — 성공/총 시도(`≥99%`), 미측정(None) 분모 제외·`n_unknown` 노출(honest-gap §6.2) · **SLO-06** `schema_pass_rate` — 통과/총 검증(`≥95%`), 미실행 분모 제외 · **SLO-07** `quarantine_dwell_times·median·median_days` — 체류 시간(종료−진입, 중앙값·일, `≤3d`), 진행 중/비정방향 제외, 빈 입력 None(부재가 OK 아님) · **SLO-08** `reprocess_benchmark` — full rebuild 벽시계 공개(10 §1.4·recompute_bench 재사용), 목표가 "벤치마크 공개" 라 임계 게이트 없이 공개 + `measured`(full_ms 부재 → False). 판정 `evaluate_slo05/06/07` — ok / `slo-gate`(10 §1.4 CI 비차단 nightly) / `not-measured`. read-only·결정적·mock/실측 격리. **스키마·계약 변경 없음 → Spec 1.0.0 유지.** TDD — `test_slo_metrics_harness` 신규 25개 — 스위트 **923→948개 통과**(회귀 0). §3 Phase 6 SLO-05/06/07/08 행·design 11 §2.3 반영. 다음: 실데이터 축적 시 목표치 확정 또는 #4·9 measured 전환 / tool schema 확정.
- **Phase 6 — SLO-01 실측 하니스 봉인 (design 11 §2.3, 첫 실측 작업).** `reflection_slo_harness.py` — deferred SLO-01(신규 문서 → graph 반영 지연 p95, 목표 `≤ 30 min`)의 **측정 계약·하니스** 봉인 (목표치 자체는 실데이터 축적 후 확정). 측정 공식(10 §1.4) `graph_commit_ts − fetched_ts`(p95) — `fetched_ts` = 원문 meta `fetched_at`, `graph_commit_ts` = 그래프 반영 시각. **단일 프로세스 일괄 처리의 정직한 한계(honest-gap §6.2)로 측정을 두 축으로 분리:** `measure_batch_reflection`(배치 내 실제 벽시계, 주입 clock·commit_fn epoch ms 축) + `measure_batch_interval_latency`(배치 간격 시나리오 — 운영 스케줄러 `ceil(t/inter)*inter − t`). p95 는 `neo4j_q4_harness` 와 동일한 **정렬 인덱스 결정법** 재사용. 판정 `evaluate_slo01` — `SLO01_TARGET_MS=30 min` 대비, 미측정 None → `not-measured`+`within_slo=False`(부재가 OK 아님), 초과 → `slo-gate`(10 §1.4 CI 비차단 nightly, `compute_graph_slo`(60s/DoD ②)와 별개 게이트). read-only·결정적·mock/실측 격리. **스키마·계약 변경 없음 → Spec 1.0.0 유지.** TDD — `test_reflection_slo_harness` 신규 21개 — 스위트 **902→923개 통과**(회귀 0). §3 Phase 6 SLO-01 행·design 11 §2.3 반영. 다음: SLO-05/06/07/08 실측 또는 #4·9 measured 전환.
- **Phase 6(Stable 운용) 진입 준비 — 측정 기반 운영 단계 신규 정의.** blueprint §16 은 Phase 5 까지만 정의 — Phase 6 는 이 프로젝트가 새로 정의하는 진행 단계이다. Phase 1–5 가 prototype 계약 봉인(단위 테스트)이었다면 **Phase 6 는 Stable(Spec 1.0.0) 위에서 deferred 항목 실측·measured 전환·지속 수집 운용** 을 수행 (honest-gap §6.2 의 "실측 백엔드 재측정 대상" 의무 이행). **이전 Phase 와 동질성이 다름: 단위 테스트 추가가 아니라 실측·운영 작업** — 게이트·SLO 판정은 CI 비차단 nightly(10 §1.4). 작업 후보: 수집 지속 운용 · **deferred SLO-01/05/06/07/08 실측 확정**(11 §2.3, 각각 신규 문서→graph 반영 지연·수집 성공률·schema 통과율·quarantine 체류·재처리 벤치마크) · **tool JSON schema 확정**(07 §7, 실제 추출·판정 골든으로) · **#4·9 measured 전환**(contradiction·lineage 실데이터 재수집 / 1M 실측) · Watchtower 운영 경보. **DoD:** ① measured=False·(deferred) 항목 중 실측 가능한 것의 measured 전환·SLO 확정 ② 지속 수집이 신호·SLO 에 반영되는 운용 루프 가동. §1·§3(Phase 6) 반영. 다음: Phase 6 우선 작업 선정.
- **MVP 최종 성공 기준(§4) Stable 스펙 기준 3중 매핑 재검증.** 설계 문서가 Spec 1.0.0 으로 확정됨에 따라 §4 의 Blueprint §21 기준 10개 전항을 **설계 절 ↔ 구현 모듈 ↔ suite(902 Green) 3중 대조**로 재검증 — Stable 승격 전 "MVP 10/10 충족"이 전부 실측을 뜻하지 않음을 정직히 재확정 (과대 주장 금지 §6.2). **측정 상태 구분 재확정:** 실측 7항(#1 raw 104,544건·#2 재처리·#3 source span+버전·#6 time-travel·#7 AB 반증 1.0·#8 연결률 1.0·#10 버전 회귀) + **부분실측 2항**(#4 ER/Claim — claim F1=1.00·ER P=1.00 실측이나 **contradiction·lineage 실데이터 골든 미자연발생 → honest-gap 미측정** · #9 비용·시간 — 단일 프로세스 실측 13.9 docs/s, **1M 스케일은 모델 투영 measured=False**) + **계약 봉인 1항**(#5 중복 방지 — dedup+lineage 골든 P/R 경로 검증, 실데이터 dup 0건 축 honest-gap). 전항 충족이되 **#4·5·9 는 계약/투영 경로로 부분 정직 표기** 유지 — 실데이터 재수집·1M 실측 시 measured 전환 가능(단위 테스트 봉인). §1·§4 반영. 다음: Phase 6(Stable 운용) 진입 준비 또는 유지·관리 작업.
- **설계 문서 Review → Stable 확정 (11개, Spec 1.0.0).** 리뷰 패스(BLOCKER 3 + MAJOR 36 해소·상호 일관성 재검증 전항목 PASS) 완료 문서를 **Stable 로 승격** (README 인덱스·01~11 전부, `상태: ✅ Stable · Spec: 1.0.0`). **Stable 확정 전 TBD 잔존 검증으로 미확정 항목을 해소 후 승격** (과대 주장 없음): **SLO-02/03/04(graph query p50/p95/p99)를 실측으로 확정** — 06 §9 실측(p95 0.66~1.16ms·10만 합성 1.08ms·5만 0.6ms) 대비 **대략 40~100배 여유**의 보수적 목표 `p50≤10ms·p95≤50ms·p99≤100ms` (11 §2.3). **SLO-01/05/06/07/08**(수집 성공률·schema 통과율·quarantine 체류·재처리)과 **07 §7 tool JSON schema 본문**은 "측정 없는 목표는 신뢰하지 않는다"(blueprint §20) 원칙에 따라 **명시적 deferred**로 유지 — 실측 후 확정 대상(11 §2.3 `(deferred)`·07 §7). **Spec 0.1.9 → 1.0.0** (Stable 확정). 스키마·계약 실질 변경은 없음 — SLO 목표 확정·deferred 명시만. ROADMAP §1·§2·§5 반영. 다음: MVP 최종 성공 기준(§4)의 Phase 0~5 매핑 재검증·Phase 6(Stable 운용) 진입 준비 또는 유지·관리 작업.
- **Phase 5 완결 블록업 (6 작업, 스위트 902 Green).** Phase 5(1,000만 문서 Challenge) 전 작업 구현·회귀 봉인 완료. 작업별 상태: 지속 Signal Spire 알림(`signal_spire`, 11 §4 — 5 트리거·fire-once·alert 스키마) · signal source adaptive scheduling(`signal_scheduler`, 04·01 — floor 보장+수율 비례, freshness) · hot/cold graph 분리(`graph_temperature`, 06 — 접근 온도·Q4 노드 게이트·SLO 라우팅) · impact graph 부분 재계산(`impact_graph`, 06·10 — 전파 BFS 범위·절감 게이트) · 다국어 ER(`multilingual_er`, 05·ADR-507 — 스크립트 탐지·NFKC·bridge·항상 POSSIBLY) · ontology migration 자동화(`ontology_migration`, 02·§6 — 버전 분류·호환·proposal·backfill). **honest-gap §6.2:** 1,000만 문서 실제 처리·실측 부하는 수행되지 않았으며 — 전 작업이 검증 가능한 **엔지니어링 계약(`measured=False`)** 을 봉인, 10M 부하 판정 게이트(분산·재구축·아카이브·마이그레이션)는 실측 백엔드 재측정 대상. **Spec 그대로(0.1.9)** — Phase 5 전 작업 read-only·결정적·mock/실측 격리. §1 현재 단계·§3 Phase 5 완결 마킹. 다음: 설계 문서 Review → Stable 확정 대기.
- **Phase 5 — ontology migration 자동화 前단 (design 02).** `ontology_migration.py` — 02 §6.1·§6.2·§6.3 버저닝·거버넌스 절차의 **계획·판정 전단** 봉인(실제 발행은 이벤트 replay 재구축·`GraphService` Applier(06 §7.3) 경로 위임, read-only §3-3). `migration_level` — §6.1 semver 분류(결정적, 비정상 구성요소 0 폴백) · `compat_current` — §6.3 `ontology_version` 호환 판정(**major 만 비호환** → 전량 재평가) · `plan_migration` — element 집합 대비 `MigrationPlan`(level·`revalidate_required`·`backfill_ids`(非호환, 정렬)·`proposal_predicates`(미등록, unique·정렬)), **`prev==cur` → `unchanged` 명시(honest-gap §6.2)** · `quarantine_trigger` — §4-2 predicate 폐쇄성, 미등록 → `quarantine_and_propose`(자동 승격 금지; 신규 수집 게이트는 `gate.py` 몫) · `migration_actions` — §6.2 proposal→review→promotion→backfill 서열 · `backfill_events` — 06 §7.3 非호환 element 재해석 `reinterpret` 이벤트 계획. **빈/미지 버전은 보수적 非호환**(자동 호환 오인 대신 재해석 대상). 결정적·read-only·mock/실측 격리. **Spec 그대로(0.1.9).** TDD — `test_ontology_migration` 30개(분류·호환성·계획·honest-gap·proposal 트리거·절차 서열·backfill·보수적 비호환·read-only·결정성) — 스위트 872→**902개 통과**(회귀 0). Phase 5 목록 **전항 완료**. 다음: design 02 Review → Stable 확정 대기.
- **Phase 5 — 다국어 ER (design 05).** `multilingual_er.py` — 같은 개체(기관)가 여러 언어·스크립트로 등장(TSMC ∽ 台積電, §1.2 `aliases`)할 때의 **교차-스크립트 해소 전단부** 봉인 (ADR-507 precision-first). `detect_script` — codepoint 다수결 지배 스크립트(latn/cjk/other, 결정적) · `norm_name` — **NFKC + 조합 발음구분부호 제거 + 로마자 소문자/공백/구두점 축약·CJK 공백 제거** → §2.1 `norm_name` blocking 의 교차-스크립트 기반 · `multilingual_alias_index` — caller-curated bridge(TSMC↔台積電)를 norm→entity 인덱싱 · `multilingual_block_keys` — bridge 를 타고 교차-스크립트로 같은 개체를 같은 bucket 으로 유도 · `propose_multilingual_candidates` — 스크립트 다른 개체를 **`POSSIBLY_SAME_AS`(kind=multilingual-script)** 로 제안 · `multilingual_merge_status` — **교차-스크립트는 항상 POSSIBLY**(자동 병합 금지). **honest-gap(§6.2):** bridge 없는 표면형은 교차 후보를 **추론하지 않음**(transliteration 은 모호·비결정적). read-only·결정적. **Spec 그대로(0.1.9).** TDD — `test_multilingual_er` 30개(스크립트·정규화·bridge·blocking 유도·제안·항상 POSSIBLY·honest-gap·read-only·결정성) — 스위트 842→**872개 통과**(회귀 0). 다음: Phase 5 는 ontology migration 자동화(02)(완료 — 902).
- **Phase 5 — impact graph 부분 재계산 (design 06·10).** `impact_graph.py` — 06 §7.2·§5.2 **영향 하류 subgraph 도출** 봉인. **프로파게이션 모델:** 전파 엣지(`ABOUT/SUPPORTS/CONTRADICTS/SUPERSEDES/SAME_AS`) 연결 클러스터 **상호 영향**(양방향 — §5.2 재지정 파급), hops·타입 한정. `impact_scope` — target→전파 BFS 영향 노드 집합(결정·read-only) · `impact_subgraph` — scope 노드 + **내부 엣지만**(부분 재계산 섬) · `partial_recompute_events` — full 중 scope 내 이벤트만(원순서 보존) · `partial_vs_full` — 이벤트 수 ratio, **partial=0 → ratio None + empty_scope**(honest-gap §6.2) · `evaluate_partial_recompute` — ratio > `PARTIAL_RATIO_GATE=5.0` → `saving_meaningful` + `slo-gate`(10 §1.4 CI 비차단 nightly). 기존 `recompute_bench`(벽시계)·`graph_incremental`(실재적용) 과 결합하는 **범위 도출층**. **Spec 그대로(0.1.9).** TDD — `test_impact_graph` 29개(범위 BFS·hops·타입 필터·subgraph 섬·부분집합·비율·empty-scope·게이트·read-only·결정성) — 스위트 813→**842개 통과**(회귀 0). 다음: Phase 5 는 다국어 ER(05).
- **Phase 5 — hot/cold graph 분리 (design 06·01).** `graph_temperature.py` — 10M Challenge 그래프 확장의 **접근 온도 기반 계층 분리** 봉인 (06 ADR-603 — 논리→물리 분리, Q4 게이트 — 노드 ≥ `NODE_GATE=1e6` 시 분리/아카이브). `temperature` — 최근성(`now-last_access ≤ hot_window`) → hot/cold, **미접근(None)→None**(미측정, honest-gap §6.2). `partition_tiers`·`tier_map` — hot/cold/unknown 분할 + unknown **보수적 기본(cold) 할당**(측정과 구분). `hot_resident_count` — hot 예산. `cold_archive_decision` — Q4 노드 게이트 초과 시 cold 아카이브(`node_count - hot_resident` 상주 보존)·`cold_ratio`, `route_query` — hot 상주/cold 아카이브 라우팅 + 조회 SLO(`DEFAULT_SLO_MS=50ms` 초과 `slo-gate` 비차단, ✓10 §1.4), tier None → `not-measured`. `mark_accessed` — 승격 read-only(새 dict 반환), 실제 아카이브 이동·접근 기록은 호출자 몫(§3-3). 접근 stats·지연은 호출자 주입(mock/실측 격리). **Spec 그대로(0.1.9).** TDD — `test_graph_temperature` 28개(온도·분할·unknown 할당·hot 예산·아카이브 게이트·라우팅+SLO·승격 read-only·결정성) — 스위트 785→**813개 통과**(회귀 0). 다음: Phase 5 는 impact graph 부분 재계산(06·10).
- **Phase 5 — signal source adaptive scheduling (design 04·01).** `signal_scheduler.py` — 10M Challenge 수집 병목, **신호 수율 기반 예산 배분** 봉인 (근거 = `signal_source_runner` 메모리: arXiv 대비 전용 반도체/공급망 언론 고신호). `signal_density=(claims+edges)/docs`·`trailing_signal_density`(문서 가중 누적) — **수율 지표**. `allocate_signal_budget` — **floor(min_docs=1) 피보장**(04 §5 freshness, 멸종 방지) + 잉여 수율 비례 배분, 예산 희소 시 floor 비례 축소(전체 보존), **미측정은 잉여 제외/전 미측정은 균등**(honest-gap §6.2). `freshness_lag` — §5 freshness(lag·overdue, 이력/기대 주기 부재는 None). `cadence_priority` — 04 §1.1 `schedule.priority`(density → high/normal/low, 임계 placeholder). `adaptive_schedule` — 히스토리 → trailing 수율 → 배분 + priority·lag 부착 결정적 오케스트레이션. **스케줄은 산출물일 뿐 실행은 호출자 몫(read-only §3-3).** **Spec 그대로(0.1.9).** TDD — `test_signal_scheduler` 38개(수율·배분 floor/비례/희소/미측정·freshness·주기·오케스트레이션·read-only·결정성) — 스위트 747→**785개 통과**(회귀 0). 다음: Phase 5 는 hot/cold graph 분리(06).
- **Phase 5 착수 — Signal Spire 알림 (design 11 §4).** `signal_spire.py` — §4 트리거·fire-once·alert 스키마 봉인 (read-only·결정적). **5 종 트리거(§4.1):** `trigger_contradicting_evidence`(CONTRADICTS)·`trigger_claim_changed`(SUPERSEDES)·`trigger_plan_to_execution`(Event.status planned→confirmed)·`trigger_new_independent_source`(독립 수 증가, §1.4)·`trigger_confidence_threshold`(Δ≥0.10). **1 회 점화(§4.2, ADR-1104):** `SignalSpire` 가 점화 dedup_key 기억 → 동일 `inv:trigger:target` 재점화 `None`(과잉 알림 금지). **Alert 스키마(§4.3):** `make_alert` — investigation_id(technical-first, campaign_id 미사용)·severity·dedup_key 자동·delta·cause(provenance gate 근거만)·fire_count·acknowledged. `evaluate_and_fire` 일괄 래퍼. **알림은 산출물일 뿐 저장·발송은 호출자 몫(read-only §3-3).** **Spec 그대로(0.1.9).** TDD — `test_signal_spire` 28개(5 트리거·정밀성·fire-once·스키마·일괄·read-only·결정성) — 스위트 719→**747개 통과**(회귀 0). Phase 5(1,000만 Challenge) 진입 — 다음: signal source adaptive scheduling(04).
- **Phase 4 완결 — DoD ①② 통합 검증 (design 01·06·10).** `test_phase4_dod.py` — #14–#18 이 봉인한 엔진을 **실제 구동**해 DoD ①② 종합 검증 (read-only·결정적·mock/실측 격리). **DoD ① — 100만 처리 시간·비용 공개:** `project_time_to_scale`·`project_cost_to_scale`(1M 투영, 병렬·**Message Batches 50% 절감**) → `phase4_dod_report` 공개, 결정적 모델 투영이 실측이 아니므로 **정직히 measured=False**(honest-gap §6.2), SLO 위반 `slo-gate` 비차단(§1.4·§6.3). **DoD ② — 신규 문서 SLO 내 graph 반영:** 실제 `GraphService` 로 `extract_events`·`apply_incremental` 구동 → **incremental 재적용 == full replay 정합성**(불변식 §3-1)·base 불변(read-only §3-3)·증분 **ratio > 10×** 완료조건·`compute_graph_slo`(p95 ≤ 60s) `ok` — DoD ② 종합. 결정성(동일 벤치). **Spec 그대로(0.1.9).** TDD — `test_phase4_dod` 12개 — 스위트 707→**719개 통과**(회귀 0). §1 현재 단계·§3 Phase 4 완결 마킹.
- **Phase 4 — 100만 처리 시간·비용 공개 + SLO graph 반영 (design 01·10, DoD ①②).** `phase4_report.py` — #14–#17 이 봉인한 계약을 **DoD ①② 측에서 통합 공개** (중복 구현 없음, 10 §4.5·§1.4 → 01·06). **DoD ① — 100만 처리 시간·비용:** `project_time_to_scale(per_doc_ms, docs, target=1M, speedup)` 선형×병렬(#14) 투영 · `project_cost_to_scale(cost_per_doc, target=1M, use_batch)` 순차 vs `batch_inference_cost`(**Message Batches 50% 절감**, #17 재사용) — **결정적 모델 투영**이 실측이 아니므로 `measured=False` 명시(honest-gap §6.2). **DoD ② — SLO 내 graph 반영:** `graph_reflect_slo` 가 `compute_graph_slo`(#15 p95≤60s)·`compute_slo_gate`(#14)·증분 완료조건(ratio>10×) 통합 판정(분류 재노출) · `phase4_dod_report` `all_measured` 로 부분 미측정 정직 노출. read-only·결정적. **Spec 그대로(0.1.9).** TDD — `test_phase4_report` 18개 — 스위트 689→**707개 통과**(회귀 0). 다음: Phase 4 DoD ①② 통합 검증 + 완결 블록업.
- **Phase 4 — ClickHouse 분석 승격 (design 01·11, 확장 게이트).** `analytics_promotion.py` — 01 §5 분석·관측 계층의 **승격 트리거(분석 쿼리 지연)** 봉인 (ClickHouse 미설치 — executor mock 주입, #14 mock/실측 격리와 동일). `measure_analytics_latency(queries, executor)` — 분석 쿼리 경로별 지연 분포 → p95(정렬 인덱스 결정법)·avg·max·n_queries. `evaluate_analytics_promotion(latency_stats)` — `ANALYTICS_SLO_MS=200ms` p95 초과 시 **`escalate_clickhouse=True`** (01 §5 승격 트리거, Q4/Q6 게이트와 동일 성격 — `classified="slo-gate"` CI 비차단 nightly 승격 평가). 미측정(None/p95 부재) → escalate=False·**not-measured** (honest-gap §6.2 — 미측정이 승격 불필요의 근거 아님). `aggregate_metrics(rows, key_fn)` — **OLAP 집계**(ClickHouse 가 대체 승격하는 분석 부하 실제 형태), `correlation_id`·`version_tuple` drill-down(11 §2.2). read-only(불변식 §3-3)·결정적. **Spec 그대로(0.1.9).** TDD — `test_analytics_promotion` 19개 — 스위트 670→**689개 통과**(회귀 0). 다음: 100만 처리 시간·비용 공개 + SLO graph 반영(01·10, DoD ①②).
- **Phase 4 — 대량 embedding·LLM batch inference (design 07·08, DoD ①).** `batch_inference.py` — 07·08 inference 단계 확장 지점 봉인 (mock/실측 격리). **embedding (08 §2.3·ADR-802/809):** 핀 `BAAI/bge-m3`(1024-dim cosine, 08 소유) · `embeddable_kind` segment/claim 만(ADR-802 문서·evidence 비임베딩) · `embedding_index_version` model:dim 축약(08 §2.1 전량 reindex 트리거) · `embed_texts` 대량 일괄 임베딩(executor 주입, 결과에 `embedding_model`·`index_version` 부착). **LLM batch (07 §2.2):** `BATCH_TIER=claude-sonnet-5` · `batch_infer` Message Batches 일괄(미주입 mock 은 항목 `status:pending` 표시 — honest-gap §6.2 진공 통과 금지, `batch:True` 경로 명시) · `batch_inference_cost` 순차 대비 `MESSAGE_BATCHES_DISCOUNT(0.5)` — **50% 절감**(비-latency-민감). **DoD ① 처리 시간·비용:** `inference_throughput`(10 §1.4 재사용) + `compute_inference_slo`(`INFERENCE_SLO_MS=60s` **slo-gate**·CI 비차단, 미측정 honest-gap). read-only(불변식 §3-3)·결정적. **Spec 그대로(0.1.9).** TDD — `test_batch_inference` 26개 — 스위트 644→**670개 통과**(회귀 0). 다음: ClickHouse 분석 승격(design 11, 확장 게이트).
- **Phase 4 — 증분 graph update (design 06 §7.2, DoD ② SLO 내 그래프 반영).** `graph_incremental.py` — 06 §7.2 incremental rebuild 실경로 봉인 + #14 이어 재구축/증분 비율 재측정. `extract_events(graph)` — 그래프→mutation 이벤트 역직렬화(결정·read-only) · `apply_incremental(base, delta)` — base 위 delta subgraph 만 `GraphService.apply`(idempotent §3.1)로 재적용, **정합성 계약: 병합 그래프 == full replay 그래프**(불변식 §3-1) · `incremental_rebuild_bench`/`evaluate_incremental_bench` — full vs incremental 벽시계·ratio(10 §4.5, 06 §9 3항), 완료조건 **ratio > 10×** → `saving_meaningful` + slo-gate(§1.4 nightly 비차단) · `compute_graph_slo` — **DoD ② 신규 문서 SLO 내 그래프 반영**(`GRAPH_SLO_MS=60s` p95, 미측정 honest-gap §6.2). read-only·결정적·mock/실측 격리. **Spec 그대로(0.1.9).** TDD — `test_graph_incremental` 16개 — 스위트 625→**644개 통과**(회귀 0). 다음: 대량 embedding·LLM batch inference(07·08).
- **Phase 4 — 분산 batch (design 01 §6, DoD ① 처리 시간·비용 측정).** `distributed_batch.py` — 설계 01 §6 분산 batch 계약 봉인 (이 환경 Ray 미설치 — mock/실측 격리, 10 §4.5). `shard(metas, k)` — 재결정·read-only 파티셔닝 (k-파라미터, 실제 Ray Data 진입점, k>len 클램프·round-robin 밸런스) · `ParallelismBench` — 병렬 벽시계·speedup 측정 (executor mock 주입; 벽시계 모델: 노드별=`len×per_node_ms`, 병렬=`max`, 순차(k=1)=`sum` → speedup=순차/병렬) · `merge_results` — 노드별 파이프라인 결과 합산 (숫자 합산·list 접합) · `throughput_docs_per_sec`·`compute_slo_gate` — MVP #9(10 §1.4 throughput) 계약 재사용. **DoD ① 처리 시간·비용 측정** 계약 봉인 — throughput·문서당 비용(MVP #9) + SLO 게이트(`PER_NODE_SLO_MS=60s`, **slo-gate**·CI 비차단, 10 §1.4·§6.3). read-only(불변식 §3-3)·결정적. **Spec 그대로(0.1.9).** TDD — `test_distributed_batch` 신규 23개(shard 분할·결정성·클램프·벽시계 모델·speedup·merge 접합·SLO 게이트·read-only) — 스위트 605→**625개 통과**(회귀 0). 다음: 증분 graph update 성능(06 §7.2) + 재구축/증분 비율 재측정(design 10 §4.5 이어서).
- **Phase 4 착수 — full rebuild vs partial recomputation benchmark (design 10 §4.5).** Phase 4(100만 문서 확장) 첫 증분 — **완료조건** "증분이 full 대비 유의미 절감". `recompute_bench.py` — `full_vs_incremental(store·base·delta)` → `{full_ms, incremental_ms, ratio}`(ratio=full/incr, 06 §9 3항) · `neo4j_rebuild_bench`(가동 Neo4j 실측 adapter, `measure_rebuild` 재사용) · `evaluate_recompute_bench` — 완료조건 게이트 **ratio > 10×** → `saving_meaningful` + **slo-gate** 분류(10 §1.4 — 성능 nightly 경보·CI 비차단). read-only·결정적·store mock/실측 격리. **Spec 그대로(0.1.9).** TDD — `test_recompute_bench` 7개 — 스위트 598→**605개 통과**(회귀 0). 다음: 분산 batch(01 §6)로 DoD ① 처리 시간·비용 측정.
- **Phase 3 완결 블록업 (5 작업 + DoD ①②, 스위트 598 Green).** Phase 3(Research Agent) 전 작업 구현·회귀 봉인 완료. 작업별 상태: 조사 품질 평가 하네스(`investigation_quality`, §12.3 — 전 지표 채점·DoD ② 증명 도구) · Investigation Planner(`planner`, §3.2 질문→subclaim 트리·known/gap) · 조사 budget·종료 + LLM routing(`investigation_budget`, §4.3 STOP=(A∧B∧C)∨D·§2.3/ADR-706 승급 게이트) · Synthesis·Audit 역추적 chain(`synthesis::Audit.trace`, §3.8/§3.9 문장→claim→extraction_record source span, 연결률=1.0) · 비용·evidence coverage 대시보드(`investigation_dashboard`, design 11 §2.2 D8). **DoD ① "그래프 공백 탐색·신규 evidence 추가"** — `test_investigation_dod`(조사가 그래프 evidence 탐색, coverage ≥ 0.80 + Audit 역추적) · **DoD ② "반증 탐색 제거 대비 평가 점수 향상"** — AB 검증: baseline(반증 제거, 반증 recall 0.0) 대비 counter-evidence 버전 **반증 발견률 1.0(≥0.70 PASS)로 향상**·연결률 1.0 유지·골든 부재 축 honest-gap(§6.2). `viewer._api_investigate`가 Planner 트리·audit_trace·D8 dashboard 일괄 노출(조사 E2E 응답 확장). **Spec 그대로(0.1.9)** — Phase 3 전 작업이 read-only·결정적 원칙을 유지하며 스키마·계약 변경 없음. §1 현재 단계·§3 Phase 3 완결 마킹. Phase 3 종료 상태 확정, Phase 4(100만 문서 확장) 진입 준비.
- **Phase 3 — Investigation 대시보드 (design 11 §2.2 D8).** `investigation_dashboard.py` — D8(Council Chamber) 지표 계산. `investigation_dashboard(investigation_id, Coverage, independent_evidence, budget, elapsed_ms)` → investigation별 **evidence coverage**(covered/planned + gap — planned 0 honest-gap §6.2)·**독립 증거 수**(11 §1.4 dup 보정)·**cost**(token/step)·**latency_ms**(10 §1.4). read-only·결정적·investigation_id 분해. `viewer`에 `dashboard` D8 노출. **Spec 그대로(0.1.9).** TDD — `test_investigation_dashboard` 5개 + `test_viewer_graph` 1개 — 스위트 588→**594개 통과**(회귀 0). 다음: Phase 3 DoD ①② 통합 검증 + 완결 블록업.
- **Phase 3 — Synthesis·Audit 증거 역추적 chain (design 07 §3.8·§3.9).** `synthesis.py::Audit.trace` — 검증가능(fact/asserted) 문장이 `claim_ref` → `extraction_record`(03 §8, ADR-305 element_id=claim_id)의 **source span(segment_id·char_start·char_end·doc_id)까지 역추적**되는지 대조 (§3.9). 출력 `{trace, blocked_statements, verifiable, linked, verified_statements, linkage_ratio}` — prediction/opinion 무출처 허용(연결률 분모 제외, 10 §1.3), 역추적 불가 → verified=False·blocked(무출처·미역추적 차단). `verify_from_trace` — blocked 존재 시 passed=False(**연결률 = 1.0** 불변식 §3-5 통과 판정). `Synthesizer`가 §3.9 trace 기반 audit 사용, `viewer`에 `audit_trace`(linkage_ratio) 노출. **read-only·결정적.** **Spec 그대로(0.1.9).** TDD — `test_audit_trace` 7개 + `test_synthesis` ADR-305 계약 반영 + `test_viewer_graph` 1개 — 스위트 580→**588개 통과**(회귀 0). 다음: 조사 비용·evidence coverage 대시보드(11).
- **Phase 3 — 조사 budget·종료 조건 + LLM routing (design 07 §2.2·§2.3·§4.3, ADR-706).** `investigation_budget.py` — §4.3 조합 종료 + §2.3 routing 구현 가능화 (placeholder 초기 기본값). `InvestigationBudget(max_steps·max_tokens)` step/token 예산 → 소진 시 **hard stop(D)** (max_steps=0 즉시, max_tokens=0 비활성). `evaluate_stop` — **STOP=(A∧B∧C)∨D**: A coverage ≥ 0.9 · B 신규 독립 증거율 < ε 0.05 · C 미해결 모순 = 0 · D budget 항상 hard stop (δ 0.02 수렴은 보조). `route_llm` — §2.2 task별 L0..L5 + **승급 게이트**(결과 confidence < τ 0.75/0.70/0.65 ∧ 예산 잔량 > cost(next) 시 1단계 승급, ADR-706) · `expected_info_gain`(=Δcoverage×var(conf))·`call_cost`(=(in+out)×단가). `InvestigationRunner`가 예산+`evaluate_stop` 배선(terminated_by 기존 어휘 유지). **read-only·결정적.** **Spec 그대로(0.1.9).** TDD — `test_investigation_budget` 16개 + `test_investigation_runner` 1개 — 스위트 563→**580개 통과**(회귀 0). 다음: Synthesis·Audit 증거 역추적 chain(07 §3.8/§3.9, 연결률 = 1.0).
- **Phase 3 — Investigation Planner (design 07 §3.2).** 질문→subclaim 트리 분해 + **기존 지식/공백 구분** (결정적 규칙). `planner.py` — `InvestigationPlanner.plan(question, scope?)` → `Plan{subclaims: [PlannedSubclaim(id·text·required_evidence_types·**known·gap_reason**·subject_id)], tool_calls[]}` (07 §3.2 strict JSON). **known/gap 판정**: 질문 subject(대문자 약어·질문 자체가 기존 subject)가 zone assertion 존재 시 known(기존 지식), 부재 시 gap_reason(insufficient_evidence — 07 §3.1 비목표: 그래프에 없는 것을 사전 지식으로 추가 금지·공백으로 유지). tool_calls 결정적 `graph:read`. `viewer._api_investigate`가 하드코딩 단일 subclaim 대신 Planner 트리 사용, 응답 `planned_subclaims`(known/gap) 노출 (07 §4 PLAN·§3.2). **read-only**(불변식 §3-3)·결정적. **Spec 그대로(0.1.9).** TDD — `test_planner` 신규 7개 + `test_viewer_graph` 신규 1개 — 스위트 555→**563개 통과**(회귀 0). 다음: 조사 budget·종료 조건 + LLM routing(07 §2.3·§4.3).
- **Phase 3 착수 — 조사 품질 평가 하네스 (design 10 §1.3·§12.3).** Phase 3(Research Agent) 첫 증분 — **DoD ② 증명 도구**. `investigation_quality.py` — golden question(§2.1 조사 질문) 대비 조사 품질 **전 지표** 채점 (`GoldenQuestion`·`InvestigationQualityHarness`·`QualityMetric`). 축: 하위질문 coverage(covered/planned, gate ≥ 0.80) · 인용 연결률(linked/verifiable, **gate = 1.0** — prediction 무출처 허용, evidence-first §3-5) · 인용 지지율(judge verdict 주입, gate ≥ 0.95 — §1.3 LLM-as-judge 보정 경로) · 독립증거 수 정확성(1 − |r−g|/max(g,1), **gold=None → honest-gap**, gold=0 과대평가 방지, target ≥ 0.90) · **반증 발견률(found/total_gold_counter, target ≥ 0.70 — DoD ② 직접 지표)** · modality 정확도(gate ≥ 0.85) · confidence 부호 일치율(지지 추가→상승·반증 추가→하락, 방향 우선·크기 부차, target ≥ 0.85). 모든 계산 순수 함수·결정적, LLM judge 점수는 호출자 주입(하네스 대조만). **read-only**(불변식 §3-3)·**honest-gap**(§6.2 — 골든 부재 축 measured=False, vacuous pass 금지). **스키마·계약 변경 없음 → Spec 그대로(0.1.9).** TDD — `test_investigation_quality` 신규 20개 — 스위트 535→**555개 통과**(회귀 0). 다음: Planner(07 §3.2)로 골든 question 실측·DoD ② AB 검증.
- **Phase 2 완결 블록업 (6 작업 + DoD ①②, 스위트 535 Green).** Phase 2(Entity·Claim 품질) 전 작업 구현·회귀 봉인 완료. 작업별 상태: 후보 blocking+ER(결정적 ER ADR-507 + entity pair 골든 평가·오병합률≤0.02) · Claim Canonicalization(F1=1.00 실측, MVP #4) · 출처 계보(dedup_clusters + lineage 골든 dup P/R) · contradiction candidate(ConflictCandidate + 골든 측정 전환) · quarantine·review(derive_golden 파생, ADR-506) · 골든 데이터셋(claim·entity pair·lineage 골든 확장). **DoD ① "핵심 품질 지표 자동 계산"** — `metrics_report` 전 축 자동(골든 존재 시 measured, 부재 시 vacuous pass 없이 honest-gap §6.2) · **DoD ② "entity merge 감사·rollback"** — `merge_audit`·`audit_rollback`(06 §5.3). 실데이터 측정: claim F1=1.00·ER P=1.00 오병합률=0.0000 PASS, contradiction·lineage 는 실데이터 골든 미자연발생 → 정직 미측정(재수집 시 measured 전환 가능, 경로 단위 테스트 봉인). §1 현재 단계·§3 Phase 2 완결·Spec 0.1.9 마킹. Phase 2 종료 상태 확정, Phase 3(Research Agent) 진입 준비.
- **Phase 2 — 계보 골든 확장 measured 전환 (design 10 §1.1·§2.1).** 계보 골든셋(dup/independent)로 `metrics_report` **lineage 축 measured 전환** — dup P/R 실측. `golden_lineage_pairs` 테이블 + `persist_golden_lineage_pair`·`golden_lineage_pairs`(결정적 golden_id ON CONFLICT no-op, §3-7/어드-1007). `EvalHarness.lineage_metrics` — 골든 두 doc이 **같은 dup_clusters 클러스터 멤버인지** 대조(design 04 §4 — `_build_cluster_membership`가 zone 계보 rows→`{doc_id: cluster_id}` 맵). dup 동클러스터=tp·오분리=fn, **independent 오축소=fp(복제 K건을 독립 K으로 세는 과대평가 방지)**. **gate Dup precision ≥ 0.98 · target R ≥ 0.90**(§1.1). `_lineage` measured·honest-gap(부재 시 §6.2). **스키마 변경 → Spec 0.1.8→0.1.9.** TDD — `test_metrics_report` 신규 4개(lineage measured·오축소 fp·오분리 fn·honest gap) — 스위트 531→**535개 통과**(회귀 0). 실데이터 dup_clusters 0건 → lineage 실제 데이터선 honest-gap 유지.
- **Phase 2 — review→골든 파생 경로 (design 05 §8.2·ADR-506, 불변식 §3-7).** `review.py`에 `derive_golden(rq, zone, gold_version, labeled_by, split)` — human review 결정을 zone 의 골든 claim pair(`persist_golden_pair`)·entity pair(`persist_golden_entity_pair`)로 파생. corrected/approved 만 파생(pending/in_review 는 골든 원천 아님 — 05 §8.2), 인간 교정본(`corrected_value`)의 `verdict`를 골든 라벨·`claim_b`/`entity_a`/`entity_b`로 쌍 추출(불변식 §3-7 원출력+수정+이유 보존). human review 가 metrics_report/DoD ① 의 골든셋으로 이어지는 경로 완결 — quarantine·review workflow → 골든셋 파생. 결정적 멱등(ON CONFLICT no-op, 03 §5)·ADR-1007 split. **Spec 0.1.7→0.1.8.** TDD — `test_review_golden` 신규 5개(claim 파생·entity 파생·미결정 skip·멱등·결정적) — 스위트 526→**531개 통과**(회귀 0).
- **Phase 2 — DoD ② merge 감사·rollback (design 06 §5.3, ADR-605).** Phase 2 DoD ② "entity merge 감사·rollback 가능" 충족. `GraphService.merge_audit(entity_id)` — read-only 병합 이력 감사 조회(canonical·resolution_ref·decided_by·merged, 불변식 §3-3) · `audit_rollback(entity_id, resolution_ref)` — 감사로 찾은 병합을 해당 resolution 의 `unmerge` 역연산으로 revert(06 §5.3 reversible·ADR-605, 실패/미매칭 no-op False · 가역·멱등 §3-6). merge/unmerge 가 이미 graph_service 에 있던 것을 명시적 감사·복구 API 로 봉인. **Spec 0.1.6→0.1.7.** TDD — `test_graph_audit` 신규 6개(감사 노출·미병합·rollback 복구·멱등·미매칭 no-op·read-only) — 스위트 520→**526개 통과**(회귀 0).
- **Phase 2 — ER 골든 확장 measured 전환 (design 10 §1.2·§2.1, ADR-1001).** entity pair 골든세트로 `metrics_report` **entity_resolution 축 measured 전환** — ER P/R·**오병합률(≤0.02, precision-first)** 실측. `golden_entity_pairs` 테이블 + `persist_golden_entity_pair`·`golden_entity_pairs`(same/not_same/uncertain, 결정적 golden_id ON CONFLICT no-op, §3-7/어드-1007). `EvalHarness.entity_resolution_metrics` — 골든 두 entity_key가 **같은 canonical entity로 병합**됐는지 대조(ADR-507: 공유 외부식별자 exact match만 병합, `build_entity_merge`가 zone 해소 entity rows→`entity_key→entity_id` 맵 재구성). same 병합=tp·오분리=fn, **not_same 오병합=fp(그래프 전역 오염 — hard block)**, uncertain=병합 가정 판정 안 함(POSSIBLY_SAME_AS 유지, 게이트 제외 — vacuous pass 금지 §6.2). gate P ≥ 0.97 ∧ 오병합률 ≤ 0.02, `format()` 오병합률 노출. **스키마 변경 → Spec 0.1.5→0.1.6.** TDD — `test_metrics_report` 신규 3개(ER measured·오병합 hard block·오분리 fn) — 스위트 517→**520개 통과**(회귀 0). 다음: contradiction·ER 골든을 실제 curated.duckdb에 채워 전 축 실측, merge 감사(DoD ②).
- **Phase 2 착수 — contradiction 골든 확장 회귀 봉인 (design 10 §1.2·§2.1).** Phase 2(BEntity·Claim 품질) 첫 증분. `metrics_report`의 **contradiction 축이 골든 contradicts 쌍 확보 시 measured=True로 전환**되는 메커니즘(MVP #4가 "Phase 2 골든 확장 후 측정"으로 명시한 경로)을 Red→Green으로 봉인 — `_contradiction()`은 골든 contradicts 쌍 존재 시 measured로 전환하도록 이미 설계돼 있었고(§6.2 pass 기준 — measured 전환은 골든 contradicts 쌍 필요, hard-negative만으로는 전환 금지), 이를 회귀로 보호. 실측 단위: 골든 contradicts + conflict 존재 → tp(measured) · conflict 부재 → fn(recall 하락→hard block) · 골든 unrelated가 conflict에 존재 → fp(precision 압박). **read-only·결정적.** **Spec 0.1.4→0.1.5.** TDD — `test_metrics_report` 신규 증분 3개 — 스위트 514→**517개 통과**(회귀 0). 다음: entity pair 골든세트(§2.1)로 ER·오병합률(≤0.02, ADR-1001) 측정.
- **Phase 1 완결 블록업 (MVP 10/10 + DoD ①② + Q4 PASS/Q6 유보).** ROADMAP 추적기가 Phase 1 완결을 반영하지 않던 것을 교차 검증 후 정리: **MVP 10/10** (§4 전항 ✅) · **Phase 1 DoD ① 재처리 가능(104,544건)**·**② source-span 보고서 E2E** 충족 · **Phase 0→1 게이트 ② (Q4) 3게이트 전항 PASS**(실신호 그래프 p95≤1.16ms·노드<1e6·재구축/증분=1.06, Memgraph 트리거 없음, [06 §9]) · **Q6 (Iceberg) Phase 4(100만) 유보**(승격 트리거=수천만 row·스키마 진화, 현재 10만 스케일 관측 미대상, [01 §120–122]). §1 상태·§3 Phase 1 완결·Spec 0.1.4 마킹. 구현·실행 증거 검증 후에만 기록 — 과대 주장 없음. 이로써 Phase 1 종료 상태 확정, Phase 2(Entity·Claim 품질 — 골든 확장·오병합감사) 진입 준비.

### 2026-08-12
- **Retrieval SEARCH 단계 — 조사 에이전트 공백 탐색 완결 (design 07 §3.4·§4, MVP #7).** MVP 최종 성공 기준 #7 충족. S43–47 read-only 조사 루프(공백·반대 증거 탐색)에** 누락됐던 SEARCH 단계**를 구축 — `retrieval.py` Retrieval Agent(07 §3.4). corpus=멘션 `context_window`(문장 텍스트), BM25 근사 선형 스코어로 그래프 공백을 채울 후보 span(`{doc_id, segment_id, span, score, retrieval_path}`) 반환 — 전체 문서 아닌 span만(§3.4 불변식), k 상한·결정적(08 BM25 경로). `InvestigationRunner`(S46)의 gap subclaim에 SEARCH 배선 → `viewer._api_investigate` 응답에 `retrieved` 노출 — read-only 루프로 공백 탐색·후보 증거 노출 완결. **read-only·결정적.** **Spec 0.1.3→0.1.4.** TDD — `test_retrieval` 신규 5개 + runner SEARCH 배선 1개 + viewer 노출 1개 — 스위트 507→**514개 통과**(회귀 0).
- **ER·Claim Extraction 평가 수치 공개 (design 10 §1.2·§2.1, MVP #4).** MVP 최종 성공 기준 #4 충족. `metrics_report.generate_metrics_report(zone)` — 실데이터 골든셋 대비 KG 품질 지표를 검증 가능한 리포트로 공개. 골든 존재 시에만 해당 축을 `measured=True`로 계산(§1.2 gate), 골든이 없는 축은 **vacuous pass 없이 `measured=False` 미측정**(honest gap — §6.2: pass는 골든 존재 시에만 판정). **실측 (curated.duckdb, 골든 22건)**: claim extraction(canonicalization) **F1=1.00 P=1.00 R=1.00** (tp=22 fp=0 fn=0, gate 0.85 ✅) 공개. contradiction·entity resolution(ER)은 골든 contradicts / entity pair 골든세트(§2.1) 미확보로 미측정 — **Phase 2 골든 확장(§2.1) 후 측정** (과대 주장 없음). **read-only·결정적.** **Spec 0.1.2→0.1.3.** TDD — `test_metrics_report` 신규 7개(구조·claim extraction 공개·contradiction/ER honest-gap·unrelated 무오병합·read-only·결정성) — 스위트 500→**507개 통과**(회귀 0).

### 2026-08-11
- **모델·프롬프트·ontology 버전 봉인 + 회귀 (design 10 §3, 02 §6.3, ADR-1003, MVP #10).** MVP 최종 성공 기준 #10 충족. 파이프라인 산출물에 부착되는 version 5축이 `pipeline_runner` 곳곳에 하드코딩(리터럴 `"ontology_version": "1.0.0"` 등)돼 있어 **한 곳에서 봉인·통제가 안 되던** 것을, `versioning.VERSION_TUPLE`(5축 봉인 상수) + `extraction_version_tuple()`(새 dict 반환 — 호출부 변조 차단) + `version_guard(vt)`(미등록 축·봉인 불일치 감지, 02 §6.3 재추출 트리거)로 단일화. `pipeline_runner` 하드코딩 4지점을 봉인 상수로 치환(파편 0). version 축 변경 시 bump→회귀(10 §3) 게이트에 진입. **Spec 0.1.1→0.1.2.** TDD — `test_versioning` 신규 5개(봉인 일치·복사 불변·가드 허용/드리프트·미등록 축) — 스위트 495→**500개 통과**(회귀 0).
- **Authoritative claim source span·버전 보존 (design 02 §2, 03 §6.2, 09 §3, MVP #3).** MVP 최종 성공 기준 #3 충족. 추출 단계(`ClaimCandidate`)가 버전(`ontology_version`·`extraction_model`)을 보유하는데 **materialize 승격 시 버려지던** 것을 보정 — `Assertion`에 `ontology_version` 필드 추가, `materialize`가 claim 버전을 승격, `assertions` 테이블에 `ontology_version` 컬럼 추가(영속·조회·`assertions_as_of` 반영). `ClaimCandidate`에 `ontology_version`·`extraction_model`을 dataclass 필드로 승격(to_row 가 파생이 아닌 실제 값 사용). 추적 중개자인 `AssertionEvidence.ontology_version` + `get_evidence_provenance` trail 의 claim step 이 버전을 노출 — 검증 가능 문장이 **원문 span(char offset)+온톨로지 버전**까지 감사되는 DoD ② trail 완결. **스키마 변경 → Spec version 0.1.0→0.1.1.** TDD — `test_assertions` +1·`test_provenance_span` +1 — 스위트 494→**495개 통과**(MinIO 가동 시, 회귀 0).
- **MVP 표-실제 정합 정리 (#1/#2/#6/#8).** MVP 최종 성공 기준 표가 실제 구현과 불일치(구현 완료 항목이 ⬜로 남음)하던 것을 교차 검증 후 반영: **#1**(raw 104,411(arXiv)+143=104,554건 수집, Q4 게이트 ② 실행) · **#2**(`bulk_pipeline` postgres SoT→replay→Neo4j 전체 재처리 실측) · **#6**(`replay_graph_at_tx`·`assertions_as_of`/catalog as_of — 시간여행) · **#8**(`get_evidence_provenance` char span 왕복, ADR-305) → ✅. 기존 ✅ #9·#5와 합쳐 **✅ 6개**. **⬜ 유지 4개는 실질 미완으로 판정**: #3(Assertion에 version tuple 미영속), #4(골든 로드 비면 — 수치 미측정), #7(Phase 3), #10(version 봉인 회귀 미정비). 표 반영은 구현·실행 증거 검증 후에만 — 과대 주장 없음.
- **동일 근원 파생 출처 독립 중복 계산 방지 (design 11 §1.4, 04 §4.2, 09 §3).** MVP 최종 성공 기준 #5 충족. `assertion_evidence`의 `independent_source_count`가 §1.4 공식의 **둘째 항(지지 근거로 등장하는 `independent_addition_doc_ids[]` — 해당 claim에 새 증거를 더하는 문서) 가산**을 누락하고 root 축소만 하던 것을 보정(under-count 극복). 지지 근거 문서 distinct(root_source) + 독립 추가 문서 중 해당 claim을 지지하는 것만 가산(04 §4.2 disjoint, root ∉ independent_addition). TDD — `test_assertion_evidence` 신규 3개(독립 추가 가산·비지지 무가산·평범 파생 기존 계약 유지) — 스위트 512→**515개 통과**.
- **MVP #9 — 문서당 비용·처리 시간 측정 (design 10 §1.4).** MVP 최종 성공 기준 #9를 충족. `pipeline_bench.py`(순수 함수 — `throughput`·`per_doc_stats`·`llm_cost_per_doc`)와 `PipelineResult.per_doc_elapsed_ms`(문서별 벽시계 수집, 파싱 실패 제외) 추가. **실측 (실신호 본문 138건, 결정적 체인 단일 프로세스)**: throughput **13.9 docs/s**(MVP 목표 50 미달 — 단일 프로세스 in-memory, 분산 batch는 Phase 4) · per-doc **p50 22.6ms**·p90 118.9ms·p99 195.9ms·max 201ms · 문서당 LLM 비용 0(결정적 체인 LLM 미사용). `slo-gate`(비차단) 분류 유지 — 50 docs/s는 부하·분산 단계에서 재측정. TDD — `test_pipeline_bench` 신규 9개 + `test_pipeline_runner` per-doc 배선 1개 — 스위트 502→**512개 통과**.
- **실신호 본문 소스 확장 — AMD IR RSS 정식 승격 (design 04 §1.4, #6).** 설계가 Phase 1+ 로 유보한 "AMD IR RSS(공식 2nd)"를 실신호 본문 소스로 정식 추가·승격. `collect_large.SOURCES`·`signal_source_runner.SIGNAL_SOURCE_IDS`에 `official-amd-ir` 등록(robots.txt 개방 확인 — `anthropic-ai` 차단 없음, 피드 실측 200). 10건 수집 후 실신호 재측정: **claims 240→299(+59)·엣지 49→70(+21, acquires/partnership/Instinct 공급망 관계)** · replay 237노드·**quarantine 0** · 조회 **p95=2.36ms** — **Q4 3게이트 전항 PASS 유지**(500ms SLO 대비 여유). **Q6 관측**: 현재 MVP 스케일(10만, template)은 Iceberg 승격 트리거(Scale/100만) 대비 **여전히 관측 미대상** — 승격 결정은 그대로 유보(게이트 계약 유지). 전 과정 TDD — `test_collect_large` AMD 등록 +1 — 스위트 501→**502개 통과**.
- **실신호 본문 Q4 재판정 + 재현 러너 (design 06 §9, ADR-304·601·602).** 이전 판정이 유보한 "1e6/p95-500ms 는 실신호 본문 소스 확보 후 재판정"의 재료를 실제로 확보해 **실데이터 엣지 그래프에서 Q4 재판정**. `signal_source_runner`(신규 재현 러너 — `SIGNAL_SOURCE_IDS` 전용 반도체 언론 선택·파이프라인→postgres SoT→replay→Neo4j→Q4 조합)로 실신호 본문 128건(nvidianews 55·semiengineering 73) → **log 243 mutations·replay 194노드·49엣지**(announces 33·supplies 16, **quarantine 0**)·Neo4j 적재 420ms·조회 **p95=0.66~1.16ms**(970회) — **3개 게이트 전항 PASS, Memgraph 트리거 없음**. 합성 10만 조회 보증은 유지. 전 과정 TDD — `test_signal_source_runner` 신규 5개 — 스위트 496→**501개 통과**.
- **object 바인딩 + 공급망 엣지 배선 (design 02 §2.5·05 §3·03 §7, ADR-302·602).** 실신호 본문(RSS) 그래프가 **엣지 0**이던 근본 갭 해소: ① 결정적 extractor 가 predicate 이후 첫 해소 엔티티를 **object 로 바인딩**(정규 삼항 완성 — `_object_for`, "TSMC supplies NVIDIA"→subject/object) ② 파이프라인이 승격 claim 의 **subject/object 엔티티 노드를 create_node**(entity_id 별 **멱등**) 배선 + **create_edge**(subject--predicate-->object) 기록 — replay 시 dangling_ref quarantine 없이 실제 공급망 관계 엣지가 그래프에 실존(불변식 §3-4). **실측**: RSS 본문 128건 → promoted 188 · log_edges 49 · **graph_edges 49 · quarantined 0** (관계 `announces` 33·`supplies` 16) — 이전 edges=0·quarantine 49 대비 해소. 전 과정 TDD — `test_claims` object 2개 + wiring 엣지 생존 1개 — 스위트 493→**496개 통과**.
- **10만 실행 완료 + Q4 게이트 ② 최종 판정 (design 06 §9).** 날짜 윈도우(S50)·resumable·규칙 확장으로 로컬 raw **104,544건** 수집(목표 10만 초과), 신호 슬라이스 풀 파이프라인 postgres SoT→replay→Neo4j 적재·조회 **p95=1.08ms**. 후처리 병목 2종(canonicalize·conflict O(n²)) 선형화 커밋으로 대량 실행 완주. **Q4 3개 게이트 전항 PASS, Memgraph 교체 트리거 없음** — 게이트 ② 충족. 실제 그래프는 결정적 extractor 의 arXiv abstract 신호 희소(edges=0)로 실스케일 조회 부하 미달 → 합성 그래프로 10만 단위 측정 보증, 1e6/p95-500ms 는 실신호 본문 소스 확보 후 재판정(게이트 계약 유지). 남은 후속: 실신호 본문 소스(신호·엣지 증대)·Q6 Iceberg 관측.
- **모순 후보 O(n²)→후보-최적 blocking (P1 병목, judge=None 경로).** 캐노니컬 선형화(위) 후에도 같은 구조의 `find_conflict_candidates` 전쌍 O(n²)이 큰 (subject,predicate) 그룹에서 폭발하는 것을 발견. 후보는 ①polarity 상충(반대 polarity cross) ②같은 polarity ∧ object 다른 쌍 뿐이므로, **전쌍 대신 polarity/object 버킷 간 cross**(출력-최적)로 대체 + 결정적 정렬(claim_id_a,b) 보존. 신규 `test_large_group_conflict_linear_not_pairwise_stall`(300 same-subject→5625 polarity 후보) — 스위트 492→**493개 통과**.
- **캐노니컬 후처리 O(n²)→선형 최적화 (P1 병목, judge=None 경로).** 104k 대량 파이프라인 실행 중 후처리가 11분째 정체(mutation 255 고정, CPU 100%) — 원인: `canonicalize_claims`의 **그룹 내 전쌍 O(n²) 비교**. 신호 밀도 60× 상승으로 특정 (subject,predicate) 그룹에 claim 수천 개가 모여 폭발. `judge=None`(결정적 bulk) 경로에서 equivalence 는 ①같은 surface_fragment ②같은 seg 겹치는 span 뿐이므로, **전쌍 대신 선형 blocking**(surface별 union + seg별 span 정렬 후 인접 중첩 union)으로 대체. LLM(`judge` 주입) 경로는 의미상 임의 쌍 판정 가능하므로 전쌍 유지. 신규 `test_large_same_surface_group_linear`(300 동일표면→단일 캐노니컬, 회귀 방지) — 스위트 491→**492개 통과**.
- **arXiv 날짜 윈도우 수집 — 10k 페이징 한계 우회(S50)로 100k 달성 경로.** `collect_arxiv` 가 단일 쿼리를 `start` 로만 페이징해 **`start>~10k` 에서 arXiv 500**(실측)으로 10만에 도달 불가능함을 발견. 해결: `arxiv_windows()`(순수 계획 — 각 윈도우 start<10k)·`_arxiv_date_windows()`(과거로 후진하는 월 구간)·connector `date_window`(config → `submittedDate:[X TO Y]`, 공백 보존 `quote_via=quote`) 추가, `collect_arxiv(total, windows)` 가 각 날짜 윈도우를 독립 쿼리로 페이징. `--windows N` CLI 노출. **실측 검증**: 미수집 과거 연대(2018-01) 100건 중 **96건 신규** 획득(단일 쿼리로는 불가능) — 전략 유효. 재실행마다 미수집 연대를 채워 100k 로 누적. 전 과정 TDD — `arxiv_windows`·`_arxiv_date_windows` 테스트 +3 — 스위트 488→**491개 통과**.
- **collect resumable — 페이지 지속 5xx가 전체 수집을 죽이지 않음 (S49+).** 100k 연도 실행 중 arXiv 페이지가 지속 500(재시도 8회 소진)을 반환해 `collect_arxiv` 가 예외 전파로 **크래시** — 한 페이지 때문에 전체 100k 실행이 중단되는 안정성 갭 발견. `collect_arxiv` 페이지 루프에 **지속 5xx 처리**: 재시도 소진 5xx를 `errors`로 집계하고 **다음 배치로 계속**(resumable). 기존 `_with_retry` 단위 계약(소진 전파)은 유지, 상위 루프가 멱등·계속을 보장. 전 과정 TDD — 신규 `test_collect_resumable.py` — 스위트 487→**488개 통과**.
- **결정적 predicate 규칙 공급망 도메인 확장 — arXiv abstract 신호 밀도 ↑ (design 04 §5.1, 05 §3).** 10만 연도 대량 실행 중 신호 희소 문제 발견: 결정적 extractor 규칙이 발표/이벤트/제품 역량(conference call·webcast·powers/enables)에만 있어 실제 수집된 데이터의 신호가 낮았다. `. 위에 **공급망 도메인 규칙 추가**: `produces/manufactur/suppl/fabricat` → `supplies(manufacturing)` · `supplies chips/processors/gpus` → `supplies` · `partnership/agreement/alliance` → `partners(partnership)`. 도메인 정의(02 §5.1 공급망)에 부합, 결정성 유지. **실측**: 로컬 raw 500건 샘플에서 claims 240(≈48%) — 기존 0.8% 대비 **약 60× 신호 상승**. 전 과정 TDD — `test_claims.py` 신규 2개 — 스위트 485→**487개 통과**.
- **Q4 재구축/증분 측정 + 게이트 ② 실측 판정 (design 06 §9).** 하니스에 **`measure_rebuild`**(full vs incremental 벽시계 비율, 3번째 게이트) 추가 — `delta_only_graph` 로 동일 규모 신규 그래프를 두 방식으로 적재해 비율 산출. TDD — `test_neo4j_q4_harness.py` +1 — 스위트 484→**485**. **Q4 전 게이트 실측** (가동 Neo4j, 2026-08-11): 실제 대량 실행(11,361 raw→ postgres SoT 96 노드→ Neo4j p95=0.8ms) · 합성 5만→27,516 노드 적재 129.4s·**p95=0.6ms** · **재구축/증분 비율=1.06** — 3게이트 전항 PASS, Memgraph 교체 트리거 없음. 1e6·p95-500ms 는 10만 문서+ 이후 조회 워크로드에서 재판정(게이트 계약 유지).
- **대량 실행 드라이버 — 결정적 파이프라인→postgres SoT→replay→Neo4j (design 06 §9, ADR-304/601/602).** Phase 1 10만 문서 기준선을 실제로 흘리는 **오케스트레이션 경로가 없던 것**을 `pipeline_bulk_driver.py` 신설 — `bulk_pipeline(metas, zone, mutation_log)`가 `run_pipeline`(결정적 체인, postgres `graph_mutations` SoT 기록)·로그 **replay**(ADR-304)로 그래프 재구축·`load_and_measure_neo4j`로 가동 Neo4j 적재+Q4 실측을 조합한다. `PipelineResult.elapsed_ms`(벽시계) 추가. 각 단계 로직은 기존 단위검증, 본 모듈은 조합만. TDD — 신규 `test_pipeline_bulk_driver.py` 3개(로그 replay·무로그 분기·반환 계약) — 스위트 481→**484개 통과**. 라이브: 로컬 raw **11,361건**을 postgres SoT 로 대량 실행(측정 기록 하단).
- **Q4 한계 측정 하니스 (design 06 §9, Phase 0→1 게이트 ② 준비).** Q4 게이트(노드≥1e6·p95≥500ms·재구축>증분10×)를 **실측할 도구가 없던 것**을 `neo4j_q4_harness.py` 신설 — 결정적 합성 그래프(`synthetic_graph`)를 가동 Neo4j Community 에 MERGE 적재(`measure_load`)·샘플 인접 조회(`measure_query_latency`→p95)·판정(`evaluate_q4`, 상수 `NODE_GATE`/`LATENCY_GATE_MS`/`REBUILD_RATIO_GATE`) — 재현 가능한 벤치 하니스. 그래프 생성·판정은 순수 함수(오프라인 단위검증), 측정은 통합(가동 시 실측, 오프라인 skip). 전 과정 TDD — 신규 `test_neo4j_q4_harness.py` 11개 — 스위트 470→**481개 통과**. 실측 판정은 Phase 1 **10만 문서 기준선 부하**에서 수행(Q4 게이트 최종 판정).
- **time-travel — 그래프 축 transaction-time AS-OF 재현 (design 03 §6.3, ADR-604).** assertion-축 time-travel(`assertions_as_of`·catalog `as_of_tx`)은 이미 구현인데 **그래프(노드/엣지) 축은 `replay_graph`가 전부 재생만 하고** "특정 관측 시점에 시스템이 믿던 그래프"를 재현할 read 경로가 없던 것을, `graph_replay`에 **`replay_graph_at_tx(mutations, tx_at)`** 추가 — `graph_mutations` 로그를 `tx_time ≤ tx_at` 까지 잘라 순서대로 재생해 그 시점의 materialized graph 를 재구축(03 §6.3·§7.2 — superseded/delete 도 로그에 남아 과거 상태 그대로 조회, 불변식 §3-3). assertion-축과 병행 시 "관측×유효" 재현 계약 완결. 전 과정 TDD — 신규 `test_graph_time_travel.py` 5개 — 스위트 465→**470개 통과**. 후속: 10만 실제 대량 실행 + Q4 부하 스트레스.
- **트랙 b — Neo4j read path 배선 (design 06 §2.1·§8.1, ADR-601/604).** orphaned 스토어(적래만 되고 read 미배선)를 읽기 경로로 연결 — `Neo4jGraphStore`에 `node_detail(node_id)`(엔티티 상세 조회)·`reconstruct_graph()`(Neo4j 노드·엣지를 읽어 `GraphService`로 재구축) 추가. 재구축은 ADR-604 replay 의 **store-backed read 백업**으로, `Entity` 노드·엣지 원형 보존 + etype 안전 필터(비 알파벳/`_` 제외) 재사용. 전 과정 TDD — `test_neo4j_graph_store.py`에 신규 2개(재구축 read-back·빈 상태 no-op) — 스위트 463→**465개 통과**. 라이브 데모: 적래 그래프(2노드·1엣지)→`reconstruct_graph` nodes 2·org-tsmc 엣지 1 재구축. 후속: time-travel.
- **트랙 a — viewer 조사 그래프(War Table) 렌더링 (design 09 §2.2 War Table).** 조사 결과에 그래프가 없던 것을, `_api_investigate` 응답에 **조사 subgraph**(entities·relationships·relation_paths·independence_summary) 추가 + `loadInvestigation` JS 가 **War Table**(노드·관계·경로·독립출처) 렌더링. subgraph 는 기존 `ApiFacade.get_investigation_graph`(트랙 B) 재사용 — 파사드 단위검증과 정합. 전 과정 TDD — 신규 `test_viewer_graph.py` 1개 — 스위트 462→**463개 통과**. 후속: Neo4j read path(트랙 b)·time-travel.
- **트랙 C — 5개 조사 질문 end-to-end + DoD ② source-span provenance (design 07 §3-5·09 §2.3, ADR-703).** 조사 모듈(S43–S47)의 단위테스트만 있던 것에 **pipeline→조사 루프→보고서 end-to-end 드라이버**를 추가 — `run_pipeline` 결과의 assertions 로 ABOUT 그래프(별도 재구축) 구성, 5개 조사 질문(Subclaim) 정의, `InvestigationRunner`→`Synthesizer` 실행, audit passed·evidence-first(asserted 문장 claim_ref 필수) 검증. **DoD ② 충족 핵심**: `ApiFacade.get_evidence_provenance` trail이 기존 claim→doc 에만 끝나던 것을 **`extraction_records` 의 segment_id·char_start/char_end span step 을 연결**(파이프라인이 이미 영속한 char offset을 왕복·노출, 03 §8 ADR-305). 전 과정 TDD — 신규 `test_investigation_e2e.py`·`test_provenance_span.py` 2개 — 스위트 460→**462개 통과**. 후속: viewer 그래프 렌더링·Neo4j read path·time-travel·10만 실제 대량 실행.
- **트랙 B — 기본 graph explorer: investigation-subgraph 엔드포인트 배선 (design 09 §2.2, ADR-906).** 기존 `get_graph_node`/`get_graph_expand`(노드·인접)만 있고 조사 subgraph 엔드포인트(`GET /v1/investigations/{id}/graph`)가 미배선이던 것을, `ApiFacade`에 `GraphExplorer` 배선 + `get_investigation_graph(subject_id, hops)` 메서드 추가 — investigation subgraph(subject entity·claims·evidence·relationships)+relation_paths+independence_summary 반환(progressive disclosure 시드, 06 §8.1·§8.3). read-only(불변식 §3-3). 전 과정 TDD — `test_api_facade.py`에 신규 1개 — 스위트 459→**460개 통과**. 라이브 데모: org-a 서브그래프·claims·relation_paths 반환. 후속: viewer 그래프 렌더링·Neo4j read path·time-travel.
- **트랙 A — parsing·dedup 실배선 (design 04 §4, ADR-403).** `Deduplicator`(S4)가 실제 파이프라인(run_pipeline)에 배선되지 않고 `dup_clusters`가 안 채워지던 것을, `_run_chain`이 각 doc 의 clean text·publication_time·source_type 을 수집한 뒤 `Deduplicator().dedup(...)` 실행 + `persist_cluster` 로 영속하도록 배선. `PipelineResult.clusters` 필드 추가. **결정적 cluster_id** — 기존 `new_ulid("clus")`(비결정적)를 `sorted(members)+DEDUP_VERSION` 해시로 교체(04 §4.2 재생성 계약, extract_smoke 의 순번 치환 우회 제거). 전 과정 TDD — 신규 `test_pipeline_dedup_wiring.py` 3개 — 스위트 456→**459개 통과**. 라이브 데모: 중복 2건→단일 클러스터(root 이른 것·member 파생). 후속: 트랙 B(explorer)·C(조사 질문+DoD②).
- **후속 ③ — Neo4j 그래프 적래 + explorer (design 06, ADR-601/602/603).** 재생 그래프(`replay_graph`)의 authoritative 노드·엣지를 가동 중인 **Neo4j Community**(bolt 7687)에 적래(MERGE)하고 조회 — `Neo4jGraphStore`(`neo4j_graph_store.py`) 신설. 노드=`:Entity`(공통 `id`+props, design 06 §2.1 단순화), 엣지=`(:Entity)-[:ETYPE]->(:Entity)`. §2.3 `entity_id_unique` 제약이 MERGE idempotency 방어선 — 중복 적래 노드 없음(불변식 §3-6). explorer `query_neighbors`(인접 조회). 드라이버 `neo4j` 추가. 전 과정 TDD — 신규 `test_neo4j_graph_store.py` 3개 — 스위트 453→**456개 통과**. 라이브 데모: 재생 그래프(2노드·1엣지)→Neo4j 적래 node_count 2→neighbors 양방향. Q4 부하 스트레스·Memgraph 전환·세부 라벨/Provenance 는 후속.
- **후속 ② — 파이프라인 → postgres SoT 배선 (design 03 §7, ADR-602·304).** `run_pipeline` 이 승격 claim 의 그래프 mutation 을 in-memory `Gate` 에만 두고 postgres 로그에 안 쓰던 것을, 선택 `mutation_log`(① `PostgresMutationLog`) 제공 시 **승격 claim 을 `create_node {id}` + version_tuple 로 `graph_mutations` SoT 에 기록**(ADR-602 — 그래프 변경은 로그로만)하고 그래프 재구축을 **`replay_graph`(⑤)로 수행**(ADR-304 실경로). 미제공 시 기존 in-memory `_build_graph(gate)` 유지(파괴 없음). 전 과정 TDD — 신규 `test_pipeline_postgres_wiring.py` 3개(NVIDIA 대표 HTML) — 스위트 450→**453개 통과**. 라이브 데모: 파이프라인 승격 2건 → postgres create_node 로그 → fresh replay nodes 2 동일. 후속: Neo4j 적래(후속 ③).
- **후속 ① — MinIO ↔ 파이프라인 실배선 + fetch.json 완비 (design 03 §2, ADR-301).** 수집(`collect_large._save_zone`)과 파이프라인 입력(`load_raw_zone`)이 로컬 fs 를 쓰는 한편 ②의 MinIO 저장소가 미배선이던 것을 연결 — `_save_zone`에 `minio_store` 백엔드 옵션(로컬 fs default 유지, content-hash doc_id 로 재개 스킵), 신규 `load_raw_zone_minio`가 MinIO `raw/<source>/<doc>/{content.bin,fetch.json}` 로부터 파이프라인 meta list(`[{source_id,url,doc_id,content}]`) 재구성(동일 계약). fetch.json 에 governance(11) 필드 **license/robots_allowed** 완비. 전 과정 TDD — 신규 `test_minio_pipeline_wiring.py` 4개 — 스위트 446→**450개 통과**. 라이브 데모: 수집→MinIO 객체→재수집 idempotent→파이프라인 입력 재구성. 후속: 파이프라인 postgres 배선(후속 ②)·Neo4j(후속 ③).
- **저장 계층 키스톤 ⑤ — postgres SoT 로그 재생 → 그래프 재구축 (design 03 §7, ADR-304).** ①의 postgres `graph_mutations` SoT(영속만 되고 미소비)를 `GraphService`(Applier/replay, design 06 §3)로 **순서대로 재생**해 materialized graph 를 재구축 — `graph_replay.py`(`events_from_mutations`·`replay_graph`). payload/jsonb 원형 보존 전달, 동일 `idempotency_key` 중복 no-op(불변식 §3-6). ADR-304 DoD("로그→동일 그래프 재구축, blueprint §21-2") 실증. 전 과정 TDD — 신규 `test_graph_replay.py` 4개 — 스위트 442→**446개 통과**. 라이브 데모: SoT 로그 create_node·edge → replay → nodes 2·edge 1 재구축. 후속: 파이프라인 postgres 배선·Neo4j 적재.
- **저장 계층 키스톤 ③ — `extraction_records` 테이블 + provenance 게이트 (design 03 §8, ADR-305).** 추출 기록이 in-memory(↓ `RawStore` dict)로만 존재하던 것을 **curated zone에 `extraction_records` 테이블(§8.2 스키마)**로 영속화 — `persist_extraction_record`(element_id → **결정적** `ext-` id, 동일 element 재영속 no-op)·`extraction_records()`·`has_extraction_record` 추가. **ADR-305 게이트 강제**: `gate.py` provenance 검증에 `missing_provenance_record` reason 추가 — `provenance_ref`(=extraction_id[]) 없는 claim은 authoritative 진입 금지→quarantine. 파이프라인 `_run_chain`이 추출 시 record 영속·`ClaimCandidate.provenance_ref` 부여(frozen dataclass `object.__setattr__`)해 실제 흐름에서 게이트 충족. 전 과정 TDD — `test_gate` 헬퍼 갱신 + 신규 `test_extraction_records.py` 6개 — 스위트 436→**442개 통과**. 후속: MinIO raw 참조 연결·fetch.json 완비.
- **저장 계층 키스톤 ② — MinIO raw 객체 스토어 (design 03 §2, ADR-301).** raw zone(§2.1)이 로컬 fs+in-memory `RawStore`뿐이던 것을 가동 중인 **minio:RELEASE 컨테이너**에 영속화 — `MinioRawStore`(`minio_raw_store.py`) 신설. §2 객체 레이아웃(`raw/<source_id>/<doc_id>/{content.bin, fetch.json}`)대로 객체 키를 두고, **content-hash idempotency**(동일 bytes=동일 doc_id, no-op)와 **ADR-301**(동일 url 변경분=새 doc_id 보존, 덮어쓰기 금지)을 보장한다. fetch.json은 §2.2 핵심 필드(doc_id/source_id/url/content_hash/fetched_at) 기록 — license/robots 등 governance(11) 필드는 후속. 전용 버킷 격리 테스트로 **round-trip·idempotency·ADR-301·객체 레이아웃** 검증. 전 과정 TDD — 스위트 430→**436개 통과**. 드라이버 `minio` 추가(오프라인 시 importorskip skip). scope ② — provenance 게이트(§8)는 후속 증분 ③.
- **저장 계층 키스톤 ① — PostgreSQL `graph_mutations` SoT + replay (design 03 §7, ADR-304/307).** append-only mutation log가 in-memory `list`뿐이던 것을 가동 중인 **postgres:16 컨테이너**에 영속화 — `PostgresMutationLog`(`postgres_mutation_log.py`) 신설. §7.1 스키마(`mutation_id PK, idempotency_key UNIQUE, op, payload, resolution_ref, actor, version_tuple, correlation_id, tx_time`)대로 `graph_mutations` 테이블을 생성하고, 기존 `MutationLog`의 append-only/idempotency 계약(불변식 §3-3/§3-6)을 유지한다. 전용 테이블명 격리 테스트로 **append-only·순서 보존 replay(ADR-304)·동일 `idempotency_key` no-op**을 검증. 전 과정 TDD — 스위트 424→**430개 통과**. 드라이버 `psycopg[binary]` 추가(오프라인 시 테스트 skip, 연결 없이 424 유지). scope ① — MinIO raw(§2)·extraction_records 게이트(§8)는 후속 증분.
- **골든·승격 기준선 재검증 (Phase 0→1 게이트 ③).** curated zone의 골든 22건 + 저장 기준선(vt-adf4482…, superseded)으로 평가 스위트(S36)·promotion dry_run(S38–40) 재실행: canonicalization F1=1.0/P=1.0 유지, **회귀 0건, suite PASS**, dry_run INITIALIZED/passed (S41 리포트와 동일 상태 재현). 호스트 venv와 prototype 컨테이너 양쪽에서 동일 결과 — 게이트 ①·③ 충족, ②(Q4/Q6)는 Phase 1 부하 측정 예약.
- **1만 문서 샘플 확보 (Phase 0 완료 조건 ①).** 컨테이너 기반 `collect_large --limit 10000 --sec 5` 실행으로 arXiv 1만 처리(saved 9,979 / skipped 21 / errors 0) + RSS 30 + SEC 5 — 총 raw 11,361건. 과정에서 abs 페이지 스크레이핑이 anti-bot 429를 반복 유발(크래시 4회)해 **04 §1.4 정본 경로(API metadata CC0)로 수집 전환**: `ArxivConnector.discover_entries`(entry 원문 XML을 raw 문서로, 문서당 GET 0회, 페이지 1000건×10회), `_with_retry`(429/5xx·URLError/Timeout 지수 backoff·Retry-After 준수), 파서 Atom entry 지원(`extract_html` 분기). 전 과정 TDD — 스위트 407→424개 통과. 신규 XML 문서 파싱 E2E 검증(title/published/segments) 완료.
- **infra: docker-compose 스택 착수 (design 01 §6.1 부분 구현).** postgres/minio/neo4j/opensearch 4종을 버전 고정·healthcheck·named volume으로 구성, prototype viewer는 `--profile prototype` 컨테이너(python:3.12-slim, `data/` 볼륨 마운트, `VIEWER_HOST` 바인딩 주입)로 선택 실행. api/worker는 FastAPI 코드 확보 후, grafana는 메트릭 소스 확보 후 추가. 크리덴셜은 `.env` 주입(`.env.example` 갱신).

### 2026-08-03
- **D 항목 확정 — 잔여 Open Question·Phase 게이트 (Q4/Q6/Q3·Phase 1).** 남은 할일(D)의 측정 가능 기준을 설계 정본에 게이트로 명시 (Phase 1에서 측정·판정):
  - **Q4 (06 §9)** — Neo4j 한계 측정 게이트: 노드 ≥1e6, 그래프 조회 p95 ≥500ms, 재구축 > 증분 10× → Memgraph 물리 분리/교체 (ADR-601/603 저장소 추상 유지).
  - **Q6 (03 §9)** — Iceberg 승격은 Scale(100만)·증분 처리 단계에서 기준 수립 (Phase 1 관측 대기).
  - **Q3 (10 §7)** — ER은 결정적 exact match 채택으로 회피 해소; embedding/LLM ER 도입 시에만 dev 실측 + test 게이트(P≥0.97) 재적용 (데이터 다변화 후).
  - **Phase 0→1 진입 게이트** — 1만 문서 확보(사용자 arXiv 실행)·Q4/Q6 첫 측정·골든/승격 기준선 재검증. DoD ①②는 prototype에서 충족(S27/S37).
- **Q5 기준선 실측 (B1).** dev proxy(bunker-flash)로 실제 LLM 판정 3쌍(canonicalization)을 실행해 S42 usage 집계로 **토큰·비용 기준선 확보**: input 628 / output 160 tokens, **호출당 ≈0.0014 USD**(placeholder 환산). 문서당·조사당 비용 목표는 Phase 1 하이브리드 대량 실행에서 확정 (design 10 §1.4). 전체 arXiv 1만 수집(`--limit 10000 --sec 5`)은 사용자 실행 대기.

### 2026-08-03 (이전 — 새 기능 트랙)
- **새 기능 트랙 완성 — LLM 저장-기반 조사 에이전트 (S43–S47, 07 조사 루프).** 소비·평가 계층(S28–S41)을 **read-only·evidence-first**로 재사용해 조사 사이클을 prototype 완결:
  - **S43 evidence coverage** (07 §4·10 §1.3) — subclaim별 근거 coverage·gap·expected_info_gain.
  - **S44 Graph Explorer** (07 §3.3) — subgraph + relation_paths + independence_summary (S26/S29 재사용).
  - **S45 Counter-Evidence** (07 §3.6) — hypotheses + negative_queries + 모순 후보(근거·판정 이유 포함, binary 금지).
  - **S46 Investigation Runner** (07 §4) — coverage→explorer→counter-evidence 루프, 종료(coverage≥0.80 / no_new_evidence / budget).
  - **S47 Synthesis/Audit** (07 §9.3·README §3-5) — 결론 봉투(09 §4) + evidence-first report(asserted/fact는 claim_ref 필수) + Audit 위반 차단.
  - **공통 불변식**: Agent는 graph mutate 금지(§3-3), 무출처 문장은 prediction만(§3-5), 결정적·TDD, main 직접 머지.
- **평가·승격 트랙 완성 (S38–S41).** design 10 3/6 승격 게이트 종단 구현 + Q2/Q3/Q5 진행:
  - **S38 durable promotion gate** — last-promoted baseline을 zone 영속(10 §3.1, ADR-1003), active/superseded 승격 이력.
  - **S39 end-to-end promotion pipeline** — EvalSuite→PromotionGate 자동 승격/차단 (PROMOTED/BLOCKED/INITIALIZED).
  - **S40 5-axis version-aware** — 5축 tuple(03 §7.1) fingerprint baseline, 온톨로지 major bump 시 전량 재평가(revalidate_required).
  - **S41 phase0 report** — 실데이터 승격 적용 + Q3/Q5 최소 실측 (29 claim confidence 균일 0.8, 결정적-only → LLM 비용 미측정 표기).

### 2026-08-03 (이전 — Q3–Q6)
- **Q3–Q6 결정 및 Q5 진행(S42).** Open Question 상세 리스트업 후 권장안으로 확정:
  - **Q3 해소(회피)** — ER은 결정적 외부식별자 exact match만 자동 병합(05 ADR-507)이라 임계값 튜닝 대상 아님. 점수·LLM 고신뢰는 POSSIBLY 후보만 유지(precision-first).
  - **Q4·Q6 Phase 1 예약** — 그래프 DB 한계·Iceberg 트리거는 규모 부족(prototype in-memory·DuckDB/parquet)으로 관측 대기, 10만 문서 진입 시 06/03 정본에서 측정·확정.
  - **Q5 진행(S42)** — `ClaudeJudge`에 토큰·비용 누적 추가(`usage()`/`cost_usd()`, design 10 §1.4): LLM 판정 호출 input/output tokens 집계, token→USD 환산 placeholder. 기준선은 Phase 1 실제 LLM 하이브리드 실행에서 확정.

### 2026-08-03 (이전 — Q2, prototype)
- **Q2 해소 — near-dup MinHash 임계값 실측.** 초기 수집 395문서 MinHash Jaccard 분포를 측정: intra/inter 모두 0.5~0.7 겹침(진짜 복제만 0.9+), 단일 임계 신뢰 판정 불가 확인 → 설계 04 ADR-403의 3-tier(MinHash→embedding→LLM ③) 전략 재확인. prototype `JACCARD_THRESHOLD`를 (구)0.6 → **0.90**으로 상향해 확실한 복제만 병합, 애매(0.80~0.90)는 수준 ③ 위임. (구)0.6 오결합 후보 5152쌍 실증 제거, 신규 임계에서 진짜 복제 6쌍만 잡음. [04 ADR-403](./design/04-ingestion-and-parsing.md)에 실측 근거 기록.

### 2026-08-03 (이전 — Phase 0 prototype 구현)
- **Phase 0 prototype 전방위 구현 (S1–S37).** Review 확정 스펙(0.1.0)의 **결정적+LLM 하이브리드 파이프라인**을 TDD(Red→Green)로 구현·검증. git 이력(`protoal/plain commit`, `feat(S#)`), 단계 계획(`.claude/plans/s#-*.md`)이 기록 지점.
  - **결정적 체인** — 수집(S1–S3 SEC/arXiv) → 중복(S4) → 파싱(S5) → ER/해소(S6) → claim 추출(S7) → 게이트(S8, S13) → 캐노니컬(S9) → 모순(S10) → assertion(S11) → 그래프 mutation(S15–S18). **bitemporal AS-OF**(S25, ADR-606).
  - **LLM 계층** — ClaudeJudge(S21)·결정적-우선 하이브리드(S22)·판정 영속(S23), ADR-507 노-자동-병합 준수.
  - **통합** — `pipeline_runner` 단일 진입점(S24)·Investigation subgraph(S26)·통합 불변식(S27, 실버그 발견·수정).
  - **소비 계층 (read-only)** — Catalog cursor 페이징(S28)·어세션 근거·다차원 신뢰도(S29, 09 §4)·subject 결론(S30, 09 §3)·랭킹(S31)·API 파사드(S32, 09 §2/§3). browser viewer 실행(S32 후속, 로컬 확인).
  - **평가 계층 (read-only)** — P/R/F1 하네스(S33)·골든셋 영속(S34, 10 §2.3)·회귀 실행기(S35, 10 §3/ADR-1008)·평가 스위트 러너(S36, 10 §6). **전 계층 E2E 불변식**(S37) 추가.
  - **실데이터 검증** — 395문서 수집(소량, 수동 coronary), 29 assertion 전 계층 E2E 일관성. 전체 테스트 318 passed.
  - **Open Question 진행** — Q2 dup(MinHash), Q3 ER/컨피던스 임계값, Q5 LLM 비용 — 하네스·회귀 토대 완성, **dev 파티션 실측 튜닝은 후속** (10 §2.4/§3.2, ADR-1008 placeholder).

### 2026-08-03 (기존 — 설계 리뷰·스펙 확정)
- **초기 Scout 5종 선정 (Q1 해소).** 미국 중심 AI 반도체·데이터센터 공급망 도메인의 초기 소스를 웹 리서치(실제 API/RSS 검증)로 선정: **SEC EDGAR**(gov)·**arXiv**(research)·**CHIPS/NIST**(gov)·**NVIDIA Newsroom**(official)·**SemiEngineering**(press). 전 source 공식 API/RSS로 수집 가능(Easy), 재배포 전면 제한(`allow_redistribute=false`, 11 §5.4 정합). 상업 테크 프레스(EE Times·The Register·TechCrunch)는 robots.txt가 AI 크롤러(`anthropic-ai`/`ClaudeBot`)를 차단해 초기 세트 제외, TSMC는 Cloudflare 403 제외. 구체 스키마·라이선스·수집 제약은 [04 §1.4](./design/04-ingestion-and-parsing.md) 정본. Open Question Q1 해소.
- **설계 문서 Review 승격 (리뷰 패스 완료).** 12개 문서를 blueprint 충실도·7 불변식·상호참조·구현가능성 기준으로 병렬 심층 리뷰(문서별 11 + 상호 일관성 스윕 1). BLOCKER 3 + MAJOR 36 + MINOR 다수를 문서에 직접 반영 후 독립 재검증(전항목 PASS). 상태 `Draft`→`Review`. Spec은 0.1.0 유지(Stable 변경 아님).
  - **BLOCKER 해소:** (1) 06 Applier에 `unmerge` op 분기 추가(merge 가역성·불변식 3·4) (2) 02 미정의 `TimeInterval` 노드/`VALID_DURING` 엣지 폐기, valid time을 inline 속성 단일 표현(ADR-206) (3) 02 `Assertion` 속성표 신설 + Claim→Assertion materialization 계약(ADR-207).
  - **교차 결정(사용자 확정):** 독립 증거 수 = **출처(source) 단위**(11 §1.4 정본, 04 위임, 09 `independent_source_count`); ER LLM '동일' 판정 → **`POSSIBLY_SAME_AS` 후보만, 확정 병합은 인간확인/결정적 식별자로만**(precision-first, 05 ADR-507).
  - **정합 반영:** 버전축 4→**5축**(`extraction_code_version` 포함); 보고서 문장 `kind`→정본 `modality{fact,asserted,opinion,prediction}`(07↔09); mention ID `men-` 접두사 등록; quarantine 논리 분리(라벨)→확장 시 물리(06 ADR-603); `allow_redistribute` 재배포 게이트(04↔11); alert `campaign_id`→`investigation_id`; retention_class 정책(11); 03 promoted-claim 저장·assertions=projection 재정의; 08 hybrid fusion(RRF k=60)·embedding/reranker 핀(bge-m3); 09 authz·테넌시; 10 slo-gate 분리·held-out partition·회귀 tolerance.
- **설계 SSOT 착수.** `docs/design/` 신설. 백본 문서 작성: README(규약·ID 체계·불변식), 01-architecture, 02-ontology(v1.0.0), 03-storage-and-data-model.
- 하위 스펙 04–11 초안 작성 (수집·해소·그래프·LLM/에이전트·검색·API·평가·관측/거버넌스).
- **상호 참조 일관성 검수 완료.** 불변식 위반 없음. 수정 반영: (a) `graph_mutations.op` enum에 `unmerge` 추가(03/08/09) (b) Signal Spire 트리거 enum 09↔11 통일(정본 11 §4.1) (c) S7 idempotency key를 `mutation_id`→`idempotency_key`(stage input 해시)로 정정(01/11) (d) `MEMBER_OF` 엣지 02 §3 등재 (e) `segments.norm_char_end` 추가(양방향 offset 매핑) (f) `clus-` 접두사·운영 ID·버전 필드명 별칭 정합.
- **UI 목업 세분화.** 설계(blueprint §1.4 공간계층)에 따라 War Table 단일 목업을 8개 Citadel 공간 화면으로 세분화(`docs/mockups/`): index + Citadel Gate·Watchtower·Grand Archive·Hall of Witnesses·War Table·Council Chamber·Chronicle Vault·Signal Spire. 공유 셸 재사용, 렌더링 검증 완료. 일러스트 생성 프롬프트(`docs/mockups/illustration-prompts.md`) 추가.
- **목업 시각화 완료(마무리).** 자산 22종 연결·검증 완료:
  - 브랜드 문장 `crest-hero`(index·Gate), 화면 히어로/masthead 8종(pixel-art, `orc-citadel-hero.png` 스타일).
  - 캐릭터 초상 5종(orc-camp 복사): Council 8-Agent roster 아바타 + Gate 온보딩 + Hall 안내.
  - 빈 상태 6종 → `empty-states.html` 갤러리(no-data 조건부라 갤러리로 통합).
  - 처리 상태 2종: orc-camp 애니메이션 재사용(CSS `steps()`, `prefers-reduced-motion` 정지) — Watchtower 수집·Council 추론.
  - 일러스트 프롬프트 전량 픽셀아트·복붙용(preamble 인라인)으로 통일. 전 목업 asset 참조·링크·파싱 최종 검증 통과.
- 본 로드맵 신설.

## 6. 열린 질문 (Open Questions)

설계 확정 전 해소가 필요한 항목. 해소 시 해당 design 문서 ADR로 이전한다. (Q1은 2026-08-03 해소 → [04](./design/04-ingestion-and-parsing.md) §1.4, Q2는 2026-08-03 해소 → [04](./design/04-ingestion-and-parsing.md) ADR-403, Q3은 2026-08-03 결정 → [05](./design/05-resolution-and-extraction.md) ADR-507.)

| # | 질문 | 관련 스펙 | 상태 |
| --- | --- | --- | --- |
| Q3 | Entity Resolution accept/reject 임계값 | 05, 10 | ✅ **결정적 ER 채택으로 회피 해소** — 자동 병합 경로는 결정적 외부식별자 exact match뿐(ADR-507), 점수·LLM은 POSSIBLY 후보만 유지 → 임계값 튜닝 대상 아님 |
| Q4 | 그래프 DB: Neo4j Community 한계 | 06, 01 | ⬜ **Phase 1 부하 후 판정 예약** — in-memory prototype 규모로 측정 불가, 10만 문서 진입 시 06 정본·교체 비용 격리(저장소 추상) 유지 |
| Q5 | LLM 비용 목표 실측 | 07, 10 | 🟡 **집계 인프라(S42) + 기준선 실측(B1)** — dev proxy(bunker-flash) 실제 LLM 판정 3쌍: input 628/output 160 tokens, **호출당 ≈0.0014 USD** (placeholder 환산) |
| Q6 | Iceberg 승격 트리거 정량화 | 03, 01 | ⬜ **Phase 1 범위로 설계 확정** — DuckDB/parquet 단계에선 관측 미대상, 확장 시 트리거 기준 정량화 |
