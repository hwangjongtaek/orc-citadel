# Secrets / local env
.env
.env.*
!.env.example
!.env.production.example

# direnv
.direnv/

# Claude Code — 개인 로컬 설정 (머신별, 커밋 제외)
.claude/settings.local.json

# Python (prototype)
__pycache__/
*.pyc
.venv/
*.egg-info/
# DuckDB 로컬 DB
*.duckdb
*.wal

# OS
.DS_Store

# Collected sample data (04) — 커넥터로 수집한 원문은 커밋 제외
prototype/data/

# Node (design-system 테마 저작 도구 — 런타임 아님)
node_modules/
# design-system/ui 는 자체 의존을 두지 않고 mockups 의 것을 심링크로 공유한다
# (단일 react 인스턴스 보장). 심링크 파일 자체는 커밋한다.
!design-system/ui/node_modules
